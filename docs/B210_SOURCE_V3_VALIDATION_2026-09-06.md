# 来源关联 v3：旧错源误拒解决，窄带停发门仍未通过

基线 `d6c73069f23551ea4d54eabd6a9c535a49d57b00`。见
[执行前登记](B210_SOURCE_V3_PLAN_2026-09-06.md)及
[完整144例有界审计](B210_SOURCE_V3_AUDIT_2026-09-06.json)。

v3代码及独立合成评估完成，**候选整体验收仍失败，未接入实机资格门**。
既有49项软件测试通过；测试成功与候选验收失败分开记录。

## 结果及失败原因

| 源半带宽 | 同源通过 | 同谱错源通过 | 同源最小分离裕量 | 停发最大相关 |
| --- | ---: | ---: | ---: | ---: |
| 32 kHz | 22/24 | 0/24 | 0.582009 | 0.204889 |
| 125 kHz | 24/24 | 0/24 | 0.766333 | 0.103220 |
| 350 kHz | 24/24 | 0/24 | 0.853622 | 0.066455 |

72同源通过70个，72错源全部拒绝。两次失败都是保留的停发对照≤0.2门：

- 源种子91827、32 kHz、无偏置、剩余频差0 Hz：停发相关0.204889。
- 源种子91842、32 kHz、偏置2+1.5j、剩余频差65 Hz：停发相关0.202130。

两例的前段、后段同源和新错源分离裕量均通过，只因一个停发窗口超过0.2而拒绝。
更窄的带限噪声在4096点中有更少的独立维度，固定相关上限对带宽敏感。这里
停发输入是已知合成白噪声，不是实测空口干扰，也不是模型误分类。
未改规则、种子或剔除窄带样例；不能把其余48个较宽带样例通过重新写成原72例
验收通过或新的已验收频带范围。

旧开发误拒90708/90721在v3下通过，且v2输出与历史审计逐字段一致、仍为失败。
这两例没有计入独立72例。下一步需要为停发对照的带宽/相关样本数及16个窗口
联合检查给出依据，再预登记新样例；不直接把0.2改到刚好覆盖这两个观测值。

## 变更与边界

复用现有数值实现，抽取同谱错源计算供v2/v3共用。v3保持31个固定相位随机参考，
每个参考保留源谱幅度和真实DC、使用同等前段完整lag/频差搜索、后段固定验证。
比较最差同源窗与全部错源窗的最大值，固定分离裕量≥0.3。报告源有效bin数、
最强错源seed及全部31个最大值的hash。此方法以实际同谱搜索对照反映偶然匹配，
并不提供统计p值或已校准的RF/OOD误接收率。

原同源最低相关、停发≤0.2、频差/相位拟合、源有效bin门保持。原v1/v2行为不变，
新API不接入默认CLI、仿射推理或生产服务。`live_rf_qualified=false`、
`recognizer_available=false`、独立标签0；epoch-10/FP16/RF-v1不变。

49项软件回归含5项新增：旧误拒与历史v2一致、31对照确定性/输入不变、后段
不能反向影响31个前段拟合、最差窗口分离裕量和旧门不可绕过、非法输入在错源
计算前拒绝。矩阵独立于普通unittest发现，专用入口和exclusive marker限制一次
执行；第二次启动在任何矩阵评估前拒绝，原矩阵hash不变。

预登记的72×2矩阵仅运行一次，失败退出码1和完整失败结果保留。没有结果驱动
重跑，也没有新RF、模型或数据集操作。Python AST与diff检查通过。

## 清理

唯一暂存为 `/var/tmp/sdrharness-dev/b210-source-v3-906h/`，上限16 MiB，保留日志
及矩阵hash、有界结果、源码和登记。已核对无符号链接和占用进程后精确删除该
目录，测试目录b210-control-test-394053也确认不存在；审计`cleanup.verified=true`。
未连接NX/P201、未创建应用结果；用户语料和应用数据未触碰。
