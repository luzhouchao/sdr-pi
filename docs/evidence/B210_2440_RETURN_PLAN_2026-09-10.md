# 切回2440MHz，TX70/RX50一次单音预登记

用户要求再切回2.4GHz，选择历史成功频点2440MHz，沿用当前双端弹簧天线和TX70/RX50、幅度0.2。
外部B210 serial2508504、RF A/TX-RX/channel0；P201仅RX1/RX0/A_BALANCED，无同轴直连。
TX2440MHz、SINE+100kHz、rate2.5MS/s、BW500kHz、25000000样本/名义10秒；远端timeout35秒+2秒强停。
三段停发前/中/后，RX2440MHz、rate2.5MS/s、BW1MHz、manual50dB、settle500ms、65535ci16点/段，
精确最大786420字节、78.642ms非连续采样。point1000ms、Controller15秒、runner120秒，预计40秒。
AGX/NX目录 /var/tmp/sdrharness-dev/b210-2440-return-20260910f/，NX仅空目录；AGX scratch隔离缓存。
初检AGX可用809250226176字节，逐段复查。P201三条agx-sweep-<generation>-0路径逐段前登记。
停止为runner SIGINT/SIGTERM、专用generation cancel、具名TX owner INT，错误/超时结束，不重发。
预检身份/哈希/唯一sdrd和空闲RX/NX USB，后验两路完整恢复/health、TX退出、P201/NX目录消失。

复用已有2440MHz tone路径，不新增TX能力。分析增加仅2440MHz/TX70/RX50显式回放配置，旧默认不变。
沿用+100kHz±5kHz搜索和两个停发同bin及谱中位数差值各≥20dB，不降低门。
一次跨频段顺序比较，不能独立归因于天线频响、环境或硬件；不运行模型或重复采集。
最小原始证据逐文件登记哈希/请求身份/人工删除命令，清理本轮空日志/缓存，旧失败不改写。
