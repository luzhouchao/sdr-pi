# 3500MHz原增益TX70/RX50单次验证预登记

用户明确“就用之前的增益”，此前已核对2.440GHz成功记录为TX70/RX50、幅度0.2。
本轮频率仍3500MHz，沿用双端已接回弹簧天线与用户室内条件；无同轴直连。
只执行一次tone，B210 serial2508504 RF A/TX-RX channel0，TX3500MHz/+100kHz，
rate2.5MS/s、BW500kHz、gain70dB、ampl0.2、25000000样本/名义10秒，timeout35秒+2秒强停。
P201仅RX1/RX0/A_BALANCED，3500MHz、rate2.5MS/s、BW1MHz、manual50dB、settle500ms，
停发前/中/后三段，各65535ci16点，共786420字节/78.642ms非连续采样。
point1000ms、Controller15秒、runner120秒，预计40秒。停止通过runner信号、专用generation cancel和具名TX owner INT。
AGX/NX目录 /var/tmp/sdrharness-dev/b210-3500-tx70-rx50-20260910e/，NX仅空目录；
AGX scratch隔离测试。P201三条agx-sweep-<generation>-0路径逐段前登记。
初检可用809246498816字节，逐段复查。每组前后核对身份、哈希、唯一sdrd、RX/NX USB空闲及两路完整恢复、health和瞬时目录消失。

新增仅显式TX70/RX50组合，不把RX20配TX70、其他增益或RML扩展进来，旧默认不变。
沿用+100kHz±5kHz内候选，同bin对两个停发背景及相对谱中位数各≥20dB的门；不降低门。
传输/削顶失败保留，结束本次，不自动重发/升级。两个增益同时改变，不能分离TX/RX各自贡献。
保留最小原始IQ/元数据/报告/执行审计/日志/分析，登记SHA-256和人工删除；清理空日志/缓存/NX空目录。
不部署、不运行模型/训练/locked test，不修改FPGA/BOOT/EEPROM。
