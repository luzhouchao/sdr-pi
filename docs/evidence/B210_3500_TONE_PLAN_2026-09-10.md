# 3500MHz弹簧天线：一次低增益单音收发对照

基线d9625a3、codex/sdr-improvements。用户明确将实验收发改到3.5GHz，说明
当前室内可发射，并确认P201和N210均接宽频弹簧天线；中断后明确要求继续。
衰减器仅为购买咨询，不视为已同轴连接。本轮沿用空口接法：P201 RX1、
外部B210（用户称N210）RF A/TX-RX、UHD channel0、serial2508504。
天线型号、间距、3.5GHz驻波和输出dBm未测，按用户连接报告记载。

只执行一次停发前→发射中→停发后。TX中心3500000000Hz，SINE偏移+100000Hz，
名义RF单音3500100000Hz；rate2.5MS/s、BW500kHz、TX gain0dB、幅度0.2，
25000000个样本，名义10秒。gain0不是输出0dBm，不自动升增益、不重复发射。
远端timeout35秒、INT后2秒强制结束，样本计数同时限定发射；不读训练波形，
不运行模型或locked test。P201仅接收。

三次RX均为3500MHz中心、RX1/RX0/A_BALANCED、2.5MS/s、BW1MHz、manual20dB、
settle500ms、65535ci16_le点、aggregate1、capture deadline1000ms；每次262140
字节/26.214ms，总786420字节/78.642ms样本。Controller每次外部15秒，
整runner外部120秒，预期约25–40秒。停止走runner SIGTERM/SIGINT、专用
generation cancel和具名TX owner INT；不在接收忙时另开普通状态连接。

AGX及NX本轮目录均为`/var/tmp/sdrharness-dev/b210-3500-spring-20260910a/`。
AGX scratch隔离测试/缓存，NX只需空工作目录，无源文件/权重/IQ暂存。
P201三个`/tmp/sdr-agent-dev/agx-sweep-<generation>-0`精确路径在各RX前登记。
初检AGX809876910080字节可用，执行前和逐次复查。预检要求一个预期sdrd、
已验证RX1、全部buffer/scan空闲、无已有接收连接，NX身份及USB无占用。
两路完整gain/mode/port和公共LO/rate/BW、全部scan/buffer逐次轮询恢复；
最终same daemon/hash、health、TX进程退出、三个P201目录消失。

3.5GHz仅开放此tone/RX20/TX0案例，不把旧2.4GHz RML或gain70授权扩展过来。
现有sdr-agent/sdrd不升级；旧冻结RF-v1和识别准入不变。UHD记录实际freq/
rate/BW/gain，三次原生报告需满足字节/样本/身份/健康/timeout及零已报告
drop/overflow/clipping。失败保留，不放宽门。

复用原单音判断：+100kHz±5kHz内取发射中峰，比较同一个观测FFT bin在
两个停发对照中的功率；相对二者各≥20dB且高于发射中谱中位数≥20dB，
才通过单音链路工程门。未通过也结束本次，不改搜索范围或追增益。该算法
遗留dbfs使用32768数值参考，只使用差值dB做门，不称为12bit ADC校准dBm。
仅证明单音可见性，不证明调制保真、独立标签或模型可用。

最小保留三份IQ、SigMF/原生报告、计划/操作审计、UHD日志、单音分析及必要
图表，登记精确字节/SHA-256/source/request/session和人工删除命令。
删除本轮测试/缓存/空日志及NX目录，保留旧证据不动。模型及训练继续暂停。
