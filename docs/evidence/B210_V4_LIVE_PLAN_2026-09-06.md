# 固定v4来源门的B210/P201实收与复偏置复验预登记

基线 `9e656f1a5851d1b10f13ac00a118d6ad1215c5a4`。用户已授权NX+B210天线、
2.4 GHz有限外部发射；P201仅RX1/RX0/A_BALANCED接收，AGX处理/推理。复用已验证
有限TX/RX工具，生产服务和冻结epoch-10/FP16/RF-v1不变。

## 固定预算与停止

AGX/NX目录各为 `/var/tmp/sdrharness-dev/b210-v4-live-906j/` 和
`/var/tmp/sdrharness-dev/b210-v4-tone-906j/`。最多两个burst，无参数重试：

- B210 serial2508504，RF A/TX-RX/channel0，LO2.440 GHz，TX gain70，peak0.2。
- tone：+100 kHz、2.5 MS/s、BW500 kHz、25000000样本，名义10秒；P201 RX
  中心2.440 GHz、2.5 MS/s、BW1 MHz、manual gain40。
- RML：已有四个train行102400/102401/102403/102404，标称ID0、+30 dB。
  2.1 MS/s、BW1.5 MHz、21000000样本，名义10秒；P201 RX同rate/BW、gain40。
  源tile SHA256必须为c95ac58c1fd91ef4da992622dbdf70a9bc5884d0941419083451473a052f534e。
  只统一幅度缩放，不加软件噪声，不读取新行或locked test。
- 每种波形各停发前/发射中/停发后65535 ci16，settle500ms、timeout1000ms；
  每次262140 bytes、计划估计1500ms，总RX上限1572840 bytes。实际控制/传输
  时间另记录，不把计划估计当墙钟完成保证。各capture最长15秒，异常直接cancel。
- 每次打印验证计划、空间（本次IQ+8 MiB）、AGX/P201确切目录和generation stop。
  NX timeout35/65秒、nsamps/FIFO byte count限制与owner核验停止沿用；每个burst
  runner外层180秒上限。先完成tone三对照并通过原20 dB门才发RML。
- 任意射频质量/身份/恢复/来源门失败，保留失败并停止后续推理；本轮不追加
  后验放行、发射或阈值修订。原始IQ仅用于本轮，最终精确清理，不写应用语料库。

## 身份与输入封存

复用native Controller。严格检查六份report、SigMF元数据/原始字节、父link记录
一致：唯一point、请求/sequence/generation/sweep_id关联、262140 bytes、无丢样/
溢出/削顶/timeout/health错误、完整固定RX身份，频率/rate/BW/gain及三个generation。
检查原始文件位于对应目录、无符号链接；seal保存原始IQ/metadata/report/source/
TX结果与link的SHA256，后续准备及推理重算验证，不以模型预测替代来源。
链路父记录须正常有限发送、子进程停止、前后radio状态一致、daemon不变。

## 先固定来源门，再进行七组对照

同时保留旧包络分析/原门结果和新v4数值记录，不能改写旧门。v4算法、31个错源、
带宽相关停发门、原估计/同源最低相关/分离裕量均沿用已提交版本。
新 `engineering_source_qualified` 需要六次输入检查、tone门及v4通过；其含义限于
本次已知源工程关联，不证明实际停发相位假设、独立标签或生产校准成立。

只有新来源门通过才用v4的前7原始窗lag/剩余频差：对前7窗做tone+剩余频差移频
及带限，保留DC，复用`fit_affine`估计y=h*x+b与h0基线；condition≤1000、|h|>1e-6。
不重新搜索参数。后8窗使用v4逐窗变换和固定系数，报告残差/接收功率及去中心相关。
这些残差不是AWGN SNR；源自身非零均值不能当偏置删除。

模型固定取第8窗offset28672，最多七组×四窗=28实验窗，另有Backend固有2warmup：
带限对齐源x、h*x、h*x+b、原始实收、tone移频带限实收、再补偿剩余频差的y、y-b。
全部复用共享RMS/顺序四窗、epoch-10 FP16 autocast+FP32权重/FP64 mean-logit。
保存所有输入hash后才加载模型；seal/准备不一致或门失败在GPU之前拒绝。共享一次
inference marker，180秒进程上限、私有GPU租约，无模型输出后重算/选窗或再次发射。

即便预测双向改变，也不定位B210/P201硬件根因或宣布盲补偿修复；实收40 dB不冒充
生产50 dB。`recognizer_available=false`、独立标签0、名称provisional保持。
测试、实机验证/恢复、精确临时清理、checklist、diff审查后单独commit并立即push。
