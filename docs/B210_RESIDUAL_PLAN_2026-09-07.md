# 保留多类 IQ 的剩余误差诊断

输入固定为 7623fd6 已封存的多类包，无新增 RF、数据集读取或 IQ 副本。
用户要求继续定位，并追问高 SNR 源端69/72与历史约99%的差异。
先核对历史98.803%是SNR>=4 dB验证集四条不同样本聚合；这轮72条均为
+30 dB train，每条重复四次，信息量/人口/归一化组不同。三个源端错误保留。

## 在本次新模型输出之前固定的矩阵

- ID5/11/13/19 的全部三轮，12条，不仅四条原门通过但预测错误的样本。
- 固定RX offset32768起连续4096点；估计只用原始前16384点，复用既有
  prefix对齐和robust scalar channel：`y=(h*s[n-lag]+b)*exp(j*w*n)+d`。
  不拟合评估窗口，不依模型输出选参数，不搜索最优变换。
- 每条最多9组四窗输入（432实验窗+2warmup）：原始源、原FIR RX基线、
  起点对齐/FIR源、匹配块起始相位源、加入频差及相位的源、完整线性信道预测源、
  RX仅去频差、RX去频差及相位、RX去拟合的额外偏置再去频差/相位。
  保留原始源真实均值，绝不将所有DC当噪声删除。
- h/b/d与频差都来自已知源的前段：仅用于系统辨识，不是未知信号的可部署修复。
  拟合不可用时报告缺失，不自动替换参数或消除原失败标记。
- 逐条保存完整logits、输入hash、参数、固定块残差；基线复核已保存的同输入logits。
  所有变换结果都报告，包括变坏的成功对照。不能当新准确率或准入证据。
- ID17/18六条做无模型的来源算法诊断：保留总功率99%带宽，并另报
  去均值谱99%带宽（仅诊断谱，不改变模型IQ）。中心化零能量则不可用；
  否则诊断带宽固定为max(3000Hz,原有方法计算的中心化99%带宽)。
  复用原相关/相位方法检查该带宽的拟合与后段残差，但不改写上次0Hz失败。
- 冻结epoch010 FP16 autocast/FP32 weights、RF-v1共享RMS与mean-logit不变。
  原始四重复源并不等于四独立样本评测；不为了得到99%再挑样本。
- 私有工作目录 `/var/tmp/sdrharness-dev/b210-residual-907v`，缓存/测试在此；
  模型预算432+2、推理deadline600秒，沿用空闲Spark暂停及700秒兜底恢复。
  本轮仅保留Git中的分析/模型审计、图和验证，不增加IQ；清理整个临时根，
  再次核验旧包hash/库存。生产能力false，独立标签0，不接无衰减器的同轴直连。

## 用户后续补充的只读检查与显示

用户要求查看4090混淆矩阵、提供24类名称截图，并要求逐点I/Q比较。
仅查看旧D8 seed44的val矩阵及生成代码；不读test数据或结果文件，不运行4090模型。
名称按用户截图作本轮诊断参考，生产映射不升级。为12条固定4096点块生成
前段参数对齐后的I/Q与差值；四组错误各搭配一条同类成功对照，固定显示前192点
及全部4096点差值，不挑最大差异片段。模型预算不增加。
绘图临时NPZ仅存转换后的显示数组，连同缓存和下载副本清除；不增加保留IQ。
