# 原始ADC离线基线重现与幅度诊断（2026-09-16）

## 结论和范围

固定原始Mamba D8 seed42与原validation的2496条既有诊断成员，ADC→历史raw/guard基线通过，
与既有预测top-1一致率100%。raw输入逐元素一致，guard最大绝对差2.38419e-7；
最大误差样本恢复历史64帧GPU批量后也逐元素一致。
source仅做逐窗RMS，正确数1578→1425；guard相对raw总体净增109条，但四AM类合计净退17条。
导频增益幅度候选明显降分，不启用。本轮是验证集诊断，不是独立最终测试或生产准入。
未训练/微调、TX/RX、部署或改变现有任务；未恢复epoch-010，recognizer_available=false保持。

[执行脚本](../../jetson-agx/sdrharness/scripts/rml2018a_offline_baseline.py) ·
[封存证据/清理/保留清单](../evidence/RML2018A_OFFLINE_BASELINE_2026-09-16.json)。

## 数据链与身份

**13份主ADC流 → 26档源SNR的processed.h5 → 分别复制其中inputs/raw、inputs/guard → 两份清洗HDF5。**
它们来自同一批接收，不是独立采集的数据集。清洗raw也已经同步、CFO/相位校正和逐窗RMS。
processed.h5的source是历史处理输入；本轮原值source直接读原RML2018A X。

用dataset-map.json、导出plan、采集plan/configuration及逐块frame记录定位，不根据目录名猜测。
主ADC共13份11,723,079,680字节；背景25份104,857,600字节只核对存在/大小，不参与推理。
相关2603份文件共146,527,351,726字节的完整SHA-256与封存记录一致；这是只读校验量，不是新增副本或全量推理。

- 初始分支codex/sdr-improvements、HEAD 14adfee6be1573e81f9937480b1aecff3be2abff，工作区干净，上游同名。
- 原D8 seed42权重SHA-256：6f984a78639a8e8e15403a1257b264839c6057165b3ae9ac8640ecc056f78143。
  集合manifest与父计划一致，导入源码/配置/权重逐项核验，strict load、原后端、FP32/关闭TF32、单窗1024、batch1024。
- 原split SHA-256：0a7cbd3b8a4b921b7dc3d0c37473322207c6bd9fb362ea58498c851fad9dc5a3。
  train1,789,132 / val383,385 / test383,387，互斥且完整覆盖；只有所选val成员送入模型。
- 复用9月15日24类×26源SNR×每格4条、按源行均匀选择的2496条；ID数组SHA-256：
  7ec78a1aea29bf9ed718ef098253dfe4a5f5bc3547a97c024dd11c73c0dd4899。保存validation_rank。
- 六个直接相关DSP模块与每批采集plan的封存哈希完全相同；已变化的采集入口不调用。
  旧profile仅保留处理血缘，不改变其配置或依赖，不用于加载已删除模型。

## 预算、重放与验收

独立根 `/var/tmp/sdrharness-dev/rml2018a-offline-baseline-20260916`，原数据只读。
每帧读取65535点，包括完整载荷、导频与后续三段保护区，最多654,301,440字节上下文读取；
上下文内其他成员不送入模型，不保存IQ副本。
预登记1800秒、主机8GiB、GPU分配4GiB、新文件512MiB，SSD可用约629GiB。
主过程711.527秒、19,968次预测，GPU峰值分配1,718,251,008字节；主机峰值未仪表化，推理期观察RSS3,462,964KiB。
停止仅针对本轮SIGINT/TERM或新根STOP，不修改原任务；原全量任务服务当时已inactive/MainPID=0。

预登记IQ atol=rtol=2e-6、logits atol=rtol=2e-4、top-1必须100%一致，离散身份/质量状态完全一致。
首帧coarse marker/CFO从ADC重新捕获；所选帧从**封存前一帧状态**重新运行原pilot tracking及rolling坐标。
这是有边界条件的逐帧重放，不是全流独立重捕获。未调整同步参数，未新增payload滤波、去均值、重采样或搜索。

| 检查 | raw | guard |
| --- | ---: | ---: |
| IQ最大绝对差 | 0 | 2.384185791e-7 |
| 最大单窗RMSE | 0 | 1.001420808e-7 |
| 重放/旧输入重新推理logits最大差 | 0 | 1.034736633e-4 |
| top-1一致率 | 100% | 100% |
| 旧输入/清洗IQ差 | 0 | 0 |
| 行号、标签、质量标志 | 完全一致 | 完全一致 |

source/raw/guard与已保存validation预测的logits最大差为5.21913e-5/4.30346e-5/1.03474e-4，均通过，top-1差异0。
marker样点、CFO、相位与封存值误差均0；条件SINR最大误差raw1.04e-13dB、guard1.98e-6dB，状态/原因相同。
该SINR是**源参考辅助的条件有效接收SINR**，包含源噪声和链路失真假设，不是独立标定的物理SINR；源Z仍另列。

最大guard误差样本源行114687/frame511：单帧CUDA拟合频率相对历史差−2.07563e-6Hz；
恢复原64帧批量后频率误差0、IQ逐元素相同。确认该样本为GPU批量尺寸舍入差，未放宽容差；
没有声称逐一定位所有其他非零舍入差。

本批是既有共同严格合格成员：本轮质量失败0、guard拒绝0、删除0，所有2496条留在分母。
**不能用此批估计失败群体表现或相消拒绝率**，未覆盖/删除旧失败记录。

## 幅度与相消配对

source_rms仅做复数RMS=1，float64计算标量、float32输入。raw/guard仍是原规则。
唯一RX候选raw_pilot/guard_pilot：将相同已同步载荷除以pilot-only接收增益，替代逐窗RMS。
增益为原同步滤波pilot与已知marker的复投影模长除以sqrt(10)，两条路径用同一增益；
不使用源幅度、真实类别或模型正确率，payload处理其他因素不变。

历史TX公式：`tx = source * sqrt(0.4) / max(abs(source))`，每窗峰值约0.63246。
导频校准只尝试恢复此TX尺度，不能推回未知源峰值或原source绝对幅度。
这是物理单位有依据的候选，不保证符合冻结模型的训练幅度分布。

| 输入 | 正确/2496 | 准确率 |
| --- | ---: | ---: |
| source | 1578 | 63.2212% |
| source_rms | 1425 | 57.0913% |
| raw | 1232 | 49.3590% |
| guard | 1341 | 53.7260% |
| raw_pilot | 163 | 6.5304% |
| guard_pilot | 142 | 5.6891% |

| 前→后 | 纠正 | 回退 | 净变化 |
| --- | ---: | ---: | ---: |
| source→source_rms | 21 | 174 | -153 |
| raw→guard | 331 | 222 | +109 |
| raw→raw_pilot | 115 | 1184 | -1069 |
| guard→guard_pilot | 85 | 1284 | -1199 |
| raw_pilot→guard_pilot | 44 | 65 | -21 |

source RMS-only下降6.13个百分点，确认原模型幅度敏感性，不是清洗新引入的问题。
source_rms与guard还差84条/3.37个百分点，不能把剩余差值全部归因硬件/同步。
导频候选降至6.53%/5.69%，不建议启用；不代表所有幅度候选都无效。

### 输入分布

复数功率PAPR用dB，下表为p05/p50/p95。全部逐类/逐SNR及624格分布见CSV。

| 输入 | RMS p05 / p50 / p95 | PAPR dB p05 / p50 / p95 |
| --- | --- | --- |
| source | 0.9774 / 1.0035 / 1.9823 | 1.6695 / 6.5090 / 9.4488 |
| source_rms | 1.0000 / 1.0000 / 1.0000 | 1.6695 / 6.5090 / 9.4488 |
| raw | 1.0000 / 1.0000 / 1.0000 | 2.9859 / 6.8238 / 8.9366 |
| guard | 1.0000 / 1.0000 / 1.0000 | 2.0245 / 6.5340 / 9.4360 |
| raw_pilot | 0.2207 / 0.3292 / 0.5483 | 2.9859 / 6.8238 / 8.9366 |
| guard_pilot | 0.1807 / 0.3021 / 0.5322 | 2.0245 / 6.5340 / 9.4360 |

候选PAPR保持一致（float32误差内），变化确为标量幅度。事后物理核对：源PAPR和TX规则给出
TX RMS中位数0.29894，实收guard_pilot为0.30206；逐行比值p05/p50/p95为0.82912/0.99600/1.04041。
支持候选尺度接近TX而非原source（其中位RMS1.00353）；此处源参考仅用于诊断，未用于RX校正/模型输入。

### 每类与AM

每类104条。四AM合计raw171→guard154/416，纠正47、回退64。
带载波SSB-WC/DSB-WC回退尤其明显；其载波与残余LO如何影响分类尚未隔离证实。
4ASK的raw71→guard13也保留，不能由raw偶然更对推导应保留干扰。

| 类别 | source | source RMS | raw | guard | 相消纠正/回退 |
| --- | ---: | ---: | ---: | ---: | ---: |
| OOK | 78 | 60 | 80 | 67 | 2/15 |
| 4ASK | 69 | 15 | 71 | 13 | 4/62 |
| 8ASK | 69 | 62 | 37 | 61 | 31/7 |
| BPSK | 79 | 79 | 80 | 72 | 0/8 |
| QPSK | 67 | 67 | 67 | 64 | 0/3 |
| 8PSK | 63 | 63 | 68 | 59 | 0/9 |
| 16PSK | 53 | 53 | 53 | 49 | 0/4 |
| 32PSK | 57 | 57 | 48 | 61 | 15/2 |
| 16APSK | 66 | 66 | 67 | 62 | 1/6 |
| 32APSK | 64 | 64 | 63 | 65 | 4/2 |
| 64APSK | 59 | 59 | 46 | 58 | 14/2 |
| 128APSK | 59 | 59 | 29 | 60 | 31/0 |
| 16QAM | 60 | 59 | 48 | 60 | 13/1 |
| 32QAM | 58 | 57 | 39 | 55 | 19/3 |
| 64QAM | 54 | 53 | 16 | 50 | 37/3 |
| 128QAM | 52 | 52 | 3 | 52 | 50/1 |
| 256QAM | 52 | 51 | 7 | 57 | 51/1 |
| AM-SSB-WC | 66 | 33 | 45 | 20 | 5/30 |
| AM-SSB-SC | 64 | 45 | 24 | 31 | 21/14 |
| AM-DSB-WC | 79 | 85 | 80 | 60 | 0/20 |
| AM-DSB-SC | 59 | 35 | 22 | 43 | 21/0 |
| FM | 93 | 93 | 87 | 73 | 0/14 |
| GMSK | 73 | 73 | 72 | 68 | 2/6 |
| OQPSK | 85 | 85 | 80 | 81 | 10/9 |

### 每档源SNR

每档96条、每类×SNR格仅4条，不据单格作稳定性推断。−10至0dB合计raw185→guard115/576
（纠正36/回退106）；2至30dB合计1026→1203/1440（279/102）。

| 源SNR dB | source | source RMS | raw | guard |
| --- | ---: | ---: | ---: | ---: |
| -20 | 6 | 6 | 5 | 4 |
| -18 | 3 | 3 | 4 | 4 |
| -16 | 2 | 2 | 1 | 7 |
| -14 | 8 | 8 | 5 | 5 |
| -12 | 8 | 8 | 6 | 3 |
| -10 | 16 | 15 | 15 | 6 |
| -8 | 19 | 18 | 22 | 5 |
| -6 | 23 | 23 | 24 | 12 |
| -4 | 33 | 32 | 35 | 22 |
| -2 | 47 | 43 | 42 | 25 |
| 0 | 57 | 54 | 47 | 45 |
| 2 | 67 | 67 | 52 | 61 |
| 4 | 71 | 65 | 55 | 66 |
| 6 | 89 | 81 | 66 | 77 |
| 8 | 96 | 85 | 67 | 82 |
| 10 | 94 | 83 | 74 | 84 |
| 12 | 94 | 86 | 72 | 85 |
| 14 | 94 | 82 | 72 | 83 |
| 16 | 93 | 83 | 69 | 87 |
| 18 | 94 | 82 | 74 | 83 |
| 20 | 94 | 80 | 71 | 80 |
| 22 | 94 | 85 | 78 | 85 |
| 24 | 94 | 84 | 62 | 82 |
| 26 | 95 | 82 | 73 | 82 |
| 28 | 91 | 83 | 65 | 80 |
| 30 | 96 | 85 | 76 | 86 |

[配对图PNG](/var/tmp/sdrharness-dev/rml2018a-offline-baseline-20260916/paired-accuracy.png) ·
[SVG](/var/tmp/sdrharness-dev/rml2018a-offline-baseline-20260916/paired-accuracy.svg) ·
[逐类CSV](/var/tmp/sdrharness-dev/rml2018a-offline-baseline-20260916/per_class.csv) ·
[逐SNR CSV](/var/tmp/sdrharness-dev/rml2018a-offline-baseline-20260916/per_source_snr.csv) ·
[624格完整CSV](/var/tmp/sdrharness-dev/rml2018a-offline-baseline-20260916/per_class_source_snr.csv)。

## 已确认、未确认与唯一下一实验建议

已确认ADC可重现选定输入，清洗未改这些IQ；原模型幅度敏感；TX峰值缩放丢失原source绝对尺度；
相消收益依类别/SNR而异；单纯导频校准不足以适配原模型。
未确认AM/4ASK回退机制、剩余同步/时序/链路失真的贡献、失败成员表现、独立整段流重捕获与可部署幅度规则。
同步与历史一致不等于物理时序绝对无误；本轮无文献引入，区分以上代码事实、实测与假设。

**仅推荐：同2496条source按历史TX公式逐窗峰值缩放后识别，作为source端幅度对照。**
固定模型/FP32/划分，仅新增2496次预测，与本轮source、source_rms、guard_pilot配对；
检验候选降分是否主要由TX数值尺度偏离训练分布造成。source处理仅用于source对照，不向RX补回源幅度。
不需RF/微调，本轮未自动执行该建议，不扩大到全量。

## 验证与收口

独立读回NPZ重算675组（总计、24类、26档、624格）计数/纠正回退/分布/源ID/分母，全部一致；
source与source_rms亦与9月15日预测top-1完全一致。图表人工检查通过，GPU64帧舍入定位通过。
首次模型venv绘图缺matplotlib，改用已有系统Python核验和绘图，无安装或环境修改。
主过程和审计均退出，无socket/P201/NX暂存。删除353缓存文件24,425,794字节，3个精确目录均不存在。
最小诊断包保留27文件12,649,116字节，逐文件SHA、源行/会话/模型/预处理血缘及精确人工删除命令见retention。
无新IQ/模型副本，原始保留数据和历史结果未删除/覆盖，收尾mtime/大小未变。


## source发射峰值缩放对照（2026-09-16继续）

用户“继续”沿用上节唯一建议，固定同一2496条原validation、D8 seed42和FP32/关闭TF32，
仅新增2496次source_tx_peak预测。旧六种输入的预测从已核验父证据只读复用，不重跑RX、模型基线或RF。
初始HEAD为7bfd9ca952b556530526fc11c45af13f9d1c5864，工作区干净。
[执行脚本](../../jetson-agx/sdrharness/scripts/rml2018a_source_peak_control.py) ·
[派生证据及保留清单](../evidence/RML2018A_SOURCE_PEAK_CONTROL_2026-09-16.json)。

### 实现身份与预检失败

从Git历史找回每个父采集plan引用的rml2018a_snr_stream.py完整文件，SHA逐一匹配后仅提取纯函数packet_block，
不导入或运行收发入口。其source为complex64，按原计算顺序执行：
`source * (.2 * sqrt(10) / max(abs(source)))`。
现有source文件重新完整SHA核验，父保留包、原split、集合manifest与全部导入文件均核对；
输入载荷与历史函数逐元素相同。检验时末批仅用所选val成员重复填满函数要求的1024窗形状，
填充仅用于字节比较，不增加模型预测或统计成员。NumPy为1.26.4。

**保留一次预检中断，不把它写成全部通过：**最初把与理论常数0.6324555…的峰值误差≤1e-7设为拒绝门。
历史complex64计算的6条样本超限，最大1.102174365e-7，因此第一次运行在推理前停止，新预测0。
载荷却与封存函数逐元素完全相同；独立复核确认是历史浮点舍入，不是载荷不匹配。
随后在独立a2根登记修正：峰值诊断仍使用原1e-7阈值，结果明确为failed、6条保留标志和全部分母；
不调幅、不升阈值、不删成员，不再把这项理论常数诊断当成历史载荷不一致。
历史载荷字节一致、PAPR≤2e-6dB、有限logits和身份条件保持。
第一次plan、脚本、失败数值及第二次plan均保留，不能称为原预检全部通过。
实际PAPR最大差4.26138e-7dB，变化仅为每窗标量幅度。

预算600秒、主机/GPU各4GiB、新结果64MiB；a2实际116.687秒，
GPU峰值1,718,250,496字节、主机峰值RSS1,426,923,520字节。无IQ落盘或生产变更。

### 结果及解释

| 输入 | 正确/2496 | 准确率 | 本轮是否新增预测 |
| --- | ---: | ---: | --- |
| source | 1578 | 63.2212% | 否，复用父证据 |
| source_rms | 1425 | 57.0913% | 否，复用父证据 |
| source_tx_peak | 150 | 6.0096% | 是 |
| raw_pilot | 163 | 6.5304% | 否，复用父证据 |
| guard_pilot | 142 | 5.6891% | 否，复用父证据 |
| guard | 1341 | 53.7260% | 否，复用父证据 |

| 前→后 | 纠正 | 回退 | 逐条top-1一致数/2496 |
| --- | ---: | ---: | ---: |
| source→source_tx_peak | 55 | 1483 | 134 |
| source_rms→source_tx_peak | 83 | 1358 | 102 |
| source_tx_peak→guard_pilot | 47 | 55 | 1546 |
| source_tx_peak→raw_pilot | 39 | 26 | 1924 |

source_tx_peak RMS的p05/p50/p95为0.21310/0.29894/0.52186，接近上一轮guard_pilot的
0.18070/0.30206/0.53218，明显低于原source中位RMS1.00353和现有raw/guard的单位RMS。
新预测中1804/2496（72.28%）落在AM-DSB-SC；该类100/104正确，但多类正确数降为0，
表现为输出集中，不能把该类局部高分当成改进。

**已确认的因果对照：**在未经过接收链路的同一source上，仅做历史发射幅度缩放，
冻结模型就从63.22%降到6.01%。导频候选的同级别低准确率不需要额外假定严重RF损伤才能出现；
其TX数值尺度与原模型训练输入幅度不匹配，是得到实测支持的解释。
这不是证明模型只使用幅度，也不证明实际链路/同步无误。
source_tx_peak与guard_pilot准确率仅差8条，但逐条top-1只1546/2496=61.94%一致，
47条纠正和55条回退互相抵消；不能将相近均值当作相同波形或相同误判。
尤其不能把此次57.21个百分点下降解释成现有source→guard差距：现有guard仍是单位RMS输入，
其53.73%与导频候选5.69%是不同幅度规则。

[逐SNR对照图](/var/tmp/sdrharness-dev/rml2018a-source-peak-control-20260916-a2/source-peak-control.png) ·
[SVG](/var/tmp/sdrharness-dev/rml2018a-source-peak-control-20260916-a2/source-peak-control.svg) ·
[逐类计数/幅度分布CSV](/var/tmp/sdrharness-dev/rml2018a-source-peak-control-20260916-a2/per_class.csv) ·
[逐SNR CSV](/var/tmp/sdrharness-dev/rml2018a-source-peak-control-20260916-a2/per_source_snr.csv) ·
[624格CSV](/var/tmp/sdrharness-dev/rml2018a-source-peak-control-20260916-a2/per_cell.csv)。

### 下一步只建议一个实验

**相消前后共用相消前raw的RMS分母。**保持相同2496条、模型及同步，仅把guard的
独立RMS改为对应raw接收窗的RMS；与现有raw/guard配对，重点看AM和4ASK的纠正/回退。
利用既有接收侧RMS，不读取源真实RMS来补偿接收幅度，不搜索常数、不新增收发或训练。
这能区分“减掉LO改变波形”与“相消后再次归一化改变有效幅度”的贡献。
本轮未执行该建议，也没有扩大到全量。

### 收口

独立读取新旧NPZ验证675组计数、纠正回退、成员和6条峰值诊断失败标志，分母始终2496，新增推理仅2496。
图表人工检查通过，主过程/审计退出，无socket/远端暂存。首次失败保留，阈值未放宽、输入未调整。
删除353缓存文件24,425,794字节；两个精确缓存目录不存在。
两次尝试共保留19文件1,384,032字节，无IQ/权重副本，文件哈希/父级血缘/人工删除命令见派生retention。
原始数据和父结果保持不变，未重新发射、采集、训练、微调或修改生产/现有任务。


## 相消前后共用raw RMS分母（2026-09-16获准执行）

用户确认上一节单一建议。初始HEAD为7ec0e6e1be7a4c51d06a5a9c8776aa2617bfa94a，工作区干净。
同一2496条原validation、同原D8 seed42/FP32/关闭TF32、batch1024，仅新增2496次预测。
[执行脚本](../../jetson-agx/sdrharness/scripts/rml2018a_shared_raw_rms.py) ·
[派生证据、血缘与保留清单](../evidence/RML2018A_SHARED_RAW_RMS_2026-09-16.json)。

### 因素和验收

记未经RMS的接收载荷为r、相消载荷为g，Rr=RMS(r)、Rg=RMS(g)。
现有raw为r/Rr、guard为g/Rg；本次只计算 **guard_shared_raw_rms=(g/Rg)×(Rg/Rr)=g/Rr**。
两个RMS来自已核验ADC重放的接收载荷，不用源真实RMS，不按类别或模型输出选择系数，也不裁剪比例。
raw与现有guard预测从已封存父NPZ复用（guard用archived_guard），不重新采集或变更相消/同步。
读取清洗guard并重新完整SHA核验，逐源行核对标签/SNR、原ADC偏移和质量标志。
父保留包、模型源码/配置/权重与原split身份重新核验。

原计划文字“source IQ/RMS not read”应精确理解为未访问source IQ文件、未使用source RMS。
实际父预测NPZ整体读取含未使用的source RMS字段；缩放计算只引用baseline-rows的RX received_rms，
完整范围澄清已写execution-notes；提交脚本仅将这一计划描述改为明确的“无source IQ文件访问、不使用source RMS”，
数值实现未变，本次实际执行快照和原计划哈希保留。质量标志为历史接收质量的保留注释，不冒充新生产准入。

预登记RMS atol=rtol=2e-6、PAPR最大变化2e-6dB、成员/标签/质量完全一致、有限logits、丢弃0。
实际RMS最大误差4.39638e-9，PAPR最大变化4.84799e-7dB，全部通过；没有改阈值。
RMS比例p05/p50/p95=0.81304/0.91697/0.97076，典型样本缩小约8.3%。
本批原本共同严格合格，质量失败0、相消拒绝0、分母2496；不代表覆盖失败群体或测得全体拒绝率。
源SNR沿用原Z，本次不重算或宣称提高物理接收SINR。

预算600秒、主机/GPU各4GiB、新文件64MiB；实际124.597秒，GPU峰值1,718,250,496字节，
主机峰值RSS1,403,109,376字节。首轮在输入SHA阶段因代码复核发现未来NPZ输出字段重复而主动停止，
新预测0；修正字段重复后在独立a2根运行，数学处理/阈值/成员不变，首轮计划/源码/中断保留。

### 配对结果

| 输入 | 正确/2496 | 准确率 |
| --- | ---: | ---: |
| raw | 1232 | 49.3590% |
| guard | 1341 | 53.7260% |
| guard_shared_raw_rms | 1203 | 48.1971% |

| 前→后 | 纠正 | 回退 | 净正确数变化 |
| --- | ---: | ---: | ---: |
| raw→guard | 331 | 222 | +109 |
| raw→guard_shared_raw_rms | 311 | 340 | -29 |
| guard→guard_shared_raw_rms | 86 | 224 | -138 |

相对现有guard净退138条（−5.53个百分点），相对raw净退29条。
原raw→guard的222条回退仅救回16条（7.21%），却丢失331条原纠正中的90条（27.19%）。
不能只统计救回量或把总体平均当成每类一致。

| 类别（每类104条） | raw | guard独立RMS | guard共用raw RMS | 原回退救回/原纠正丢失 |
| --- | ---: | ---: | ---: | ---: |
| OOK | 80 | 67 | 51 | 0/0 |
| 4ASK | 71 | 13 | 7 | 0/2 |
| 8ASK | 37 | 61 | 61 | 0/1 |
| BPSK | 80 | 72 | 65 | 0/0 |
| QPSK | 67 | 64 | 62 | 0/0 |
| 8PSK | 68 | 59 | 60 | 2/0 |
| 16PSK | 53 | 49 | 51 | 1/0 |
| 32PSK | 48 | 61 | 57 | 1/4 |
| 16APSK | 67 | 62 | 56 | 0/1 |
| 32APSK | 63 | 65 | 66 | 0/1 |
| 64APSK | 46 | 58 | 23 | 0/11 |
| 128APSK | 29 | 60 | 35 | 0/19 |
| 16QAM | 48 | 60 | 60 | 0/2 |
| 32QAM | 39 | 55 | 53 | 1/4 |
| 64QAM | 16 | 50 | 38 | 0/10 |
| 128QAM | 3 | 52 | 42 | 0/12 |
| 256QAM | 7 | 57 | 58 | 0/11 |
| AM-SSB-WC | 45 | 20 | 21 | 5/3 |
| AM-SSB-SC | 24 | 31 | 29 | 2/4 |
| AM-DSB-WC | 80 | 60 | 18 | 0/0 |
| AM-DSB-SC | 22 | 43 | 74 | 0/0 |
| FM | 87 | 73 | 71 | 1/0 |
| GMSK | 72 | 68 | 67 | 1/2 |
| OQPSK | 80 | 81 | 78 | 2/3 |

四AM类合计raw171、独立RMS guard154、共用分母142/416；guard→共用分母纠正40、回退52。
AM-DSB-WC 60→18，AM-DSB-SC 43→74，带/抑制载波类别的得失方向相反。
4ASK 13→7且原相消回退救回0，不能把整体问题简单归因于相消后的独立归一化。
这也不证明归一化无影响：相同guard波形仅变标量便显著改变分类，影响还与波形和类别有关。

[逐SNR与重点类别图](/var/tmp/sdrharness-dev/rml2018a-shared-raw-rms-20260916-a2/shared-raw-rms.png) ·
[SVG](/var/tmp/sdrharness-dev/rml2018a-shared-raw-rms-20260916-a2/shared-raw-rms.svg) ·
[逐类CSV](/var/tmp/sdrharness-dev/rml2018a-shared-raw-rms-20260916-a2/per_class.csv) ·
[逐源SNR CSV](/var/tmp/sdrharness-dev/rml2018a-shared-raw-rms-20260916-a2/per_source_snr.csv) ·
[624格CSV](/var/tmp/sdrharness-dev/rml2018a-shared-raw-rms-20260916-a2/per_cell.csv)。

### 是否发射端出问题、如何处理

现有证据支持**发射/接收预处理与原模型输入尺度不一致**，不支持直接诊断发射硬件故障。
历史TX逐窗峰值缩放去掉源窗之间的绝对幅度关系，导频无法恢复被去掉的源峰值。
无失真且忽略浮点舍入时，逐窗RMS满足N(T(x))=N(x)：TX的标量缩放会被RX RMS消去，
但并不能变回模型训练时未经归一化的x。现实中的噪声/LO/链路失真另有影响，不能由此假定没有。

当前保持已验证的guard独立RMS作为工程对照；两种幅度候选都不启用，不改生产配置。
后续模型研究应优先固定一致的训练/推理预处理（例如同一RMS规则），再按原划分验证；
这只是研究方向，不等于已经证明某种微调有效，更不授权启动训练。
source原值63.22%、source RMS57.09%、实收guard53.73%的三个分数不可混为一个“发射损失”。
若另行检验TX硬件/发射幅度关系，需独立的统一缩放、峰值/削顶预算和有限重新采集计划；本轮未执行。

### 唯一下一实验建议

对既有AM四类及4ASK的全部520条validation成员，给**source_rms**乘本轮对应RX比例Rg/Rr，
固定同模型，只新增520次source端预测，与已保存source_rms对照。
检验AM/4ASK回退能否仅由这约5%–20%的缩幅在无接收失真的source中重现，还是依赖接收后的波形。
五类每格4条全保留，不按正确/错误选样，不把源RMS补回RX，不扫参数、不训练/收发。
这是诊断建议，尚未执行；不扩大当前单位RMS候选为生产修复。

### 收口

独立读取新旧NPZ核对676组（总计、AM合计、24类、26档、624格）计数/配对/救回与丢失、
RX RMS血缘、成员/质量和PAPR/RMS，全部通过；图表人工检查通过。
主过程与审计已退出，无socket/远端暂存；删353缓存文件24,425,794字节，两处精确缓存目录不存在。
两次尝试共保留19文件1,431,486字节（含预推理中断记录），无IQ或权重副本。
逐文件SHA、源行/父采集会话血缘及人工删除命令见派生retention；父证据/原数据保持不变。
无收发、训练、微调、生产部署或现有任务修改。

## LO与50Ω历史复核及官方资料评估（2026-09-16）

用户随后选择优先优化LO和相消，并要求复核50Ω记录、查阅文献；此前520条source幅度实验建议暂缓。
本单元只读历史记录和联网官方资料，未收发、未校准写入、未新增模型预测或干预用户自行训练的轻模型。
前述幅度对照只证明模型尺度敏感，不能替代以下已经存在的射频归因证据。

### 实测事实

复核[全量RF历史](RML2018A_FULL_RF_CAMPAIGN_2026-09-10.md)及
[9月9日负载对照](P201_TERMINATION_BACKGROUND_VALIDATION_2026-09-09.md)，
6份相关保留包及9月9日三个父审计共360文件、19,907,060字节的大小/SHA通过。
这是历史证据完整性核对，本轮未重新计算频谱。

| 既有对照 | 观察 | 能支持的结论 |
| --- | --- | --- |
| 9月12日TX LO偏移+250→−250kHz | 强峰移动−499995.66Hz，有用单音功率仅+0.09dB；扣除CFO后LO位置误差≤1.25Hz | 有很强的TX LO相关泄漏/杂散证据，不等于确定某器件损坏 |
| 数字幅度减半 | 有用单音−5.77dB，LO峰反而+1.09dB | LO并非简单随载荷同比缩放 |
| 数字幅度提高、TX70→60，匹配有用功率 | 单音LO下降7.28–7.39dB；实际OOK/QPSK下降6.82/7.48dB | 已验证的改善，现有峰值0.632455532/TX60已采用，不作为新实验重复建议 |
| 9月13日P201独立50Ω，B210停发 | 非LO功率中位数5.77–6.14 counts²，最大14.75–490.02，仍有突发 | 背景瞬态不能全部归因TX载荷或相干LO |
| 再接回同轴且停发 | 最大12.39–107.92 counts²，旧大尖峰未稳定复现 | 不能据此前后差异确认同轴/B210故障或负载已解决问题 |

9月9日负载→天线→负载的128点RMS显示事件为0/34/0，支持外部输入影响；
与9月13日增益、滤波和指标不同，不作直接功率比较。约16ms间隔只为短记录线索，未确证周期来源。

### 官方资料及本设备适用边界

本轮以厂家技术手册/应用资料为依据，不将其称为本设备实测或独立学术验证。

1. [UHD 4.1.0.5校准说明](https://files.ettus.com/manual_archive/v4.1.0.5/html/page_calibration.html)：
   B200系列使用RFIC自身的专用校准，不在通用前端校准章节范围内。
   当前国产B210兼容设备使用改版UHD，不能直接套用`uhd_cal_tx_dc_offset`或假定通用DC接口可用。
2. [AD9361 UG-570 Rev A，第13页](https://www.analog.com/media/en/technical-documentation/user-guides/AD9361_Reference_Manual_UG-570.pdf)：
   TX quadrature calibration降低发射DC偏置、增益及相位误差；频率/温度变化、输出路径及滤波配置均有关。
   手册中的ADI驱动函数不等于当前UHD暴露同名功能，实际驱动路径尚待审计，不建议手写寄存器。
3. [Analog Dialogue：Transmit LO Leakage](https://www.analog.com/en/resources/analog-dialogue/articles/transmit-lo-leakage-lol-an-issue-of-zero-if-that-isn-t-making-people-laugh-out-loud.html)：
   正确位置的I/Q偏置补偿可压低LO；观察接收机与TX使用不同LO有助于区分TX泄漏和RX自身DC。
   文中AD9371观察接收/跟踪能力不能直接移植为本B210已有能力。
4. [UHD调谐说明](https://files.ettus.com/manual/page_general.html)：RF LO与DSP共同调谐，offset tuning可以移动泄漏位置。
   当前+250kHz仍在配置的1.5MHz接收通带内；是否避开有效信号需实际占用带宽支持。
   B210 DSP绕回条件涉及master clock，不可只按host Fs=2.1MS/s推算；本记录MCR=33.6MHz。
5. [ADI AN-1039](https://www.analog.com/en/resources/app-notes/an-1039.html)：区分DC/LO补偿与I/Q镜像补偿；
   低中频方案需要模拟/数字滤波和带宽余量。文中的中频数值不是本设备建议值。

### 对当前调制和算法的建议

优先核对TX原生校准是合理的；offset tuning只移动泄漏，不能声称降低本征泄漏。
源头抑制可以减轻接收动态范围压力，后端相消不能挽救此前已发生的削顶。
当前数字幅度已有较高峰值，继续提高须先核对插值后的峰值余量。

RML X本身就是已调制IQ，保持其AM/QAM/PSK等类别，不能把所有载荷再调为QPSK或FSK。
需区分有用AM载波（目标2455MHz）、TX LO（名义2455.25MHz）与RX本身的零频DC。
改变整窗标量不会改变理想窗内调制度；逐点幅度拉平、削顶或随意去均值则可能损坏AM。
TX与RX均做整窗归一化并非原则性错误；训练与推理输入约定需一致，且不能恢复已丢掉的绝对窗幅度。

尤其不能把文献中的I/Q DC补偿直接理解成给主机RML窗加常数：已有数字LO偏移时，
主机常数可能改变有用载波，未必落在物理LO频率。先查清RFIC/DSP坐标与补偿注入位置。

现有保护区拟合、导频频率约束、留出检查比盲目对载荷去均值/陷波更适合保留AM结构。
历史宽频搜寻出现过稀疏保护区频率混叠；旧24行帧的约80.42Hz周期不能套到当前16行帧，
后者17920点间距对应约117.19Hz。保持现有窄搜索/拒绝门，不按类别、源真实幅度或识别准确率挑补偿。
新拟合候选须保持raw/guard配对、全部分母与拒绝原因；本轮未证明新相消算法优于既有实现。

### 唯一下一步

先完成**本机改版UHD/AD9361发射校准路径与既有RF/DSP配置的只读审计**：
确认实际加载库/驱动身份、初始化及重调谐是否执行TX校准、输出路径/滤波依赖、支持的补偿接口，
并核对占用带宽与LO偏移可行范围。不开设备初始化、不写校准、不发射。
之后才能提出固定有用功率的有限校准前后A/B计划，以LO/信号比、镜像、波形误差及削顶为验收，
不按模型准确率调硬件；本轮不执行该收发实验。
用户轻模型训练可独立继续；之后的接收域微调另行规划一致的输入处理与原划分。

### 本单元状态与保留

先前起草的“增加前置保护区”代码仅通过6项合成测试，未重放真实IQ，已暂停并移出工作树，
两份草稿留在独立研究目录，未作为算法交付或生产候选启用。
首次UG-570下载截断，后经Content-Range完整取得2,130,294字节并成功解析；未依据截断版本形成结论。
本轮保留5文件14,117字节（资料来源/哈希摘要、历史完整性核对、两份草稿、清理清单），
删除24个下载/解析暂存文件4,634,714字节；无新IQ、权重、socket或常驻任务。
URL、下载身份、父血缘及精确人工删除命令见[审计](../evidence/LO_HISTORY_LITERATURE_REVIEW_2026-09-16.json)。

## UHD校准路径只读审计与获准镜像重载（2026-09-16）

用户同意只读审计，随后明确要求重新加载设备镜像恢复USB3，并将启动/校准知识写入N210技能。
前半段未初始化设备；后半段只执行获准的现有`b210.py probe`，没有TX/RX数据流、P201操作、
模型加载/训练、源IQ读取或生产配置修改。镜像加载会初始化硬件并可能触发内部校准，不能把整个单元称为纯只读。

### 身份与历史配置

- 工作起点`0b618c3`、`codex/sdr-improvements`，工作树干净；专用runtime manifest全部文件及系统库SHA通过。
- 系统`libuhd4.1.0`、`uhd-host`为Ubuntu `4.1.0.5-3`；`dpkg -V libuhd4.1.0`无差异。
  库SHA为`357273c6059cc81d02585a79635e82d8a5bf1db46c07288e9fad0c7746ad9dc1`，
  包MD5也相同。专用A7-100T/FX3镜像与主机库是不同身份，不能因目录名将整个主机驱动说成改版。
- 从dataset-map的original_root定位13批plan/tx-events/tx.log，全部配置一致：
  Fs=2100000.0074797985、综合RF=2455000000.0020895、TX60、BW1500000、
  MCR=33600000.119676776、internal clock/time、LO locked。
- 13批均指向`/var/tmp/sdrharness-dev/rml2018a-full-snr-20260913/tx-events`，
  本体SHA与计划`b609f5cf741fc2780f275364151ae504dccc583cfe5f97961d37551549d8cae3`一致。
  `ldd`解析到上述系统libuhd。历史日志不是当前硬件状态；当前probe默认MCR16MHz不应冒充历史33.6MHz。

### 完整调用链与修正后的结论

查看UHD上游v4.1.0.5的7份相关源文件和Ubuntu4.1.0.5-3打包补丁，补丁未触及这7个路径。
这提供版本对应的源码证据，未进行可重复二进制构建、寄存器追踪或残余LO测量。

现有helper顺序为set_tx_rate → set_tx_freq → gain/antenna → set_tx_bandwidth → get_tx_stream。
底层关键路径为：

`b200_impl::get_tx_stream`（b200_io_impl.cpp:605）
→ `update_enables`（b200_impl.cpp:1471）
→ `ad9361_ctrl::set_active_chains`
→ `ad9361_device_t::set_active_chains`（ad9361_device.cpp:2058）
→ `_calibrate_tx_quadrature`。

TX启用时该路径执行TX校准；函数分别处理TX_A/TX_B并恢复选择，轮询完成位，超时会抛异常。
因此不能因为应用没有显式校准调用，就断言历史发送没校准。
最初观察到set_bw_filter不调用TX正交校准，曾把调频/带宽顺序列为疑点；追到后续创建流后，
这个“带宽之后漏校准”的解释不成立，不建议仅为此交换设置顺序。
校准完成位通过也不等于本征LO达到某个dBc指标，历史日志没有记录残余LO验收值。

另一个入口`tune`对同请求频率直接返回；只有距离上次校准频点超过100MHz才触发相应重校准。
重复set_tx_freq同频不构成强制校准，不自行绕到其他频点制造触发。

B200前端树只创建RX的自动DC/IQ属性；通用multi_usrp的TX DC/IQ API缺少目标属性时会警告并返回。
驱动也明确不支持TX DC/IQ自动tracking。不能把“API存在”当作“本机支持手动TX补偿”，
不能套用通用校准工具或手写寄存器。自动TX quadrature校准与RX自动DC跟踪是不同机制。

### RF/DSP及滤波仍缺什么

综合RF频率由硬件LO与DSP平移共同决定。helper请求2455MHz、LO offset +250kHz，
但没有保存`tune_result_t.actual_rf_freq/actual_dsp_freq`；日志只有综合频率、BW和MCR。
因此名义2455.25MHz LO及相应DSP平移是请求/源码推导，不冒充逐批独立硬件读回。
同样未保存完整AD9361 FIR系数/插值状态和校准内部NCO状态。
此次没有测定RML占用带宽，不据此指定更大LO偏移或直接改滤波；+250kHz位于配置接收通带内，
是否及多大程度与有用谱重叠仍需有限离线测量。

唯一下一实验建议：复用已有2496条validation的source及配对raw/guard，固定窗长和频谱定义，
检查+250kHz泄漏附近的有用信号占用与拟议移频所需通带余量；只做频谱统计，不新增预测、
不按真实类别选择处理、不改波形、不扫RF参数。完成后再决定是否值得提出有限LO偏移A/B收发计划。

### 获准镜像重载与技能维护

初始sysfs为480Mbps、启动序列号`0000000004BE`。用户指出本板需加载镜像才能进入USB3，
授权后使用现有probe，先校验无人占用及manifest，再加载专用FX3和A7-100T镜像。
命令预算35+70秒，执行前SSD可用675,095,818,240字节；新增IQ预算0。
实际discover/probe均退出0；恢复serial2508504、5000Mbps，两个寄存器回环通过，
最后USB节点`/dev/bus/usb/002/009`无占用。节点号不是后续固定配置。
未写EEPROM/flash、未替换系统镜像/库。无数据流不表示测得零射频能量。

按用户要求更新[n210技能](../../.codex/skills/n210-sdr-workflow/SKILL.md)及其finite-tx参考：
启动阶段身份、加载后验收、避免重复初始化、镜像加载与TX/RX授权边界，以及版本限定的校准/API事实。
`skill-creator` quick_validate通过；文档/链接及diff检查完成，不新增与本次文字修改无关的测试。

### 保留与清理

审计根保留13文件374,058字节，镜像重载根保留3文件5,350字节，共16文件379,408字节；
包含相关上游开源源码、下载身份、发行包补丁摘要、13批元数据血缘及probe日志。
删除发行源码补丁下载暂存1文件53,428字节；无新IQ副本、模型、构建缓存或socket。
本单元命令均已退出，未改现有后台任务。逐文件SHA/精确路径/人工删除命令见
[审计](../evidence/LO_DRIVER_AUDIT_2026-09-16.json)。本单元完成审计和USB3恢复，不宣称LO已进一步降低。

## 获准设备调试：镜像重载后的LO功率基线（2026-09-16）

用户要求精简skill并开始设备调试，随后两次“继续”。已移除skill参考文件中的校准分析长段，
仅保留必要镜像/识别/操作边界；本报告中的历史事实保留。本单元获准重新进行有限收发，
不延伸为模型训练、全库采集或生产配置修改。

### 计划、身份与执行

本机USB3/serial2508504空闲；P201只有一个PID1135，镜像SHA对应已验收的9月14日整档流版本
`dae7c32f…fce74e`，Controller仍为`24b8340d…3e25f`。
旧LO入口只锁定更早daemon，因此新增逐计划daemon身份字段：新计划精确锁定当前已验证版本，
无字段的旧调用仍要求旧版本，未知版本在网络访问前拒绝；未放宽质量/峰值门或替换运行程序。
该修改7项相关测试通过，精简skill的quick_validate通过。

实验根`/var/tmp/sdrharness-dev/b210-cablelo-reload-20260916`，代码起点9c53aed加封存源码改动。
沿用20dB/15cm同轴→P201 RX1、2455MHz、2.1MS/s、BW1.5MHz、RX40、LO+250kHz。
合成98437.5Hz单音；A幅度0.1/TX70，B幅度0.316227766/TX60，顺序停流→A1→B1→A2→B2→停流。
每次TX8,399,872点，4次合计33,599,488点、≤16秒；每次RX65,535点，共1,572,840字节。
settle500ms、单点1000ms、接收外层15秒、整体360秒；开始前SSD可用675,271,675,904字节。
SIGINT/TERM触发专用generation取消及精确子进程停止。每点均复核双路RX/scan mask/buffer恢复。

沿用原阈值：两次配对均须有用单音功率变化绝对值≤1dB、LO功率降低≥6dB；
分量峰值≤512计数。频率只用前32768点估计，后32767点按±1kHz固定频带计功率。
6点均完成，峰值依次7/49/34/46/79/6，全部恢复；各点耗时合计43.29秒（不含首尾全局检查）。
4次TX均完成有限FIFO喂送并退出，最终P201状态与采前完全相同、USB无占用，所有AGX/P201暂存路径消失。

### 结果及不能忽略的发送事件

| 配对 | 有用单音功率变化 | 接收LO频带下降 | 原功率验收 |
| --- | ---: | ---: | --- |
| A1→B1 | +0.0087dB | 7.3174dB | 通过 |
| A2→B2 | −0.1997dB | 7.5772dB | 通过 |

A两次有用信号/LO比0.479/0.502dB；B为7.805/7.879dB。
扣除单音估计CFO后的LO位置误差为−0.074/+0.150/−0.061/−0.359Hz，符合当前LO线位置。
这是已有约7dB增益配对改善在镜像重载后的复现，不是新增抑制方案，更不是自动校准带来的新增收益。
TX60下LO仍只比该单音低约7.8dB；不同调制/数字幅度下不能直接套用这个相对值。

**四次TX尾部标记依次U/S/U/U**。旧发送入口没有给这些标记附设备时间，无法确认发生在
31.21ms接收窗内、外或结束阶段；FIFO喂送完成不能证明USRP没有欠载/序列异常。
B2接收约3.8ms处有突发，保留全部样本，没有把它与尾部事件强行关联。
因此只把功率配对条件记为通过，不把本轮写成“无发送事件”或完整链路质量验收通过。
未运行模型、未读取源数据集，单音拟合误差不是RML识别率或独立物理SINR。

[频谱及全部接收时序](/var/tmp/sdrharness-dev/b210-cablelo-reload-20260916/baseline-spectrum.png)。
独立读取6份原始IQ核对字节/格式/峰值/频带功率、4份TX喂送字节、两组功率阈值与恢复结果通过。
模型venv缺matplotlib，改用已有系统Python3/matplotlib3.5.1完成离线绘图，未安装软件；图已目视核对。

**唯一下一实验**：固定TX60/幅度0.316227766/原LO与带宽，用带设备时间的TX事件记录入口
进行有限单音复测，定位U/S发生于稳定发送还是结束阶段，并记录RF/DSP读回；
在此之前不把再次初始化、改LO或改校准参数的差异解释为改进。该新实验尚未执行。

本轮保留62文件1,983,683字节，含6份ADC、计划、日志、图、源码及独立验证。
清理35个AGX暂存/波形副本/缓存文件328,330字节；P201暂存已核对不存在，瞬时字节未另记账。
无遗留本单元进程/socket，不修改现有任务。精确哈希、血缘和人工删除命令见
[保留审计](../evidence/B210_LO_RELOAD_BASELINE_2026-09-16.json)。

## 固定TX60单音的发送事件时间复测（2026-09-16）

用户同意上一单元建议。新单元只更换为已有的预缓冲/定时/显式EOB发送入口，并增加RF/DSP读回与
EOB主机时间记录；不改变TX60、单音幅度0.316227766、98437.5Hz、LO+250kHz、2455MHz、
Fs2.1MS/s、BW1.5MHz或RX40。发送入口整体不同，不能拆分归因于某一个缓冲或EOB动作，
也不能用本轮时间记录倒推上一轮FIFO发送的U/S发生时间。

### 实现、预算与检查

新增固定入口`validate-b210-tone-events.py`，复用现有event-controls采集/停止/恢复路径。
计划固定停流前、tone1、tone2、停流后，4×65535点ci16共1,048,560字节。
两次TX各26,112×321=8,381,952点，合计16,763,904点、≤8秒；这是既有事件入口长度，
比前轮每次8,399,872点少17,920点，不能把两个发送器当作字节/协议完全一致。
源包逐元素复用前轮1024点单音并整周期拼接，不读取RadioML或调用模型。

单点settle500ms/截止1000ms，RX外层15秒，TX外层65秒，整体400秒、外层430秒；
每次send及async分别上限20,000条，日志每次预算16,000,000字节；磁盘保留预算128MiB，
实际采前可用675,261,702,144字节。INT/TERM走既有generation取消与精确TX停止路径。
接法仍为20dB/15cm同轴→P201 RX1，运行时/daemon/Controller身份沿用前轮并逐计划固定。

同版本libuhd-dev头文件deb SHA与既有证据一致，仅解包到本单元构建目录，未系统安装。
C++以`-O2 -Wall -Wextra -Werror -pthread`严格编译通过，10项离线发送自检通过；
6项既有事件解析测试及3项新增固定计划/源连续性/时间分类测试通过。
新增校验拒绝修改发射预算、RX增益、LO、模型/源范围及二进制身份。

### 结果

| 项目 | tone1 | tone2 |
| --- | ---: | ---: |
| UHD接受样本 | 8,381,952 | 8,381,952 |
| 异步事件 | 1次burst ACK | 1次burst ACK |
| 报告U/S/late | 0/0/0 | 0/0/0 |
| ACK设备时间减名义结束时间 | +0.65405μs | +0.65405μs |
| 有用单音/接收LO功率比 | 8.29375dB | 7.77555dB |
| 扣除CFO的LO位置误差 | −0.05457Hz | −0.32537Hz |

两次configuration一致：实际RF LO=2455249999.6125693Hz，DSP=−249999.61048002754Hz，
两者相加为综合RF=2455000000.0020895Hz；采样率2100000.0074797985、MCR33600000.119676776，
TX60/BW1500000。RF/DSP独立读回的缺项已补齐，未声称读回全部内部滤波器或校准寄存器。

两个ACK均在名义结束之后；不是U/S结束阶段事件。此次没有U/S可定位，不把“未复现”写成
“上一轮U/S确定在结束阶段”。旧FIFO尾部事件依然没有设备时间；当前新入口提供了未报告
此类异常的两次有限发送。接受计数/ACK不独立证明ADC连续性或实际辐射样本完整。
P201采样起点仍未与UHD设备时钟映射，主机接收调用区间只作界限，不作逐样本时间定位。

残余LO约低于有用单音8dB，与前轮7.8dB同量级。**本轮未报告U/S，LO仍明显存在**，
因此不能将相干LO问题等同于发送尾部事件问题；尚未取得新的LO抑制收益。
所有4份IQ、两个完整send/async日志保留，未删除突发或失败样本，无模型/训练/生产配置改动。

### 恢复门的真实失败与后续验证

4个采集点均通过原双路状态恢复和USB空闲检查。最终全量preflight严格相等检查失败，
原`postflight.json`保留`restored=false`，采集程序退出1：唯一差异为RX0在已恢复的
`slow_attack`模式下瞬时hardwaregain从71变为66dB，其余频率/采样率/带宽/端口/模式/
scan mask/buffer、PID/程序身份均一致。

不写IIO、不修改门。第一次只读跟进的状态轮询达到完整相等，紧接preflight又读到61dB；
另登记最多5次的有界只读复查，第1次不等、第2次完整preflight与采前逐字段相等。
原失败快照、全部跟进均保留，证明自动增益读数随时间变化；不声称其在自动模式下应恒定71dB，
也不以省略该字段把首轮失败追溯改为成功。本单元结束前确认原门通过、USB无占用，未再发送。

独立读取全部原始IQ、每条send连续偏移/接受计数、事件数、RF+DSP恒等式、ACK/EOB设备时间、
固定后半窗频带功率以及原失败/后续完整恢复记录，全部核对通过。

### 下一实验与保留

唯一下一建议：用既有2496条验证成员的源/配对实收做有限占用频谱检查，确认有用谱与
+250kHz LO的重叠及可用通带余量，再据此选定一个LO偏移对照，避免用干净单音的结果直接改全调制链路。
本单元不扫LO/滤波参数，不把移频等同于降低本征发射泄漏；新频谱实验尚未执行。

保留50文件3,885,730字节，含4份ADC、发送事件、原失败及跟进、计划/源码/验证和构建身份。
清理179个波形副本/构建头文件及二进制/重复输出等条目1,596,141字节（包含2个头文件包链接条目），
已核验精确路径不存在；P2014个瞬时目录在执行器中逐个核对不存在，无新socket或遗留本单元进程。
编译制品可按封存源码、头包和库SHA重建，未替换既有发送程序。
精确清单及人工删除方法见[审计](../evidence/B210_TONE_EVENT_TIMING_2026-09-16.json)。

## 既有验证成员的频谱占用与LO移频几何检查（2026-09-16）

用户同意上一单元建议。本轮只用原2496条seed42 validation成员，24类×26源SNR×4条，
source/raw/guard共7488个窗，不重新收发、不加载模型、不新增预测、不修改任何IQ或生产配置。
原共同合格成员全部保留，raw/guard历史质量标志均63，丢弃0；不代表覆盖全库失败成员。

### 预登记口径与身份

1024点对称Hann估计频谱，4096点FFT仅插值频率网格，不提高原窗约2051Hz的基本分辨率。
不去均值、不陷波、不对输入滤波；Hann只用于统计。每窗频谱归一为总能量1，再等权聚合，
因此结果是频谱能量占比，不是跨窗绝对功率或物理抑制dB。
99%区间固定等尾0.5%～99.5%，不是最短区间；LO区域固定+250kHz±10kHz。
源SNR≥20dB单列576成员，其他1920条照常报告，低SNR的噪声不能当作有用调制带宽。
全部逐源SNR与高源SNR逐类统计保留；类别仅用于报告，不选择逐条处理。

名义RX通带为[−750,+750]kHz；保持有用RF不动，TX LO正偏移Δ时，TX通带在有用信号坐标下为
[Δ−750,Δ+750]kHz。仅比较现有Δ=250与单个参考Δ=750，不做参数搜索。
这个矩形掩码是几何筛查，不是模拟滤波器实测响应或实际衰减预测；带边的Hann展宽也使占比不能
解释成精确硬件损失，尤其对DC附近窄信号。

父retention SHA与仓库封存审计一致，selection/成员/标签/源Z/validation_rank、原split SHA核对通过。
26份processed.h5和原source的大小/mtime与已封存身份一致；本轮没有重算整套HDF5全文件SHA，
仅继承父哈希并新增所读2496×3窗的逐窗SHA。父NPZ读取整文件字节校验SHA，但只解码身份、
RMS/PAPR数组，不解码或利用logits/分类结果。原模型仅保留身份引用，未加载。

首次计划核对发现父行记录按采集顺序排列，已按显式index恢复对应关系，未改变成员。
首次执行又因把原source直接与processed的source比较而停止：后者实际已逐窗RMS归一化。
保留失败计划/源码/日志，新a2根改为核对“原source按历史RMS规则归一化后”与封存source逐元素一致，
2496条全通过；频谱统计仍取原source，不改变统计定义或容差。
raw/guard与父RMS/PAPR身份检查最大误差分别4.41215e−9、8.41922e−7dB，通过预登记2e−6及2e−5dB门。

### 结果与解释

高源SNR576条的source 99%带宽中位数256.35kHz、P95为269.81kHz。
source左右端点的P5/P95为−135.86/+136.38kHz；这是端点分位数，不意味着所有成员严格限制于此范围。
平均频谱显示+250kHz与主要信号谱已有明显间隔。

| 高源SNR输入 | +250kHz±10kHz平均能量占比 | 99%带宽中位数 |
| --- | ---: | ---: |
| source原值 | 0.0000309% | 256.35kHz |
| raw（已同步/校正/RMS） | 11.0483% | 379.91kHz |
| guard（现有相消/RMS） | 0.14403% | 263.53kHz |

现有相消大幅压低LO区域的归一能量，但未归零。不能把11.05%→0.144%的比值直接当成绝对LO
抑制dB，因为两个窗分别归一；本轮也未重新估计条件接收SINR或证明AM分类改善。
source逐窗标量RMS不会改变本统计的归一频谱，所以此处的原source与单位RMS源有相同理想频谱形状，
不意味着两者的模型识别相同。

| 源SNR | source LO区占比 | raw LO区占比 | guard LO区占比 |
| --- | ---: | ---: | ---: |
| −20dB | 0.9502% | 31.9945% | 1.9640% |
| 0dB | 0.4372% | 23.5279% | 0.8068% |
| +10dB | 0.0082% | 12.3220% | 0.1729% |
| +20dB | 约0.0001% | 10.6247% | 0.1454% |
| +30dB | <0.0001% | 11.3676% | 0.1562% |

低源SNR组的source 99%带宽中位数约2.057MHz，接近2.1MS/s的整个Nyquist区，主要反映源噪声。
不得据此把宽带噪声都称为调制带宽，也不得借高SNR结论承诺保留低SNR原始统计分布。
这些SNR均为源Z，不是独立实测接收SINR。

保持1.5MHz配置时，Δ250的名义TX/RX交集是[−500,+750]kHz，高SNR源频谱位于交集外的平均占比约0.00101%。
Δ750的交集变成[0,+750]kHz，交集外平均占比47.07%；多数双边谱类别约44%～53%，
AM-DSB约50%，AM-SSB短窗也处在带边；FM呈偏正频谱，不应拿其特殊结果决定所有类别的处理。
**不建议保持现有带宽而直接把LO偏移提高到+750kHz**。这不否定联合重新设计模拟带宽与频率规划，
但那已是不同配置且需独立波形保真验收。本轮没有修改或验证该方案。

[频谱/逐类几何图](/var/tmp/sdrharness-dev/rml2018a-spectrum-20260916-a2/spectral-geometry.png) ·
[逐源SNR LO区域图](/var/tmp/sdrharness-dev/rml2018a-spectrum-20260916-a2/lo-region-by-source-snr.png) ·
[逐源SNR CSV](/var/tmp/sdrharness-dev/rml2018a-spectrum-20260916-a2/per_source_snr.csv) ·
[高源SNR逐类CSV](/var/tmp/sdrharness-dev/rml2018a-spectrum-20260916-a2/per_class_high_snr.csv)。

### 唯一下一实验与收口

优先恢复此前暂停的**增加前置保护区的相消单因素对照**：仍用同2496成员对应原始ADC，
固定同步、导频频率约束、窄搜索及独立RMS，仅改变拟合使用的保护区上下文，
比较留出区预测误差、LO残留、拒绝原因，并在同原D8上报告配对纠正/回退，重点保留AM结果。
当前+250kHz已与高源SNR有用谱分离，先改善其幅相估计比盲目加大偏移更有依据；这不是认定
相消可替代硬件校准或保证候选有效。该新对照尚未执行，既有6项合成测试不能代替真实IQ验收。

预算CPU600秒/内存1GiB/输出32MiB；成功轮38.32秒、RSS131,174,400字节，无GPU。
4项频谱测试通过，独立复核150个聚合组、1872个四成员格、均谱能量和统计分母通过；图表目视通过。
两轮保留23文件7,517,400字节（含首次失败），删除3个重复计划输出/绘图缓存文件141,572字节，
两处scratch已不存在；无新IQ副本、权重、socket或遗留本单元进程。
逐文件SHA/父身份/源行血缘/人工删除命令见[审计](../evidence/RML2018A_SPECTRAL_GEOMETRY_2026-09-16.json)。
本轮仍是复用validation的诊断，不是独立最终测试。

## 增加前置保护区的单因素相消对照（2026-09-16）

用户同意后执行此前暂停的草稿对照。固定同2496条原seed42 validation、13份原ADC、原D8、
历史同步/CFO/相位、窄频搜索及逐窗独立RMS；仅增加当前导频前的完整保护区。
无新RF、源载荷辅助拟合、训练或生产配置改变。6项合成测试通过后登记计划，未按结果搜参数。

### 单因素定义、身份和基线门

原65535点上下文起于导频前256点，使用后续3段384点保护区；候选起于导频前448点，
新增当前导频前384点区间，三个后续区间的全局ADC坐标不变。
每段前192点拟合、后192点留出；候选本地坐标为[0,384)、[17920,18304)、
[35840,36224)、[53760,54144)。前帧必须存在且相邻导频间距精确17920；
其他情况及所有候选拒绝均使用已有guard输入，不退回raw、不丢样。

原/候选均使用同一单帧CUDA FP64频率拟合及历史CPU验收，搜索±30Hz、各稳定性/导频门保持。
固定档案同步值，本轮不重新优化同步。每个原ADC65535点上下文的SHA与已复现父记录完全一致；
新联合上下文另记SHA。原ADC/HDF5全文件SHA继承父封存清单，本轮只核对大小/mtime，未再全文件重算。
历史DSP与各采集plan SHA一致，原模型配置/权重/源码、split及成员血缘重新核对。
原source IQ没有进入拟合；类别和源Z只用于报告，未按类别挑处理。

预登记IQ atol=rtol=2e−6，logits atol=rtol=2e−4，基线top-1须100%一致。
实际raw/guard重放最大输入差1.19209e−7/2.38419e−7；固定全局同步旋转与历史数值路径存在
浮点舍入差，本轮不宣称raw逐位相同。guard模型最大logit差1.03474e−4、RMSE2.45307e−6，
与父保存结果top-1一致100%，通过后才运行候选预测。
共新增4992次预测：guard重放2496、候选2496；raw仅复用父预测。

### 结果：候选不启用

| 输入 | 正确/2496 | 准确率 |
| --- | ---: | ---: |
| raw | 1232 | 49.3590% |
| 原guard | 1341 | 53.7260% |
| 增加前置保护区 | 1331 | 53.3253% |

| 配对 | 纠正 | 识别回退 | 净正确数变化 |
| --- | ---: | ---: | ---: |
| raw→原guard | 331 | 222 | +109 |
| raw→候选 | 325 | 226 | +99 |
| 原guard→候选 | 4 | 14 | −10 |

候选相对原guard净退0.4006个百分点，top-1改变54条；原222条相消回退救回0条，
原331条相消纠正丢失10条。高源SNR≥20的576条也由495→490，不能把局部低SNR小幅得分当成全面改善。
四AM类合计仍154/416，纠正1/回退1；AM-SSB-WC20→19、AM-DSB-SC43→44，
AM-SSB-SC31、AM-DSB-WC60不变。4ASK仍13/104，未解决已有主要退化。
其余较明显变化包括OOK67→64、64QAM50→48、256QAM57→55。

候选应用2379条，117条保持原guard：前帧间距不符84条（17919点76、17921点8）、
没有前帧12条、guard_tone_error_margin拒绝21条。117条的预测与原guard一致，经独立核对；
全体分母2496，历史共同严格合格标志63保留，历史质量失败0，不据此推断全库拒绝率。
候选只报告新保护区/导频验收；历史strict_quality/SINR字段仍属于原处理，不冒充候选的新物理质量认证。

### 波形指标及原因边界

- 所有2496条共同后续留出区，候选/原误差功率比P5/中位/P95为0.99822/1.02430/1.28835：
  中位误差增加2.43%；只看2379条实际应用者，中位增加2.75%。
- 可核验前置保护区的2400条（包括21条原样保留者）对应误差比中位0.72024，约降低28%；
  其余96条因无前帧/间距问题不可评估此局部指标，仍在全部分类和共同后续留出统计中。
- 同载荷、相消后RMS前的+250kHz±10kHz绝对ADC频带功率比P5/中位/P95为
  0.20079/0.99335/4.66419，中位仅降0.665%，部分样本反而明显上升。
  该区域还含噪声，不应全部叫纯LO能量，更不是重新测得的物理接收SINR。

已确认：增加前置区间改变了同一个恒定复数单音拟合在前后时间段之间的折中，
局部预测改善没有变成整体留出/识别收益。未确认：LO真实幅相漂移、背景瞬态与频率估计误差
各自的贡献；不能仅由这次对照确诊硬件漂移或推出某种插值一定更好。
预登记研究门为总体正确数>1341、AM≥154、共同留出误差比中位≤1；两项未满足，候选明确拒绝。
不改阈值、不剔除117条、不部署或替换已有guard。

[逐类与逐源SNR配对图](/var/tmp/sdrharness-dev/rml2018a-leading-guard-20260916/paired-results.png) ·
[逐类CSV](/var/tmp/sdrharness-dev/rml2018a-leading-guard-20260916/per_class.csv) ·
[逐源SNR CSV](/var/tmp/sdrharness-dev/rml2018a-leading-guard-20260916/per_source_snr.csv) ·
[54条top-1变化血缘](/var/tmp/sdrharness-dev/rml2018a-leading-guard-20260916/changed-top1.csv)。

### 唯一下一实验与收口

建议先对既有保护区做**固定频率下的LO幅相随时间变化诊断**，保留每段噪声/估计不确定度，
检验是否确有稳定的时变趋势；不新增校正、不模型推理、不重新发射。
在物理/估计误差证据不足前，不直接尝试更多区间权重、插值或更宽频率搜索。
这项诊断尚未执行；当前继续保留原guard，用户轻模型训练不受本任务干预。

预算1800秒、主机8GiB/GPU4GiB、输出128MiB；实际477.47秒，GPU峰值1,718,251,008字节，
RSS1,620,369,408字节。6项合成测试、676组独立配对/分母/拒绝门/留出比复核通过，图表已目视检查。
本单元进程退出，清理354缓存/重复输出文件24,465,207字节，精确cache/scratch路径不存在。
保留24文件28,500,734字节，含逐行参数/血缘、预测、候选拒绝、代码与验证，不复制原IQ或权重。
文件SHA及人工删除命令见[审计](../evidence/RML2018A_LEADING_GUARD_2026-09-16.json)。
本轮为复用验证集的单因素诊断，不作独立最终测试或生产准入。


## 固定频率保护区LO幅相时序诊断（2026-09-17）

用户通过会话`01a0a992-837e-7622-befe-ac16ecb52a5c`要求继续，执行上一单元唯一建议。
同2496条原validation成员对应2496不同帧、13份原ADC；固定每帧历史guard频率，
不重新搜索频率、不生成新校正、不读取源载荷作拟合、不加载模型或收发。
本轮只诊断保护区相干分量，原guard及生产能力保持。

### 方法与身份复核

每帧复用3个后续384点保护区；2400帧另有可核验前置区，96帧因没有前帧或间距不符
保留缺失原因，所有2496帧均进入后续区统计。半区长度192点、每段记录复数均值、
局部去均值残差功率，以及12/24/48/96点连续分块的复数均值标准误差。
后续区相邻间距17920点（8.533ms），相位二阶差分`φ1−2φ2+φ3`消除恒定频差对应的
线性斜率；另外报告端点幅度变化和三段幅度极差。后者能显示端点相等但中段变化的情况，
不能把“仅1帧端点超过筛查线”等同于只有1帧发生任何幅度变化。

预登记描述性筛查：两个半区同号、各自超过误差传播后的4SE；并列四种块长及全部块长
共同通过数。此SE是经验指标，**不是校准置信区间或显著性检验**；相邻半区共享频率拟合，
且可能存在相关噪声，相邻上下文也可能共享ADC，不能当作独立实验重复。
历史频率由后续训练半区估计，训练侧相位斜率接近零不构成额外稳定性证据。

父rows/plan/frame索引及全部引用frame JSON SHA通过；每帧完整联合上下文及原65535点
上下文SHA均通过。大ADC/HDF5只核对父登记大小/mtime，不宣称本轮重算全文件SHA。
14,976个后续半区对历史保存复数幅度与24点SE重现通过，最大误差分别
1.0501e−10及2.2447e−11；独立多项式系数和传播误差复算2496帧通过。

### 结果与解释

| 指标 | 结果 |
| --- | --- |
| 可辨识上下文 | 2496/2496 |
| 两半区相位曲率相关系数 | 0.97833；2341帧同号 |
| 相位曲率绝对值中位数（前/后半区） | 0.07533 / 0.07468 rad |
| 相位曲率绝对值P95（前/后半区） | 0.21993 / 0.21872 rad |
| 三段幅度极差/均值中位数（前/后半区） | 0.8408% / 0.8687% |
| 三段幅度极差/均值P95（前/后半区） | 2.3855% / 2.4420% |
| 相位曲率双半区4SE筛查，块长12/24/48/96 | 1029 / 1060 / 1041 / 1034帧 |
| 相位曲率四种块长共同通过 | 888/2496（35.58%） |
| 幅度端点双半区4SE筛查，块长12/24/48/96 | 2 / 2 / 2 / 1帧 |
| 幅度端点四种块长共同通过 | 1/2496 |

13份采集各192帧，曲率共同通过52–85帧（27.08%–44.27%），现象不只集中于一份采集。
正负曲率总体对称，中位数接近零，不能解释为全程同一方向的单调漂移。
线性相位斜率仍与历史频率估计偏差混淆；非零曲率说明一个恒定频差不能独自解释全部
段间相位变化。结合半区一致性和块长敏感性，这批保护区更支持**可重复的相干相位时变结构**，
幅度变化相对较小。它尚不能确定TX/RX哪端产生、是否周期性、是否背景相干干扰，
也不能推出载荷区一定遵循同一轨迹或某种插值能提升识别。

[诊断图](/var/tmp/sdrharness-dev/rml2018a-lo-temporal-20260917/temporal-diagnostic.png) ·
[逐采集计数](/var/tmp/sdrharness-dev/rml2018a-lo-temporal-20260917/per_capture.csv) ·
[逐帧幅相与误差](/var/tmp/sdrharness-dev/rml2018a-lo-temporal-20260917/rows.json)。

### 下一建议及交付边界

下一建议是复用既有ADC的连续保护区相位序列，在固定分析规则及时间留出下检查
周期性/相关时间与局部突变，先判断相位是否可预测；仍不直接启用时变相消或新增模型推理。
这一步尚未执行。当前结果不等于修复LO、物理SINR改善或生产准入。

6项合成测试覆盖恒定单音、恒定频差、幅度趋势、相位曲率、噪声SE及坏输入，全部通过；
独立逐行复算与图表目视通过。预登记600秒/主机2GiB/输出64MiB，ADC上下文读取上界
656,218,368字节；实际诊断37.22秒，峰值RSS170,876,928字节。新RF/模型预测/训练均0。
进程退出；清理绘图缓存1文件123,432字节，精确cache目录不存在；保留13文件
11,280,182字节，无新IQ副本。保留目的、源/处理身份、SHA与人工删除命令见
[审计](../evidence/RML2018A_LO_TEMPORAL_2026-09-17.json)。无生产部署。


## 低源SNR相消回退的逐行关联（2026-09-17）

用户在论文预处理讨论后要求核对记录并“继续”，本单元优先关联既有低源SNR回退，
原连续保护区相位可预测性建议暂缓。复用同原D8 seed42/FP32/单1024窗的2496个val预测，
主分析为源Z−10至0dB的576条（24类×6档×每格4条）。不加载模型、不读ADC/HDF5/source IQ，
不重新收发、推理或变更相消。source Z不是独立物理接收SINR；全部是反复诊断过的开发证据。

### 固定分析与血缘

计划在关联执行前固定8项指标：后续保护区相位曲率绝对值（两半区均值）、guard最大误差余量、
留出残差功率及其相对单音功率、相消后payload的+250kHz±10kHz功率及其相对raw窗功率、
guard/raw RMS比、条件SINR变化。残余频带包含源/噪声，不等于纯LO；保护区指标描述整个上下文，
不能直接当作目标1024点载荷的相消误差。本轮payload频带功率继承前置保护区单元的原guard重放臂。

父审计/保留清单、预测、行记录、时序诊断及引用的frame JSON逐文件SHA核验；
source_row/validation_rank/标签/Z、frame/raw_path/上下文SHA、频率/marker位置关联通过。
原2496行共同严格合格、guard均应用，质量失败0；不扩大到失败群体。
独立从原NPZ逐行重算预测与身份，再复算所有四状态分布、8指标和匹配比较通过。

### 回退集中在哪里

576条：未相消185正确、guard115正确、共用raw RMS102正确；原guard产生106回退、36纠正，
另有79条保持正确、355条两者均错误。以下各类分母均24，不按结果删样：

| 类别 | 原始源正确 | source RMS正确 | raw正确 | guard正确 | 回退/纠正 |
| --- | ---: | ---: | ---: | ---: | ---: |
| AM-DSB-WC | 21 | 23 | 21 | 3 | 18/0 |
| AM-SSB-WC | 19 | 14 | 12 | 3 | 10/1 |
| AM-SSB-SC | 8 | 9 | 9 | 2 | 9/2 |
| AM-DSB-SC | 4 | 3 | 3 | 10 | 0/7 |
| FM | 24 | 24 | 23 | 13 | 10/0 |
| 4ASK | 11 | 10 | 14 | 9 | 8/3 |
| 8ASK | 9 | 4 | 7 | 1 | 7/1 |
| 8PSK | 3 | 3 | 8 | 0 | 8/0 |

106回退中37条来自四AM类；最常见单一转移为AM-DSB-WC→AM-DSB-SC的9条，
其次4ASK→OOK7条、8ASK→4ASK6条。预测转移不能证明有用载波被物理减掉。
106条中原source正确78条、source RMS也正确78条，其中74条两者都正确；
28条原source错误不能等同于28条干扰收益，物理原因尚未隔离。
AM-DSB-WC的18条回退在source RMS上全部正确，FM的10条回退在两种source上都正确。
因此不能把这批回退全部解释成源域不可识别，也不能全部解释成source仅做RMS后的损失。
共用raw分母仅救回106条中的5条，之前的幅度对照不重复执行。

### 误差指标是否随回退增大

106条回退的条件SINR全部提高，中位+1.741dB；79条保持正确的中位+1.706dB。
这是含源噪声/链路失真假设的条件估计，不能据此称物理SINR改善，更不能当作分类质量门。

| 指标 | 回退106条中位 | 保持正确79条中位 | 同类同Z比较中回退指标更大的比例 |
| --- | ---: | ---: | ---: |
| 相位曲率绝对值/rad | 0.06763 | 0.06962 | 0.5595 |
| guard最大误差余量 | 0.07532 | 0.06774 | 0.4405 |
| guard留出功率/counts² | 8.6586 | 7.0575 | 0.4702 |
| guard留出/单音功率 | 0.01317 | 0.01013 | 0.4702 |
| payload LO区域/counts² | 22.9539 | 17.4059 | 0.5536 |
| payload LO区域/raw总功率 | 0.009369 | 0.006233 | 0.4821 |
| guard/raw RMS | 0.85117 | 0.87041 | 0.5833 |
| 条件SINR增量/dB | 1.7411 | 1.7057 | 0.4524 |

右列先在同类别/同源Z格内计算回退与保持正确的全部两两比较（相等记0.5），再对格等权平均；
只有14格同时有两种状态，覆盖25/106条回退和24/79条对照、39对，81条回退无法同格比较。
每格源行不同，可能有时间/共享ADC相关性；不计算伪独立显著性或据此设置切换阈值。
未匹配时某些功率指标偏大，但匹配后方向不一致；相位曲率两组分布相近，原四块长筛查
分别33/106、27/79。当前证据不足以把低SNR回退归因于更大相位曲率/保护区误差，
也不能从有限匹配结果排除载荷内失真或证明相消无害。

### 结论、下一建议和收口

可确认的是**识别回退具有类别差异**；接收处理与模型适配的机制仍未隔离，不能说“相位变化已解释106条回退”。
其中AM-DSB-WC与FM保留清楚的源域可识别反例；“未相消干扰偶然帮助模型”仍是未验证假设。
下一建议仅为同576成员的source RMS/raw/guard载荷特征对照，检查带载波分量、幅度包络及
噪声频谱形状的变化；全部成员保留，重点解释AM-DSB-WC/FM，不读取类别来选择校正参数，
不新增推理或部署。本项尚未执行；先不直接试新相消/FFT阈值，不恢复训练。

4项测试覆盖重复ID拒绝、类别/SNR混淆隔离、格等权/排除错误状态及相等值处理；
独立2496行/8指标复算、图表目视通过。首次缓存所有帧JSON导致RSS1,990,369,280字节，
超过预登记1GiB，结果不验收且失败plan/日志/代码/results保留。修正为单文件缓存后原门通过：
15.58秒、RSS156,372,992字节，300秒/1GiB/32MiB预算不变；统计与首轮完全一致。
删除首轮三份与接受轮逐字节一致的重复CSV及绘图缓存，共4文件1,196,676字节；cache不存在，
本单元进程退出。保留20文件2,827,409字节，无新IQ副本或生产部署，精确SHA/删除方式见
[审计](../evidence/RML2018A_LOW_SNR_ASSOCIATION_2026-09-17.json)。

[类别和指标图](/var/tmp/sdrharness-dev/rml2018a-low-snr-association-20260917/low-snr-association.png) ·
[106条回退血缘](/var/tmp/sdrharness-dev/rml2018a-low-snr-association-20260917/low-snr-regressions.csv) ·
[混淆转移](/var/tmp/sdrharness-dev/rml2018a-low-snr-association-20260917/regression-confusions.csv)。

## D10替换重跑预检（2026-09-22）

用户指定 `/home/jetson/models/d10/rml2018a/d10_2018a_seed42_nw8/best.pt`
替换当前D8离线对照；本轮按同2496条成员（低源SNR主576条）、单1024点FP32、
source/source RMS/raw/guard口径准备，旧证据保持。不是全库任务或生产服务切换。

预检基于仓库 `dfb4e5a1a99a5e2f5ab76bce92eafd8eb14d465f`：

- 权重1,603,050字节，SHA256
  `5d217592cabab54be4352d8ebddcc47eb9cf9b7ea81d1010027e8dd213546b81`，与同目录manifest一致。
- 使用既有模型venv、`-B`、CPU `torch.load(weights_only=True)`成功，
  `model_state`有70个张量，含`_d10_schema`、gated stem、segment/evidence pool等。
  这只证明文件可读，未完成计算图严格加载或AGX数值兼容性验证。
- 配置声明AMCMambaD10、126958参数、24类；manifest的标签为数字0–23。
  后续须核对对应源码、原划分成员及标签语义，不能从D8键名重建/猜测forward。
- 附带README明确要求D10计算图。其来源是shapan4090上的
  `/home/luzhouchao/projects/mamba`，训练提交声明
  `76657c78a9c0681b88753487b842a1263d328c27`且dirty，故仅提交号不足以重建源码。
- 按connect-4090-server技能只读检查既有`4090-via-aliyun`：实际目录为
  `/data/lzc/mamba`、HEAD `daecb2f24e143fdac0912c2fb3bd436a343ea974`，
  models及results_package内未找到D10。远端没有rg，改用有界路径find核对。
  本地权重目录仅含README、checkpoint、config、manifest与metrics，没有模型源码。

已向用户询问对应D10源码位置或shapan4090连接；新推理尚未启动，结果不得标为完成。
未创建开发产物、缓存目录或IQ副本：本单元新增保留/删除均0文件0字节；
用户权重保持原处，不纳入开发清理。CPU预检与SSH命令均已退出，无RF、训练或部署变更。
下一步取得训练对应的源码及依赖后，登记有限预算/输入与模型身份，再严格加载、
做有限数值检查并执行同成员对照；保持recognizer_available=false。

## D10同成员重跑完成（2026-09-22）

用户补充本地`source_03fa833/`及`LOAD_D10.md`后，执行
`jetson-agx/sdrharness/scripts/rml2018a_d10_replay.py`，复用原2496条成员及既有
source/clean raw/clean guard HDF5，增加source RMS诊断。无新收发或ADC重解码；
本轮只替换模型，单1024点FP32。D8使用历史预测，未重推理。

| 范围与模型 | source | source RMS | raw | guard | 相消纠正/回退 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 全2496 D8 | 1578 | 1425 | 1232 | 1341 | 331/222 |
| 全2496 D10 | 1618 | 1463 | 1286 | 1364 | 284/206 |
| 源Z−10至0、576条 D8 | 195 | 185 | 185 | 115 | 36/106 |
| 源Z−10至0、576条 D10 | 210 | 203 | 190 | 131 | 39/98 |

D10低源SNR raw32.99%、guard22.74%，相消净减少59条正确（10.24个百分点）；
D8为32.12%→19.97%。更换D10改善了本子集相消后的识别，但未消除退化。
全体D10 raw51.52%→guard54.65%，不能把全体收益推广至各SNR。
低档AM-DSB-WC raw16→guard5、FM24→19（各24条），仍有明确局部退化。
这些是源Z分组，不是独立实测接收SINR，不证明相消移除了有用载波或相位漂移是原因。

验证及边界：

- supplied SOURCE_MANIFEST全部源码SHA核对通过，D10 `model_state` strict=True加载，
  126958参数及真实Mamba2后端一致，无CPU/fallback替代。权重SHA同预检。
- D10数据集源码按seed42重新生成的val与既有383385成员精确一致；
  原2496 IDs/validation_rank、Y/Z及raw/guard严格质量全部核对，未筛掉任何旧成员。
  训练数据入口仅转轴/FP32且配置关闭去噪，无外部RMS；source RMS明确单列。
- 四路各49探针覆盖24类/26个源Z，单条对批量的logits均通过原2e−4绝对/相对门、
  top1一致100%；最大绝对差4.462e−5。实际128批量对应探针也通过同门。
- 独立脚本读回预测，对ID、标签、全体/低档两模型正确及纠正回退数、26档D10正确数
  全部重算通过；低档四路混淆计数保存。图已目视检查。
- 实际99.21秒、峰值RSS1,420,001,280字节、CUDA395,857,920字节，
  低于1800秒/8GiB/4GiB预算；输出低于128MiB。仅有TF32配置API弃用警告。
- 原HDF5沿用父完整SHA及精确大小/mtime门，本轮另存选中输入SHA与RMS一致性；
  没有重新哈希全部HDF5。模型及源码在结束后重新哈希通过。
- 提供源码提交03fa833与训练配置声明的dirty 76657c78不同；本轮核验了供给清单、
  严格加载和AGX数值一致性，没有原训练主机logits，不能宣称跨主机逐值重现。
  manifest另声明seed42由五seed测试准确率选出，本次反复诊断成员也不是独立locked-test。

证据根`/var/tmp/sdrharness-dev/rml2018a-d10-replay-20260922`，
保留12文件1,235,912字节；删除精确cache目录内376文件25,711,366字节，目录不存在。
本单元进程已退出，无IQ副本、训练或服务部署，recognizer_available保持false。
权重和用户供给源码保持原位置，未纳入清理。
[逐文件审计及人工删除命令](../evidence/RML2018A_D10_REPLAY_2026-09-22.json)；
[比较图](/var/tmp/sdrharness-dev/rml2018a-d10-replay-20260922/comparison.png)。

## D10低源SNR载荷特征排查（2026-09-22）

用户要求继续解释低源SNR相消退化。固定D10原2496成员及原预测，主组为源Z−10至0的576条，
98回退/39纠正/92保持正确/347均错；无新推理、RF或训练。先登记指标、频带与预算，
读取同一source/raw/guard HDF5，选中2496输入字节SHA与D10重跑记录完全一致。
通过原baseline逐行received_rms把raw/guard恢复到共同ADC尺度，避免将归一化误作实际删除。
频谱沿用2.1MS/s、Hann1024、FFT4096，LO240–260kHz，中心±10kHz，不去均值。
source本身包含合成噪声，所有source-fit残差仅称拟合残差，不是独立纯噪声估计。

98条回退的主要结果：

| 指标 | 结果 |
| --- | --- |
| 归一频谱对同源PSD的总变差距离变小 | 98/98；距离变化中位−0.10043 |
| 对源复数波形的归一相干度提高 | 98/98；变化中位+0.21255 |
| 包络变异系数更接近源 | 62/98 |
| 被减去波形的LO频带能量占比 | 最小99.9995416% |
| 中心±10kHz实际功率相对变化绝对值 | 最大5.417e−7（约0.0000542%） |
| 排除LO区并重新归一后的谱距离变化 | 中位−8.66e−7，范围−1.31e−5至+2.07e−5 |
| 相消后归一化对剩余分量的共同放大倍数 | 中位1.17546，范围1.10994–1.26166 |
| 原source预测正确 | 74/98 |

全部576条的谱距离和源相干度都改善，不只回退组。因此本指标不能解释哪些行会回退。
这批证据不支持相消直接删除中心载波；可以定位为主要去除+250kHz窄带分量，
并因逐窗RMS而共同放大剩余信号/噪声。谱距离改善不等于分类边界更合适，
相干度也不是独立物理SINR。LO区内是否仍含少量有用信号、时变残余影响不能由此完全排除。

回退对保持正确按同类/同源Z配对，仅20格、35/98回退与34/92对照、52对；
63条回退没有匹配对照。八指标按格等权计算，回退值更大的比例：
包络误差变化0.6583、全谱距离变化0.4000、LO外谱距离变化0.4667、
相干度变化0.5042、中心功率变化0.4042、被减去LO比例0.6333、
RMS倍数0.5333、源拟合残差尾带占比变化0.5125。
包络是有限线索，未作显著性检验；LO比例差异仅约1.22e−9，不能因排序比例偏离0.5就解释为物理原因。
尾带（|f|400–700kHz）占比增加可由去掉残差中窄带能量导致，不是新增宽带噪声的证据。

当前更值得验证的是D10分类对“窄带分量去除”和“RMS尺度变化”的分别响应，
而不是直接调大相消力度或按类别设计滤波。已有D8共用RMS失败不能直接替代D10实测。
本轮没有运行这些新干预；未完成退化的因果归因，也不把“干扰帮助模型”写成结论。

初次系统Python缺h5py，改用既有venv；其后严格ID门发现父baseline按处理顺序存储，
原失败和预登记保留。a2按明确index排序，再核对0..2495及源ID，未改门或删样。
实际10.98秒、峰值RSS306,683,904字节，低于600秒/1GiB/32MiB预算。

解析单音位置/中心载波保持/逆RMS不变性检查通过；独立验证从HDF5逐行读回576条，
另行向量FFT复算中心变化、删除LO占比、谱距离，状态统计及八指标匹配结果全部通过。
复核脚本构建行映射耗时数分钟，未据此声称诊断算法性能改善。
留存两根共10文件1,487,173字节（含失败预登记/执行代码），无非保留缓存/IQ副本，
删除0文件0字节，进程均退出。
[精确保留、SHA及人工删除命令](../evidence/RML2018A_D10_PAYLOAD_FEATURES_2026-09-22.json)。

## D10相消与RMS尺度分离干预（2026-09-22）

用户继续后，固定原2496成员、D10 seed42 FP32/单1024点，主576条源Z−10至0。
以原ADC窗R、相消后G及各自RMS r/g定义k=r/g，四路：
A=R/r（raw），B=G/r（guard_shared），C=R/g（raw_scaled），D=G/g（guard）。
同一条样本的k在有/无相消两支固定；C是使用guard结果得到k的反事实对照，
不是独立raw端可部署增益估计。无阈值搜索、重新拟合LO、RF或训练。

| 主576条输入 | 正确数 | 准确率 |
| --- | ---: | ---: |
| A 原raw | 190 | 32.99% |
| B 只相消，使用原raw分母 | 105 | 18.23% |
| C 只改变尺度，保留LO | 176 | 30.56% |
| D 当前相消及RMS | 131 | 22.74% |

固定原尺度A→B净−85条（37纠正/122回退）；固定放大尺度C→D净−45条
（41纠正/86回退）。尺度单独A→C净−14条，已相消后B→D反而净+26条
（49纠正/23回退）。正确数交互D−B−C+A=+40，说明不能把两因素当成简单相加。
原A→D的98回退中，B仍错93条，改用raw分母仅救5条，且损失20条原相消纠正。
因此本固定子集的主要退化出现在去除LO分量后，不能归结为相消后RMS重归一化；
当前RMS对已相消输入有部分补偿效果。B不作为修复启用。

全2496条A/B/C/D正确1286/1254/1255/1364，整体与低源SNR效应不同。
结合上一单元中心载波几乎不变、源谱/相关性改善的结果，本轮支持继续研究模型
对窄带分量去除的响应，不能宣称已经证明它学到了LO捷径、相消删除有用载波，
或已确定训练域失配的内部机制。未来可做固定尺度下匹配强度的窄带回添及相位/频率
对照来区分敏感性；本轮未执行，也不按标签切换预处理。

验证：所有供给源码/权重身份及严格加载通过，原val重建一致。
原D10 source/raw/guard选中输入SHA全部一致；A/D全2496 logits逐值相同，top1一致100%。
四路49探针单条/批量及实际128批量通过2e−4绝对/相对门与top1一致门，最大差5.7221e−5。
独立脚本按source ID重建2496条RMS比与四路输入哈希全部通过；预测读回复算
全体/主组五种配对、26档及24类正确数、回退救回与交互项通过；图已目视检查。
实际95.44秒、RSS1,438,490,624字节、CUDA395,857,920字节，
低于1800秒/8GiB/4GiB/128MiB预算；仅有既有TF32 API弃用警告。
这些仍是反复诊断过的工程成员；供给源码与dirty训练版本的来源限制继承D10重跑记录，
不是独立locked-test、训练域因果或物理SINR校准证据。

保留`/var/tmp/sdrharness-dev/rml2018a-d10-factorial-20260922`的15文件1,259,172字节；
删除cache目录376文件25,873,563字节，目录不存在，进程全部退出。无IQ副本、RF、训练、
部署或recognizer_available变更。权重和原始证据保持。
[审计与人工删除命令](../evidence/RML2018A_D10_FACTORIAL_2026-09-22.json)；
[四路对照图](/var/tmp/sdrharness-dev/rml2018a-d10-factorial-20260922/factorial.png)。

## D10固定尺度窄带回添对照（2026-09-22）

用户继续后，固定同2496成员与D10 FP32单1024点，主组576条源Z−10至0。
定义b=G/r（上一单元guard_shared），d=raw−b；所有变体为
b+αd·exp(jφ+j2πΔf n/2.1e6)，沿用原raw分母，不再逐变体归一化。
预先固定α∈{0.5,1}，φ∈{0,π/2,π}，Δf∈{0,−125k,+125k}Hz，不搜索最优参数。
回添分量能量在相位/频移组匹配，半幅组为原能量1/4；输入总功率因交叉项不严格匹配。
相位/频移按单窗n=0起算，是模型输入干预，不声称连续物理振荡器实验。

| 输入 | 低源SNR正确/576 | 全体正确/2496 | 挽回raw→b的122条回退 |
| --- | ---: | ---: | ---: |
| 不回添b | 105 | 1254 | 0 |
| 半幅原分量 | 137 | 1308 | 28 |
| 原分量完整回添 | 190 | 1286 | 122 |
| 原频率，相位+90° | 189 | 1307 | 92 |
| 原频率，相位+180° | 189 | 1287 | 95 |
| 频移至约125kHz | 27 | 220 | 1 |
| 频移至约375kHz | 181 | 1219 | 93 |

这里122条是raw→固定raw分母相消的回退，不是原raw→guard的98条，不能混用。
完整回添的输入/logits逐值重现raw是代数正对照，不是恢复有用信号的独立证明。
+90°/+180°总体正确数接近raw，但逐行预测与raw仅68.23%/67.19%一致；
375kHz组64.76%，故不能声称模型对相位不敏感或所有原正确行均恢复。
相对b，+90°为118纠正/34回退，+180°为116/32，375kHz为112/36。

与上两单元合看，低源SNR退化更符合D10对窄带分量存在/强度/位置的分类响应；
精确原相位不是本组总体准确率恢复的必要条件。仍不能断言训练时学了LO捷径，
也不能把LO视为有用调制载波或建议生产故意加干扰。125kHz组可能与有用频带重叠，
本轮未量化各样本谱重叠，不能仅凭该组称模型依赖250kHz频率。
没有相同总输入功率的宽带扰动对照；窄带结构、总能量及包络交叉项仍需区分。
下一建议为固定成员的总功率匹配对照（包括无窄带的尺度控制及宽带扰动），
或直接分析模型中间表征；均未在本单元执行，不训练、不按类别切换、不调相消阈值。

验证：原raw/guard_shared与父factorial全2496 logits逐值一致；完整回添与raw输入及logits逐值一致。
八路49探针单条/批量和实际128批量通过2e−4绝对/相对容差及top1一致门，最大差5.7221e−5。
独立脚本重新读HDF5重构六变体，2496输入哈希一致；验证旋相/频移的回添能量守恒、
半幅能量1/4及谱峰落在目标频率一个FFT bin内。ID、原val成员、全体/低组/26档/24类
统计和纠正回退读回复核通过，图已目视检查。来源与反复诊断成员限制继承D10原记录。
实际107.62秒、RSS1,667,985,408字节、CUDA395,857,920字节，
通过1800秒/8GiB/4GiB/128MiB门；无失败，仅既有TF32 API弃用警告。

保留`/var/tmp/sdrharness-dev/rml2018a-d10-tone-controls-20260922`的14文件2,286,332字节；
删除cache目录376文件25,713,620字节，目录不存在，进程退出。无IQ副本、RF、训练、
服务/生产配置变更，recognizer_available保持false。
[审计与人工删除命令](../evidence/RML2018A_D10_TONE_CONTROLS_2026-09-22.json)；
[回添对照图](/var/tmp/sdrharness-dev/rml2018a-d10-tone-controls-20260922/tone-controls.png)。

## D10总功率匹配对照与诊断收束（2026-09-22）

用户同意最后一轮总功率匹配对照后执行。保持同2496成员、D10 seed42 FP32单1024，
主组576条源Z−10至0；不训练、不收发、不改相消阈值。令b=G/r、d=raw−b，
对照为b仅缩放、b+d（原raw窄带回添）、b+w（宽带扰动）。每条输入最终复数总功率
均匹配该条原raw的功率。w为全采样带内独立复高斯扰动，先按窗匹配d的能量，
再对b+w整体归一化；最终缩放后添加分量与d能量不再强制相同。
使用预登记三个种子20260922/23/24，PCG64 SeedSequence([seed,source_row])；
所有实现均报告，不择优。三种噪声实现不是三个独立数据集。

| 总功率匹配后的输入 | 低源SNR正确/576 | 准确率 | 全体正确/2496 |
| --- | ---: | ---: | ---: |
| 相消后仅缩放 | 131 | 22.74% | 1364 |
| 回添原窄带（raw） | 190 | 32.99% | 1286 |
| 宽带种子20260922 | 142 | 24.65% | 1189 |
| 宽带种子20260923 | 144 | 25.00% | 1216 |
| 宽带种子20260924 | 147 | 25.52% | 1201 |

相对仅缩放，窄带组98纠正/39回退；三个宽带组分别44/33、41/28、47/31，
净提升11/13/16条。原raw→guard的98回退中，宽带仅救回27/27/22条。
窄带组就是raw的代数重现，其救回98条不是独立恢复有用信号的证明。

结论限于本固定工程子集：相同总输入功率下，所测宽带扰动未重现窄带回添效果，
总功率差异不足以解释窄带组的分类变化。结合前两轮，支持D10对输入频谱结构/位置
与扰动形式敏感，不支持将低源SNR退化主要归于RMS或中心载波误删。
但全带宽白噪声与窄带分量在有用频带重叠、相干性、包络及源相关性上不同；
本实验不能把这些因素完全分开，更不能证明训练时学到LO捷径。
宽带三个实现仅作稳健性观察，没有显著性检验、独立样本扩增或独立测试结论。
整体2496条仍是相消后仅缩放最好，不能因为低档回添改善而建议默认保留LO、
主动加扰动、按真实标签/SNR选择支路或事后选噪声种子。

本轮后收束这条诊断，不再自动展开更多扰动/调参。适合论文的表述是：
“在本同轴工程对照中，保护区参考相消改善总体识别，但低源SNR下存在分类退化；
该退化在更换D10及控制归一化尺度后仍存在。总输入功率匹配的反事实对照表明，
分类响应还受到扰动频谱结构影响，波形一致性指标改善不保证分类性能改善。”
这是已观察现象及适用边界，不作为已验证的新相消方法或独立模型准入成绩。
论文模型主线可据此继续，训练/生产授权保持原边界。

验证：原raw/guard全2496 logits逐值重现，窄带组与raw logits相同；
七路49探针单条/批量及实际128批量通过2e−4绝对/相对和top1一致门，最大绝对差1.30654e−4。
独立重读HDF5，重建五组输入与三噪声哈希、添加能量门及全部2496成员、
全体/主组/26档/24类正确与纠正回退数通过。总功率最大相对误差8.659e−9，
低于预登记2e−7门；聚合白噪声最大单FFT bin占比约0.00104，仅作宽带实现诊断。
图已目视核对。实际106.94秒、RSS1,674,469,376字节、CUDA395,857,920字节，
通过1800秒/8GiB/4GiB/128MiB门；仅既有TF32 API弃用警告，无运行失败。
供给源码与dirty训练版本的来源限制、重复诊断成员非locked-test边界均沿用原记录。

保留`/var/tmp/sdrharness-dev/rml2018a-d10-power-match-20260922`的15文件1,988,981字节；
删除cache目录376文件25,841,351字节，目录不存在，进程均退出。
无IQ副本、RF、训练或生产服务修改，recognizer_available保持false。
[逐文件审计及人工删除命令](../evidence/RML2018A_D10_POWER_MATCH_2026-09-22.json)；
[匹配功率对照图](/var/tmp/sdrharness-dev/rml2018a-d10-power-match-20260922/power-match.png)。

## 实测结果按接收SINR重分层（2026-09-22）

用户要求实测结果均按SINR。已将此报告口径写入AGENTS：源Z只作溯源，实收图表使用
接收端SINR，现有数据标注“条件估计SINR”，不得称独立标定真值。旧封存源SNR图表不改字节，
本单元生成有血缘的新报告；纯源域离线模型实验不由本规则改为虚构接收SINR。

重分层范围：既有8个seed42模型全validation共同严格合格369497源行；
另报原2496诊断成员上的8模型+D10，共9模型。D10未跑满369497，不混合分母。
只读归档metadata/logits与已有SINR，不重读IQ、不新推理、不收发。
主表用raw相消前估计SINR统一分箱，同一格raw/guard/所有模型比较相同成员；
辅表按raw/guard各自的SINR分箱，整体仍为共同人群，但格内成员不同，不可直接当配对收益。

固定边界−20/−15/−10/−5/0/5/10/15/20 dB，左闭右开，两端开放组，
非有限值单列invalid。369497及2496两个人群这次均无非有限值；不能推广为所有采集
都有有效估计。共同369497的raw SINR范围−26.943～13.413 dB，guard−26.054～27.286 dB。
raw各非空组计数30836/35756/39797/33724/36127/43657/134015/15585；
15 dB以上及invalid为空组，CSV仍保留。不同SINR格的类别构成可能不同，不能由曲线直接定策略阈值。

D10同2496成员的配对结果：

| raw参考条件估计SINR/dB | 行数 | raw正确率 | guard正确率 | 变化/百分点 |
| --- | ---: | ---: | ---: | ---: |
| <−20 | 200 | 3.00% | 3.00% | 0.00 |
| [−20,−15) | 231 | 6.49% | 3.03% | −3.46 |
| [−15,−10) | 258 | 16.67% | 8.53% | −8.14 |
| [−10,−5) | 232 | 37.50% | 24.14% | −13.36 |
| [−5,0) | 238 | 52.52% | 48.74% | −3.78 |
| [0,5) | 287 | 68.99% | 78.40% | +9.41 |
| [5,10) | 925 | 76.86% | 89.95% | +13.08 |
| [10,15) | 125 | 80.80% | 80.00% | −0.80 |

8模型369497共同人群的[−10,−5) dB档（33724条）相消变化：
CNN2 +1.13、ResNet −1.08、GRU −10.73、CLDNN +10.24、MCLDNN −10.23、
MCformer −6.00、MAMC +0.21、D8 −10.24个百分点。
这确认按接收SINR展示后，低档退化仍存在且依赖模型；没有自动推导SINR=0为开关阈值，
尤其此估计使用已知源参考，不能假定普通未标注接收拥有同等可用指标。

验证：父metadata/预测逐文件SHA通过，所有保存predicted_class与logits argmax及有限性重核；
源ID唯一、共同成员、原validation_rank及全部8模型预测覆盖通过。
D10同成员SINR与原baseline浮点容差内一致，分箱无变化。
独立区间比较（不复用searchsorted）、187行配对统计、374行自身SINR统计、
8模型原26个源标签分组的旧正确数、边界及NaN/正负无穷单列测试通过。
实际13.71秒、RSS244,051,968字节，通过600秒/1GiB/128MiB预算。
模型venv在数值复核完成后绘图缺matplotlib，失败脚本/事实保留；改用已有系统Python
绘图通过，未安装软件或修改统计。三张图已目视检查；热图统一±75个百分点色标，
自身SINR曲线开放两端用±22.5 dB作显示坐标并明确标注，并非尾组测量均值。

保留`/var/tmp/sdrharness-dev/rml2018a-sinr-stratification-20260922`的14文件6,848,537字节；
删除绘图cache内1文件123,432字节，目录不存在，进程均退出。无IQ副本、新模型推理、
RF、训练或生产服务变更。保持recognizer_available=false。
[审计与人工删除](../evidence/RML2018A_SINR_STRATIFICATION_2026-09-22.json)；
[9模型同2496配对图](/var/tmp/sdrharness-dev/rml2018a-sinr-stratification-20260922/shared2496-paired.png)；
[8模型369497配对图](/var/tmp/sdrharness-dev/rml2018a-sinr-stratification-20260922/common369497-paired.png)；
[各自SINR曲线](/var/tmp/sdrharness-dev/rml2018a-sinr-stratification-20260922/own-sinr-curves.png)。

## source曲线追加取消（2026-09-22）

用户先要求解释raw/guard并加入未经过RF的source曲线，随后明确“不要加source了”。
只读source预测关联在处理取消前已正常退出（9.58秒，RSS141,807,616字节）；
未生成新图、未新推理、未读取IQ或进行RF/训练。按最新指令不交付source曲线，
既有raw/guard SINR图表保持。raw是同步/CFO及相位校正后、不作保护区LO相消的
归一化载荷；guard是保护区参考LO相消后、按自身RMS归一化的载荷，不是保护区样本本身。

清理精确根`/var/tmp/sdrharness-dev/rml2018a-sinr-source-comparison-20260922`内
三份派生输出及未跟踪的临时仓库脚本，共4文件6,320,881字节；逐路径确认不存在。
仅保留预登记、执行脚本快照和取消/删除SHA清单，共3文件331,002字节，以复核取消事实；
不保留source新比较结果，不改历史预测/IQ/图或生产服务，进程已退出。
[取消与精确清理审计](../evidence/RML2018A_SOURCE_OVERLAY_CANCELLED_2026-09-22.json)。

## 当前图表以D10替代D8（2026-09-22）

用户要求移除D8，D10为当前模型。由封存SINR结果派生现用报告，当前8模型为
CNN2-stable、ResNet、GRU、CLDNN、MCLDNN、MCformer、MAMC、D10。
2496共同诊断成员的自身SINR曲线调整为2×4子图，配对热图与CSV也去除D8；仅raw/guard，无source。
369497共同验证成员图表只有其余7模型，未给D10补造该范围数据。
历史D8预测/权重/封存图留作历史证据，本次删除范围为当前对比展示，不进行权重或生产配置替换。

父results/plan SHA核验、所有保留记录与父过滤D8后的结果逐项相同、当前模型8/大样本7、
无D8/无source及无D10大样本数据检查通过。自身SINR各格成员不同，配对图仍统一raw参考SINR。
三图已检查，未改变数值门、分组或预测。系统Python绘图正常退出，无新推理、IQ读取、RF、训练或部署。
保留`/var/tmp/sdrharness-dev/rml2018a-current-model-plots-20260922`的10文件678,296字节，
删绘图cache1文件123,432字节，目录不存在，进程退出。
[保留审计及人工删除](../evidence/RML2018A_CURRENT_MODELS_2026-09-22.json)。

- [现用8模型raw/guard曲线](/var/tmp/sdrharness-dev/rml2018a-current-model-plots-20260922/own-sinr-curves.png)
- [现用8模型同成员相消变化](/var/tmp/sdrharness-dev/rml2018a-current-model-plots-20260922/shared2496-paired.png)
- [其余7模型大样本配对](/var/tmp/sdrharness-dev/rml2018a-current-model-plots-20260922/common369497-paired.png)

## D10补齐369497共同验证成员（2026-09-22）

用户明确授权补跑D10。沿用SINR重分层封存的369497共同seed42 validation成员，
raw/guard均为usable且strict_quality_pass，源行与validation_rank逐条核对；未追加source。
使用本地D10 best.pt（SHA `5d217592cabab54be4352d8ebddcc47eb9cf9b7ea81d1010027e8dd213546b81`）、
source_03fa833、严格加载126958参数、真实Mamba2、FP32/TF32关闭、batch128，直接读取既有单窗RMS HDF5。
每路369497条，共738994预测；没有RF、训练或生产切换，recognizer_available=false。

运行636.15秒，峰值RSS1,432,268,800字节、CUDA394,199,040字节，均在预登记预算内。
每路48条singleton/batch门通过，最大logits误差raw2.09e−5、guard1.45e−5；
两路与此前2496成员全部logits逐值相等、top1一致。原验证划分重建相等；
HDF5依据既有SHA身份以大小/mtime核对，未重新完整哈希42GB输入。
独立区间掩码重新验证88配对分层、176自身SINR分层、逐行身份/argmax及总体统计；
其余7模型所有保留统计与封存父结果逐项一致。三图检查通过，调整长标题布局后重新绘图，未重复推理。

| 相消前条件估计SINR | 样本 | raw准确率 | guard准确率 | 变化百分点 |
| --- | ---: | ---: | ---: | ---: |
| 总体 | 369497 | 50.45% | 55.09% | +4.65 |
| <−20 | 30836 | 4.38% | 4.23% | −0.15 |
| [−20,−15) | 35756 | 6.78% | 4.41% | −2.37 |
| [−15,−10) | 39797 | 16.97% | 8.89% | −8.08 |
| [−10,−5) | 33724 | 34.70% | 25.40% | −9.30 |
| [−5,0) | 36127 | 52.56% | 50.63% | −1.93 |
| [0,5) | 43657 | 67.95% | 80.69% | +12.74 |
| [5,10) | 134015 | 76.66% | 91.30% | +14.64 |
| [10,15) | 15585 | 81.94% | 81.51% | −0.44 |

总体正确186393→203566，净增17173；低SINR退化在大样本仍存在，不能据此确定内部机制或自动设切换阈值。
相消收益使用同成员及raw参考分箱；自身SINR曲线各格成员不同。SINR仍是条件估计，未独立标定。
此为既有同轴工程语料验证对照，未升级为独立locked-test或生产准入。

- [8模型自身SINR曲线](/var/tmp/sdrharness-dev/rml2018a-d10-common-20260922/own-sinr-curves.png)
- [8模型统一raw参考配对曲线](/var/tmp/sdrharness-dev/rml2018a-d10-common-20260922/raw-reference-sinr-curves.png)
- [8模型相消收益](/var/tmp/sdrharness-dev/rml2018a-d10-common-20260922/paired-gain.png)

本单元进程正常退出；清理377缓存文件25,836,886字节，cache目录不存在。
保留19文件87,923,648字节，包括两路逐行预测、脚本、表图和核验记录，无IQ副本；
精确路径、SHA、父血缘与人工删除见[保留审计](../evidence/RML2018A_D10_COMMON_2026-09-22.json)。

## D10部分相消与保护区关联（2026-09-26）

用户要求继续优化数据处理，并允许必要时重新设计传输。本单元先完成现有2496条诊断成员的
固定五档相消，未重采RF或改变帧结构。与旧固定raw分母回添不同，本轮对每个候选独立RMS归一化：
R=raw×历史raw ADC RMS，G=guard×历史guard ADC RMS，输入为normalize((1−α)R+αG)，
α=0、0.25、0.5、0.75、1。端点直接使用原raw/guard输入；只读两份清洗接收HDF5，未读取源IQ或追加source推理。
预登记固定五档、FP32/真实Mamba2、batch128、1800秒、8GiB主机/4GiB CUDA/128MiB输出预算。

严格权重及模型源码SHA、原验证划分重建、成员/质量/RMS/旧输入SHA通过；五路singleton/batch数值门通过。
原raw/guard全部logits逐值重现；独立复数减法构造三个中间候选的输入SHA与推理输入完全一致。
独立区间掩码核对13组×5路计数及纠正/回退数；按统一相消前条件估计SINR统计，不以源SNR分层。
102.61秒完成12480次预测，RSS1,535,119,360字节、CUDA395,857,920字节；三项预算内。

| 相消强度 | 总体正确/2496 | raw SINR<0正确/1159 | raw SINR[5,10)正确/925 |
| --- | ---: | ---: | ---: |
| 0% | 1286 | 276 | 711 |
| 25% | 1369 | 272 | 777 |
| 50% | 1359 | 233 | 801 |
| 75% | 1364 | 215 | 823 |
| 100% | 1364 | 207 | 832 |

25%比完全相消总体仅多5条，不能认定为普适最优；低SINR减轻退化但未超过不相消，
且[5,10)损失55条，显示强度存在权衡。固定比例尚非已验证优化，不据此次诊断部署0dB门限。
SINR仍是条件估计，使用接收可观测量设计规则前须另查估计依赖；这批反复分析的成员不是独立验收集。

同时重用封存保护区指标，按source_row/validation_rank/truth映射到D10，CSV SHA及2496 SINR核对通过。
低SINR下raw正确→guard错误134条、两路均正确142条；相位曲率绝对值中位0.069032/0.069731 rad。
同类别/同raw SINR格比较18格，仅覆盖68/134回退及71/142保持正确，共217对；格等权的回退指标更大比例：
相位曲率0.5315、guard误差余量0.3980、留出残差/单音功率0.4876，方向不支持统一“误差更大即回退”。
剩余成员没有匹配对照；共享采集上下文相关，结果仅描述性，不作独立显著性或因果结论。
历史保护区仍显示相位时变，但不能据此确认它解释分类退化，也不能认定加密保护区能修复问题。

本轮没有必要立即改传输。下一候选若继续，应先在剩余共同验证成员上检验预先固定的25%强度，
并核对接收端可计算的泄漏占比/估计可信度；剩余成员也已用于基线分析，不能改称独立locked-test。
如果后续证据指向跟踪误差，再登记短帧/局部零保护区有限A/B，用户允许重设计不等于无界持续发射。
未训练、未切换生产服务，recognizer_available=false。

[五档相消图](/var/tmp/sdrharness-dev/rml2018a-d10-partial-20260926/partial-cancellation.png) ·
[SINR表](/var/tmp/sdrharness-dev/rml2018a-d10-partial-20260926/sinr-table.csv)

推理和核验进程均正常退出；删376缓存25,711,688字节，cache目录不存在。
保留17文件1,663,640字节，无IQ副本；[路径/SHA/血缘/人工删除审计](../evidence/RML2018A_D10_PARTIAL_2026-09-26.json)。

## 固定25%相消的剩余共同成员验证（2026-09-26）

用户继续上一轮预先固定25%强度的后续检验。本轮主统计排除此前2496诊断成员，
使用367001剩余共同seed42 validation成员；另列prior2496与all369497用于数值/总体核对。
此批成员已有raw/guard基线被分析，且同源采集共享上下文，不声称独立locked-test或独立RF场景验证。
alpha固定0.25，全程未调参，不推理source，不训练或重新收发，未更改生产处理。

### 幅度恢复与执行核验

清洗HDF5的两路独立RMS不能直接混合。按source_h5_index/source_block_index/source_row从
26份历史processed.h5的quality_json提取received_power_adc_squared，平方根恢复接收RMS，
用normalize(0.75×raw×raw_rms+0.25×guard×guard_rms)构造候选。严格核对原验证成员、
标签、usable/strict_quality_pass及原SINR；26份文件按封存大小/mtime核对，选中元数据流另计SHA，
不宣称本轮重哈希全部大HDF5。全部输入仅来自已登记旧语料，无IQ副本。
旧2496成员RMS最大误差raw=0，guard=7.05446e−7，在预登记2e−6容差内。
D10相同SHA权重、source_03fa833、真实Mamba2、FP32/TF32关闭、batch128，严格加载与划分重建通过。

共369497次候选预测，包含2496数值控制；646.26秒完成，峰值RSS1,421,262,848字节、
CUDA394,199,040字节，均在4小时/8GiB主机/4GiB CUDA/256MiB输出预算内。
48条singleton/batch最大logits差2.26498e−5，完整旧2496候选logits最大差7.77245e−5，
满足原2e−4绝对/相对容差且top1全部一致；不能称逐值重现（功率恢复存在微小数值差）。
主/旧/全集三个集合明确划分，SHA核对已有两路基线；独立区间掩码与有符号正确性差核验39组统计，
逐行source/rank/truth与logits argmax核验通过，图已查看。

### 主结果：排除旧2496条

| raw参考条件估计SINR | 样本数 | 不相消 | 25%相消 | 完全相消 |
| --- | ---: | ---: | ---: | ---: |
| 总体 | 367001 | 50.44% | 53.92% | 55.10% |
| <0 dB合计 | 175081 | 23.39% | 22.81% | 18.89% |
| <−20 | 30636 | 4.39% | 4.16% | 4.24% |
| [−20,−15) | 35525 | 6.78% | 6.04% | 4.42% |
| [−15,−10) | 39539 | 16.97% | 15.99% | 8.89% |
| [−10,−5) | 33492 | 34.68% | 32.57% | 25.41% |
| [−5,0) | 35889 | 52.56% | 53.75% | 50.65% |
| [0,5) | 43370 | 67.94% | 75.58% | 80.71% |
| [5,10) | 133090 | 76.66% | 84.58% | 91.31% |
| [10,15) | 15460 | 81.95% | 81.63% | 81.52% |

其余高SINR/invalid格均0行，保留在CSV。总体正确185107/197896/202202；
25%相对raw纠正24023、回退11234，净+12789（+3.48百分点）；
相对完全相消纠正22617、回退26923，净−4306（−1.17百分点）。
低SINR正确40943/39937/33070：25%比完全相消多6867，但比raw少1006。
[5,10)则比完全相消少8964，故固定25%不采用为统一替代。
小样本总体多对5条的优势没有保持；可确认强度权衡存在，不能据此将SINR门限设为0dB。

### 接收端可用性与结论

只读核对PayloadBatch.quality：条件SINR明确依赖reference IQ及source_snr_db；
其中接收功率yp=mean(abs(received)**2)自身不依赖参考，可用于本轮幅度恢复。
因此图上的SINR适合工程离线分层，不能直接成为未知信号在线相消开关。
本轮收束固定25%候选，不继续扫更多固定比例；若用户继续优化，下一单元应先检验
仅依赖接收IQ/保护区的可观测指标能否预测相消得失，预先划分设计/评估成员并保留采集相关性限制。
尚未证明重设计帧能改善识别；无新传输计划或RF执行，recognizer_available=false。

[剩余成员图](/var/tmp/sdrharness-dev/rml2018a-d10-alpha25-common-20260926/remaining-comparison.png) ·
[三集合分层表](/var/tmp/sdrharness-dev/rml2018a-d10-alpha25-common-20260926/sinr-table.csv)

进程正常退出；删除376缓存文件25,748,886字节，cache目录不存在。
保留16文件58,681,330字节（候选预测、成员/RMS、来源、统计图及脚本），
[精确路径/SHA/人工删除审计](../evidence/RML2018A_D10_ALPHA25_COMMON_2026-09-26.json)。

## 接收端指标选择相消强度（2026-09-26）

用户继续接收端可观测指标检验。本单元只读既有指标/预测/接收元数据，无IQ读取、新模型推理、
训练、RF或生产部署。规则设计使用设计组真实标签比较正确数；应用规则只输入接收端指标，
不输入类别、源波形、源SNR、条件SINR或模型置信度。此为事后探索性处理规则校准，不是训练识别网络。

### 预登记与执行

对2496旧诊断成员的13个raw.sigmf-data精确路径排序，偶数序号7份/1344条作为设计，
奇数序号6份/1152条作为评估。整份采集不跨组，避免相邻窗口共享ADC跨组；路径清单在plan.json。
所有成员此前已分析，同一campaign且采集/条件/时间相关，不能称新独立测试或跨日验证。
预先指定主指标guard/raw RMS比，另外完整报告相位曲率、guard误差余量、留出残差/单音功率、
载荷LO窄带功率/raw功率四项，禁止根据评估成绩换主指标。窄带功率可能含有用信号/噪声，不称纯泄漏功率。
每项边界仅取设计组四分位，格内按设计正确数从raw/25%/full选动作；并列依次raw、25%、full。
指标数值/母记录SHA和逐行source/rank/truth核对；源SINR仅为已有溯源，统计仍按raw条件估计SINR。

首次脚本因系统NumPy不支持quantile(method=...)，在第一个分位计算即退出，未产生规则或评估结果。
保留失败plan/脚本/错误说明；a2改用等价interpolation='linear'，不更改划分、指标或选择逻辑。
五条规则均保存，独立逐行标量分箱/设计计数/动作复算以及26组独立SINR区间核验通过。

### 冻结的主规则与诊断评估

设计组主指标四分位0.8613944459655973、0.9227873698928544、0.9424934427251396，
动作依次25%、25%、full、full，因此精确等价为：

- guard_RMS/raw_RMS < 0.9227873698928544：25%相消，然后按窗独立RMS；
- 否则：完全相消，然后按窗独立RMS。

诊断评估1152条，raw/25%/full/主规则正确545/564/552/586，即47.31/48.96/47.92/50.87%。
其他四指标规则评估正确552/559/556/576，均完整报告，不因成绩删选。
主规则低SINR583条正确107，full74、raw117；改善相对full但未超过raw。
该RMS比表示相消导致的幅度变化，不等同真实泄漏功率占比或SINR。
现有guard处理需缓冲保护区上下文，接收端可计算不等同本轮验证了实时延迟/上线能力。

### 固定规则的大样本复核

主规则不改变阈值，先保存extension-plan再关联已有全体raw/25%/full预测和接收RMS。
主统计只选6份评估采集的剩余172218条，明确排除全部旧2496；
设计采集剩余194783及全体剩余367001另列，不混作主评估。
大样本每一行均以独立标量规则验证选择，39分层含invalid；动作数、净收益/纠正回退守恒检查通过。

| 范围 | 行数 | raw | 固定25% | full | RMS规则 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 评估采集剩余（主） | 172218 | 45.98% | 48.62% | 49.11% | 50.70% |
| 主组raw SINR<0 | 87885 | 19.05% | 17.60% | 13.54% | 17.58% |
| 设计采集剩余（辅） | 194783 | 54.38% | 58.61% | 60.39% | 61.68% |
| 全体剩余（辅） | 367001 | 50.44% | 53.92% | 55.10% | 56.53% |

主组full正确84575→规则87321，净+2746（+1.59449百分点），纠正9180/回退6434。
主组低SINRfull11903→规则15452，仍低于raw16744。全体剩余辅组净+5253（+1.43133百分点）。
六份主评估采集按登记路径顺序的净变化为−44、−112、−653、+2068、+1503、−16；
四份退化、两份贡献收益，不能用总体正增益声称各条件稳定提升。
采集路径名字的源SNR文字仅溯源，未作为决策或统计分层输入。

### 结论与边界

找到一个仅用接收指标且总体有正收益的候选，超过固定25%方案；并未解决低SINR相对raw的全部退化。
阈值来源是本campaign设计组，不是物理常数；采集条件相关性、重复使用旧语料、四份采集退化仍限制结论。
不继续在同组调阈值或直接部署。下一有效验证应固定此规则，在新采集会话中随机交错多个接收条件，
同时保存raw/full/候选并核对输入重现，检验收益是否跨条件保持；目前未登记/执行新的硬件计划。
允许必要时重设计传输的用户意图保留，但本轮没有证据要求立即改帧。recognizer_available=false。

[剩余成员图](/var/tmp/sdrharness-dev/rml2018a-d10-rx-selector-20260926-a2/receiver-routing-extension.png) ·
[冻结规则](/var/tmp/sdrharness-dev/rml2018a-d10-rx-selector-20260926-a2/rules.json)

所有本单元进程退出。删1缓存123,432字节，cache目录不存在；失败与成功两根合计
保留19文件5,973,464字节，无IQ副本；[路径/SHA/人工删除审计](../evidence/RML2018A_D10_RX_SELECTOR_2026-09-26.json)。


## 新会话交错试验映射准备2026-09-26

本单元完成收发前置的源行映射和离线波形排程，未执行新RF或模型推理。
现有continuous-pilot把2048窗口固定映射为连续OOK源行，直接替换跨类别波形会导致标签错误。
现有SnrStore新增可选显式source_rows，配置哈希固定观察顺序；重复波形保留各观察身份，
失败窗口仍写入对应源行。拒绝非法类型/长度/范围/源Z和越界块，显式试验不能封成完整96块SNR。
默认连续全SNR路径保持原合同，未改历史证据。

新排程只取原seed42验证索引：24类各28个源Z30波形，共672个不同源行；
每16行一组，同组按随机次序连续发三个帧，载荷相对功率0/−10/−20 dB，合计2016观察。
随机种子20260926，另32观察作尾部控制，预先排除于主统计，质量失败不得隐去。
导频、前后256零点和16窗帧结构不变；幅度缩放在逐窗峰值归一化后进行。
完整波形2,293,760复样本、18,350,080字节、2.1MS/s下1.092266667秒，
这里只在RAM合成并记录哈希，没有启动TX，也没有保存IQ副本。

实际NPZ哈希及集合互斥检查通过；672源成员全部属validation，train/test选中0；
直接读取原HDF5逐行核对Y/Z通过，每个幅度档24类各28观察。
RMS规则父文件哈希与0.9227873698928544阈值已记录，后续不得据新结果调阈值。
主对比为raw/固定25%/full/固定规则，相消前后采用同成员raw参考条件估计SINR；
源Z与幅度档是溯源/实验条件，不能冒充接收SINR。新会话复用源成员，不称独立locked-test。

验证：存储10项、stream/SNR回归17项、排程3项，共30项通过。
包括重复源行跨块重开、失败记录及映射篡改拒绝、三档同波形配对/类别均衡、
导频和零点逐值不变、载荷幅度及峰值/字节数核验。测试内模拟停止日志不代表实机操作。
测试进程均退出，TemporaryDirectory自动移除产物，空scratch目录移除并核对不存在。
保留1文件81,428字节，0份IQ副本；[审计/哈希/人工删除](../evidence/RML2018A_INTERLEAVED_MAPPING_2026-09-26.json)。

尚未完成：排程接入continuous-pilot运行入口、实际运行软件/波形身份固定、
新鲜设备全状态/空间/前后背景的有限执行计划、RF采集及恢复后D10推理。
本次不能判断新会话收益，recognizer_available=false，未部署。


## 自生成1024点可控相消对照2026-09-26

用户提出自生成不同调制的1024点信号并明确继续；本单元先完成离线生成、D10推理和独立读回复核，
不发射、不训练、不访问RadioML原始IQ，不追加旧实测source曲线。之前交错RF排程仍保留未执行。

生成BPSK/QPSK/16QAM/AM-DSB-WC/AM-DSB-SC各64个独立波形。先生成4096点，再取中心
1536:2560的1024点；数字调制8点/符号、RRC滚降0.35、跨度12符号；AM使用50kHz截止的
257抽头Hamming低通高斯消息，WC调制度0.8；随机载波相位、无频偏。采样率2.1MS/s。
固定参数用于初次诊断，没有根据识别率搜索生成参数。

逐波形加入复高斯噪声SNR−10/0/10/20dB及+250kHz单音SIR−10/0/10dB，3840配对观察；
同波形同噪声档跨SIR复用噪声，未把重复条件当成独立样本。
五路为raw、精确减去已知单音oracle_full、512点前后保护区已知频率LS估计guard_full、
同估计25%相消及固定RMS规则。每一路相消后独立RMS；oracle逐值核验保留原信号+原噪声。
简化guard拟合不是现有生产相消算法；oracle使用生成真值，仅为归因上限对照，不能上线。
RMS阈值保持0.9227873698928544不调参。

总20,800预测（五路各3840、干净320、仅噪声1280），严格加载指定D10/真实Mamba2、FP32、
TF32关闭、batch128；七路singleton/batch数值及top1门通过。耗时98.32秒，GPU峰值348,388,864字节。
2项生成/能量/频谱/精确相消测试通过，7输入哈希重生成、3840行血缘及13组统计独立读回通过。
出现PyTorch TF32旧API弃用提示，无运行失败。

### 干净输入先暴露生成分布问题

| 调制 | 干净正确/64 | 主要预测 |
| --- | ---: | --- |
| BPSK | 0 | AM-DSB-SC 41、QPSK 14 |
| QPSK | 63 | QPSK 63 |
| 16QAM | 0 | 256QAM 57 |
| AM-DSB-WC | 0 | 8ASK 44、AM-SSB-WC 19 |
| AM-DSB-SC | 0 | 8ASK 63 |

构造公式及基础波形检查通过，但四类干净输入识别失败，不能把此混合总体成绩当成公平的五类精度基准，
也不能据此认定硬件故障。需要先审计与训练分布的符号率、成形、消息和幅度等差异，不能用调参后结果
冒充独立验证。BPSK仅噪声−10/0/10/20dB正确63/64/34/1，亦显示模型响应并非随信号质量单调变化。

### 精确相消仍存在局部回退，但低SINR总体未复现退化

| 范围 | 观察数 | raw | oracle_full | guard_full | guard25 | 固定规则 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 全部（正确数） | 3840 | 1147 | 1113 | 1110 | 1173 | 1116 |
| raw合成SINR<0（正确数） | 3197 | 845 | 941 | 939 | 943 | 945 |

oracle相对raw纠正285/回退319，全部319回退的波形误差均下降；证明在本合成设置中，
“误差改善但判错”无需干扰估计错误也能出现，不证明实测低SINR的根因或训练LO捷径。
低SINR主子集oracle净+96，**没有复现低SINR总体下降**；其中仍有186回退/282纠正。
QPSK同768观察raw362→oracle528，BPSK702→486，类别混合强烈影响总体，不能推广为普遍退化。
波形误差按相消后、归一化前与干净信号差值计，模型输入另作单位RMS；未分离所有尺度影响。

分层统一采用相消前同一窗口的已知分量合成SINR=10log10(Pclean/P(noise+tone))，包含有限窗交叉项；
它既不是源SNR，也不是实测条件估计SINR。极窄[0,5)区间仅3观察，图中标n，不据此推断趋势。
干净输入图仅属于独立合成诊断，不改写实测图。见[本轮图](/var/tmp/sdrharness-dev/rml2018a-d10-synthetic-20260926/synthetic-comparison.png)。

下一有效单元先审计生成参数/模型输入分布，QPSK可作已有干净基线控制；暂不直接把五类波形用于
RF精度比较，也不修改固定相消规则。recognizer_available=false。
全部进程已退出，删376缓存25,821,384字节和空测试目录，保留11文件3,738,513字节，0份IQ副本。
[脚本/参数/权重/输入哈希、原始logits、图表及人工删除](../evidence/RML2018A_D10_SYNTHETIC_2026-09-26.json)。


## 自生成信号符号率与相位对照2026-09-26

用户继续后，先核对D10源码H5IQDataset._process_sample：转置到[2,T]并转float32，
不额外RMS归一化；配置关闭去噪、IQ增强等。标签server-v1的3/4/12/19/20映射一致。
只读旧2496诊断metadata中五类Z30各4行，原source RMS范围0.938685～1.043137，
因此未发现单位RMS与这20行幅度严重不匹配；样本量有限，不外推全库，也未访问原始IQ。
训练读取器不包含原始RadioML波形生成参数，不能从它认定原数据集符号率或消息分布。

预登记固定sps2/4/8/16与随机/零载波相位，数字三类每格64；AM沿用原消息、只比较两相位，
共1792输入。RRC滚降0.35、跨度12符号、单位复RMS，无噪声或干扰，不按结果选“最佳配置”。
低sps需更多符号以保留4096点长波形中心截取；同seed跨sps并非完全相同符号片段，
因此是参数组敏感性对照，不是只变一个采样时钟而其他样本逐值相同的因果干预。

| 调制 | sps2 随机/零相位 | sps4 | sps8 | sps16 |
| --- | ---: | ---: | ---: | ---: |
| BPSK（每格64） | 64/64 | 0/0 | 0/0 | 3/0 |
| QPSK（每格64） | 0/0 | 0/0 | 63/61 | 0/0 |
| 16QAM（每格64） | 4/1 | 0/0 | 0/0 | 0/0 |

表中斜线分隔两种相位的正确数，不是正确数/总数；AM两类随机及零相位均0/64。
BPSK低sps能够识别、QPSK只在sps8表现较好，支持符号率/带宽/窗内符号数量相关输入结构敏感。
零相位未挽救大部分失败，所以固定载波相位不是充分修复。不能据此声称原始数据集每类的真实sps、
训练捷径或原实测低SINR根因已经确定；16QAM和AM未解决，不部署不同类别专用预处理。
用这些成绩筛选出的配置后续只算探索设计，不可称独立验证。

5项生成基线逐值检查及20组相位包络/功率检查通过；严格加载同D10/FP32/真实Mamba2，
28条单样本/批次数值最大差5.72205e−5，实际batch128门通过。
上一轮320个干净输入logits逐值一致（最大差0、top1一致），排除两轮加载/执行不一致。
1792输入哈希全部重生成，28组正确数/预测类别分布独立读回通过；GPU峰值348,388,864字节。
只有TF32旧API弃用提示，无执行失败；无RF、训练或生产变更，recognizer_available=false。

下一有效单元需进一步核对数字调制的符号/匹配滤波恢复及原数据集生成参数证据，再设计独立种子验证；
AM消息和载波定义另查，不以不断试准确率替代物理波形验证。旧交错RF计划仍未执行。
进程退出，删375缓存25,592,442字节，保留7文件484,115字节，零IQ副本；
[逐行参数/完整结果/哈希/人工删除](../evidence/RML2018A_D10_SYNTHETIC_PARAMETERS_2026-09-26.json)。


## 自生成波形物理核验与生成依据2026-09-26

本单元独立于模型验证既有自生成波形，不新增推理、RF或训练。继承固定seed及sps2/4/8/16，
数字三类各64窗/档，共768窗；AM两类各64窗。先登记门：数字误符号0、EVM<3%，
AM消息重建及边带对称误差<1e−12。父证据全部哈希复核，不修改封存脚本或结果。

数字接收器用另一种等价解析式计算RRC匹配滤波器，核对发端抽头和生成波形；
使用生成真值的相位、幅度和符号时刻恢复。1024点窗两侧各剔除接收滤波器半长，
避免边缘卷积缺少上下文；比较原已知符号并按合法星座最近点判决。
768窗共175104有效符号，误符号0，最差EVM0.49534%。包括上一轮模型不能识别的16QAM。
这是已知同步条件的物理构造检查，不是盲同步、实机解调或模型准确率证明。

AM用已知相位/幅度恢复实消息；128窗最大消息误差1.112e−15、边带对称误差6.317e−16，
WC完整稳态区包络最小0.2、调制度峰值0.8，没有过调；SC构造不含独立载波项。
SC单1024点窗的消息均值绝对值最大0.088623，是有限窗消息均值，不据此判成硬件LO泄漏。
全部物理门及逐窗计数/误差独立汇总通过；无IQ副本，保存示例符号坐标和
[匹配滤波星座图](/var/tmp/sdrharness-dev/rml2018a-synthetic-physical-20260926/matched-symbols.png)。

资料核对：

- [DeepSig官方数据集页面](https://www.deepsig.ai/datasets/)将2018.01A与2016.10A分列，
  2018说明24类、1024点并指向下述论文；GNU Radio公开生成仓库链接位于2016说明下，
  不能直接当成本地2018的精确生成配置。官方也提示这些历史数据存在已知勘误，但本轮未确定具体影响。
- [O’Shea等，Over the Air Deep Learning Based Radio Signal Classification](https://arxiv.org/abs/1712.04578)，
  PDF第3页Table I及Section III：数字RRC成形，滚降U(0.1,0.4)、时间偏移U(0,16)、
  载波相位U(0,2π)，时钟/频偏及多径随场景设置。我们固定滚降0.35处于该范围内，但
  固定符号位置、缺少信道扰动并不复现其整体生成分布。
- 以上读取尚不能确定本地HDF5每类的标称sps、AM消息分布/调制度或精确生成版本，
  不能从BPSK2/QPSK8的模型成绩倒推这些真值。旧仓库master/README.md访问404，失败保留，未套用2016默认值。

结论：当前没有证据把失败归为“生成了错误调制”，训练分布匹配和模型泛化仍待查；
原实测低SINR退化根因仍未确定。下一单元应预登记统一的随机时间偏移/滚降生成分布及新seed，
先物理核验再观察模型，报告全条件；不按类别选择最高识别率配置，也不将探索结果当独立准入。
未改生产配置，recognizer_available=false。

所有进程退出；删除论文PDF/全文临时文件及绘图缓存共3文件1,789,402字节，
保留9文件274,525字节（含来源摘录、PDF哈希及失败状态），临时路径不存在；
[路径/SHA/人工删除审计](../evidence/RML2018A_SYNTHETIC_PHYSICAL_2026-09-26.json)。


## 新种子随机滚降与时间偏移对照2026-09-26

预登记新种子统一四组：fixed、仅rolloff、仅timing、both。数字三类共享sps2/4/8/16，
每格64，共3072输入；两种AM各64新种子原参数控制，总3200次D10预测。
同类/sps/rep四组使用完全相同符号和载波相位，各数字类别共享同一组随机干扰参数抽样。
滚降U[0.1,0.4)，时间偏移U[0,16)按样本单位实现为整数裁剪偏移+分数延迟脉冲，
该单位约定是本实验实现，不宣称是原RadioML精确实现；未加入频偏、多径或噪声。
不按类别挑选最佳sps，不更新相消阈值，不训练；新seed独立于前两轮，但仍是诊断实验而非locked-test。

先做物理门再加载模型：登记零误符号及EVM<12%（随机低滚降+有限12符号跨度滤波器本轮合同，
不更改上一轮3%门或封存结果）。3072窗恢复700416符号零错，最差EVM6.91504%，全部进入推理，
没有按物理误差或识别成绩筛掉样本。接收使用已知同步/相位/增益，不代表盲接收能力。
2项测试覆盖可去奇点、分数脉冲能量中心、跨类参数共享、四组配对和符号恢复；
另4抽头恒等及12配对检查通过。

| 调制 | 点/符号 | 固定 | 仅滚降随机 | 仅时间随机 | 两者随机 |
| --- | ---: | ---: | ---: | ---: | ---: |
| BPSK | 2 | 64 | 64 | 64 | 64 |
| BPSK | 4 | 0 | 0 | 0 | 0 |
| BPSK | 8 | 0 | 1 | 2 | 2 |
| BPSK | 16 | 2 | 2 | 2 | 2 |
| QPSK | 2 | 1 | 1 | 2 | 3 |
| QPSK | 4 | 0 | 0 | 0 | 0 |
| QPSK | 8 | 62 | 63 | 63 | 64 |
| QPSK | 16 | 0 | 0 | 0 | 0 |
| 16QAM | 2 | 2 | 0 | 2 | 1 |
| 16QAM | 4 | 0 | 0 | 0 | 0 |
| 16QAM | 8 | 0 | 0 | 0 | 0 |
| 16QAM | 16 | 0 | 0 | 0 | 0 |

表内每格为正确数/64中的正确数。AM-DSB-WC和AM-DSB-SC原参数新seed各0/64，
AM没有做数字脉冲滚降干预，不能用它评判随机RRC效果。
完整50组预测分布及48组相对fixed纠正/回退均保留。

结果：BPSK2点/符号稳定64/64，QPSK8点/符号62～64/64；其余大多数配置失败，
16QAM没有实质改善。增加这两种随机性不足以修复合成输入识别；不能据此锁定原数据集sps、
训练捷径或原实测低SINR相消根因。新seed验证的是此前现象的可重复性，不证明最佳参数泛化。
应暂停继续靠准确率试生成参数；下一有效诊断是固定同一合成输入，用一份已有冻结参考模型作
交叉检查，区分D10特有响应与共同的数据分布差异，仍不训练、不改生产、不扩大为全模型重跑。

同指定D10严格加载、FP32/真实Mamba2、batch128，50条singleton/批次数值门通过，
最大差5.817413330078125e-05；GPU峰值397910016字节。3200输入重生成SHA、50组统计及48组配对独立读回通过。
TF32旧API弃用提示保留为已知运行提示，无失败；无RF/训练/部署，recognizer_available=false。
进程退出，删375缓存25,639,580字节，保留8文件1,343,252字节，零IQ副本；
[完整参数/逐行物理证据/原始logits及删除审计](../evidence/RML2018A_SYNTHETIC_RANDOMIZED_2026-09-26.json)。


## 同合成输入CNN2冻结参考对照2026-09-26

按当前选择只增加一份已有冻结CNN2-stable seed42参考，不重跑D10或全模型。
选择依据是传统卷积结构与D10不同，推理前登记，非事后挑选表现较好的模型。
上一轮3200合成输入由相同生成器/seed重建，完整tensor SHA、逐行truth及父级证据哈希一致；
不追加变换，保持与D10相同单位复RMS，不访问RadioML原始IQ。
参考权重be0a2705ef720e577491b99b9d574308e97ddd6f0c2c9b9f812dc6d123b5fe4e，
导入manifest中源码/配置/本模型文件逐项核对，server-v1类序一致；严格加载859805参数、
pytorch_cnn2_stable，无回退。FP32/TF32关闭，batch128，50条singleton及实际batch门通过，
最大差7.62939e−5，GPU峰值685148160字节。

| 同成员范围 | 观察数 | D10正确 | CNN2正确 |
| --- | ---: | ---: | ---: |
| BPSK | 1024 | 269 | 258 |
| QPSK | 1024 | 259 | 232 |
| 16QAM | 1024 | 5 | 198 |
| AM-DSB-WC | 64 | 0 | 0 |
| AM-DSB-SC | 64 | 0 | 0 |
| 全部 | 3200 | 533 | 688 |

总体两者都对375、都错2354；仅CNN2对313、仅D10对158；top1相同628。
共同判错不代表判成同一类，也不证明共同错误具有相同原因。
16QAM的sps16四组CNN2正确47/35/48/37（每组64），D10四组均0；
sps4两个模型四组均0。保留完整50组类别分布，不只展示较好档位。
四种生成条件共享底层符号，不能将1024观察视为1024个完全独立波形作显著性推断。

结论：这批16QAM存在明显模型差异，D10并非对所有正确生成的16QAM都能泛化；
AM失败同时出现在两模型，全部问题不能只归为D10特有。
同组合成条件的总正确数不是CNN2优于D10的通用基准，亦不确定训练分布或原实测低SINR根因。
停止扩大全模型/生成参数搜索。后续可用已具干净识别基线的固定QPSK8点/符号、新seed，
开展小规模噪声/已知单音配对相消，对比这两份冻结模型；它只验证单类机制，不作为多类精度成绩。
未部署，recognizer_available=false，无RF/训练。

原始logits独立读回，重生成3200输入SHA、truth、56汇总及配对计数守恒全部通过。
运行只有既知TF32弃用与same-padding内部复制提示，没有错误。新增执行进程已退出；
专属缓存为空（删除0文件0字节及其空目录），目录确认不存在。
保留6文件398028字节，零IQ副本；[身份/结果/清理及人工删除](../evidence/RML2018A_SYNTHETIC_CNN2_REFERENCE_2026-09-26.json)。


## 固定QPSK两模型配对相消2026-09-26

停止多类生成参数搜索后，固定QPSK8点/符号、RRC滚降0.35/跨度12、随机相位，
64个新seed，1024点中心截取；全部保留，不按干净识别结果筛选。
已知同步的物理检查7424有效符号零错，最差EVM0.24021%。
SNR−10/0/10/20与+250kHz单音SIR−10/0/10组合成768观察，同波形同噪声档跨SIR复用噪声。
处理为raw、oracle精确减单音、512点保护区已知频率LS全相消、25%相消及旧冻结RMS规则，
每路处理后独立单位复RMS；guard仍是简化估计器，不冒充实机算法。两模型各3904、合计7808预测。

D10与CNN2分别独立进程严格加载原冻结权重/真实后端，FP32/TF32关闭、batch128。
每模型六路各12条singleton及实际batch数值门通过；2项既有生成/能量/精确相消回归通过。
两模型重生成6路输入SHA一致、逐行768记录一致；16组配对统计及跨SIR同噪声oracle logits
独立复核通过。没有修改模型、规则阈值、原实测成员或生产配置。

| 模型及范围 | 观察数 | raw | 精确全相消 | 估计全相消 | 估计25% | 固定RMS规则 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| D10全部 | 768 | 360 | 513 | 516 | 358 | 353 |
| D10 raw合成SINR<0 | 640 | 232 | 386 | 389 | 230 | 226 |
| CNN2全部 | 768 | 320 | 522 | 522 | 347 | 360 |
| CNN2 raw合成SINR<0 | 640 | 224 | 415 | 415 | 249 | 253 |

表内为正确数。干净基线D10 62/64、CNN2 44/64，不宣称两模型干净基线均已完善。
精确全相消相对raw：D10纠正176/回退23，CNN2纠正232/回退30；这些回退波形误差均下降。
低SINR子集精确相消D10纠正176/回退22，CNN2纠正209/回退18，均为总体净改善。
估计全相消与oracle的逐行top1各有12处不同；CNN2两路正确数相同不代表逐行预测一致。
旧RMS规则D10总体360→353、低SINR232→226，CNN2则改善，不能将该规则当通用保障或重新调阈值。

本轮没有复现原实测低SINR的总体退化，但复现了两种结构模型均可出现“波形更准、局部判错”。
这不足以认定训练LO捷径、相消误删载波或原实测根因；不同调制/生成分布/实际信道等差异仍存在。
768条件共享64底层波形，不作为768独立样本作显著性推断。分层采用同成员已知分量raw合成SINR，
不是源SNR，也不是实测条件估计SINR。单类设置出自前期诊断，结果不是独立多类性能基准。

本轮合成探索收束，不继续扩张扰动/模型/阈值搜索。下一有效单元回到已保存实测预测，
按调制类别×统一raw条件估计SINR做相消收益配对分解，核对总体退化是否集中于少数类别，
只读重统计，不新增模型或RF。recognizer_available=false。

两模型进程退出；删重复stdout及缓存共376文件25,570,924字节，缓存路径不存在；
保留11文件1,167,041字节，无IQ副本。只有既知TF32/same-padding提示，无失败。
[输入/权重血缘、原始logits、复核及人工删除](../evidence/RML2018A_QPSK_CANCELLATION_PAIR_2026-09-26.json)。


## 实测类别与raw条件SINR配对分解2026-09-26

只读已有369497共同seed42 validation成员的D10/CNN2 raw/guard预测及raw条件估计SINR，
父NPZ哈希、source_row/truth/validation_rank逐值关联、D10 logits argmax及server-v1标签核对通过。
不加载模型或IQ、不发射、不追加source/D8。统一使用相消前raw参考SINR，相同成员比较；
11档含invalid、每模型24类，保存528单元及全部低SINR回退目的类别，不将源SNR当接收SINR。
本成员集无无效SINR，仍保留空invalid单元。结果是已检查工程语料的描述性分解，不是独立准入。

低raw条件估计SINR<0共176240成员：D10正确41219→33277（23.388%→18.882%，−4.506百分点），
纠正10630/回退18572，净−7942；CNN2正确34163→34116（19.384%→19.358%，−0.0267百分点），
纠正15054/回退15101，净−47。CNN2接近持平不代表没有严重回退，而是不同类别相互抵消。

| 低SINR类别 | 成员数 | D10净正确变化 | CNN2净正确变化 |
| --- | ---: | ---: | ---: |
| FM | 7271 | −1457 | −238 |
| AM-SSB-SC | 7729 | −1410 | −2479 |
| BPSK | 7354 | −1340 | +441 |
| AM-DSB-WC | 7341 | −1281 | −751 |
| AM-SSB-WC | 7880 | −1094 | −1083 |
| QPSK | 7411 | −319 | −51 |
| 64APSK | 7282 | +218 | −2658 |
| 32APSK | 7247 | +1150 | +541 |

D10有16/24类净退化，负贡献合计−11445，8类正贡献+3503；上表前五类损失6582，
占负贡献总量57.51%，不能说问题仅局限这五类。CNN2有9类净退化，负贡献−8578，
其余正贡献+8531。两个模型退化类别存在明显差异，不能以统一相消收益或单一总准确率概括。
实测QPSK两模型低SINR均净退化，合成QPSK却总体改善，直接表明合成结果不能替代当前实测结论。
类别和采集会话、源波形条件、接收强度仍相关；本轮定位贡献来源，不声称识别了因果根因。

独立读回采用四状态混淆计数核验528单元及全部低SINR目的类别；各类/各档总和与总体
rows/raw/guard/纠正/回退/net守恒通过。[低SINR类别贡献图](/var/tmp/sdrharness-dev/rml2018a-receive-class-sinr-20260926/low-sinr-class-net.png)。
完整24类×11档CSV和回退去向CSV保留，不用挑选表格替代全量结果。
下一步在现有实测证据上交叉核对主要损失类别的采集文件/会话分布，区分类别效应与单次采集偏差；
保持只读、不训练、不调阈值，recognizer_available=false。
本单元进程退出，删1绘图缓存123432字节，保留8文件474808字节、无IQ副本；
[路径/哈希/人工删除](../evidence/RML2018A_RECEIVE_CLASS_SINR_2026-09-26.json)。
