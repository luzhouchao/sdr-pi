# RadioML2018A 全量脚本：有限先导与未通过的接收质量

[入口](../README.md) · [操作说明](../reference/RML2018A_FULL_RF_CAMPAIGN.md) ·
[保留与清理清单](../evidence/RML2018A_FULL_RF_CAMPAIGN_EVIDENCE_2026-09-10.json)

2026-09-10，分支`codex/sdr-improvements`，基线`02e5cdc`。用户明确要求全部原始
RadioML2018A样本，恢复此范围的冻结模型工程推理，训练/微调仍暂停。
交付边界是全量迭代脚本、有限空口先导、结果关联、取消/续跑和证据；**未执行全库，
接收识别质量未通过**。未部署生产Worker/profile，`recognizer_available=false`。

## 实现与源码检查

新增AGX campaign、NX有限文件TX helper、纯帧/同步合同及12项单元测试。
全库2,555,904行按24条/批覆盖106496批，每行保留原始ID、Y.argmax和Z，
不按类别/低SNR筛掉失败样本。全HDF5字节数、SHA-256及X/Y/Z形状均在plan中核对。
旧test成员只按本任务授权作工程源访问，不重写旧split或独立审计。

实际运行环境没有SciPy、NX没有h5py/Python UHD。实现改用NumPy FFT相关，
NX通过FIFO喂给已安装UHD程序，未新增系统依赖。
发射程序必须收到精确GO；payload哈希/幅度/设备/频率/有限字节不匹配时在UHD前拒绝。
推理每32批使用新的spawn进程，避免Torch线程池二次初始化并在分片后释放CUDA。

12项测试通过：全量/尾批覆盖、峰值与波形保真、篡改和越界拒绝、未知时刻/相位/CFO恢复、
错批/静音/截断拒绝、强带外tone下仅同步滤波且payload不变、尾部标记定位前一完整帧、
RMS保留恒定载波、FIFO有限实际字节流/EOF、GO前断连不送payload、汇总分母、已完成续跑
校验IQ且不重发。尾部标记新增测试先发现滤波边界影响CFO，改为半标记内部取样后通过。
最后命令为运行环境Python `-B -m unittest discover -s jetson-agx/sdrharness/tests
-p test_rml2018a_campaign.py`，TMPDIR为本单元开发scratch；没有Rust/前端修改。

## 真实参数与设备

NX hostname/user均为wheeltec；用户称为N210的设备UHD实际枚举B210，serial2508504，
USB3、A:A/channel0/FE-TX2、TX/RX。P201仅RX1/RX0/A_BALANCED。
沿用用户当前433MHz天线；天线产品型号及具体更换端未另作推定。

中心433.920MHz，TX/RX rate2.1MS/s、BW1.5MHz，TX70/RX50，峰值0.2、TX LO offset+250kHz。
每批TX最多4秒，完整24条帧重复321次，8381952个复数样本。
RX settle500ms、point deadline1000ms、65535个ci16复数样本，最大262140字节。
调用的batch数量、外层deadline见下表；每批audit保存执行前可用空间（约809GB）、
AGX/NX/P201精确路径、两路RX状态及直接停止方法。

P201 sdrd PID5909，实际哈希`83a661a892b8ab71de3f4e7d64064c9c65030623dc7245eb3d3a1420a696ba4f`；
Controller哈希`24b8340dd5c56bcf643a44e1eadbd11450e3b5e528d2ec72dc26103e9f23e25f`；
NX UHD文件TX程序哈希`fe3aebc556c16a5065d63d4e6ef8f02ef277ac01dcf250a35dec58b84eceb5cf`。
未修改已安装二进制、P201持久配置/启动链或生产TX能力。

## 先导结果及失败沿革

各根完整路径均为`/var/tmp/sdrharness-dev/b210-rml2018a-pilot-20260910{a,b,c}`，
原始run-plan、audit和哈希封存于原处，具体文件见库存。

| 根/操作 | batch与源行 | 调用范围/deadline | 结果 |
| --- | --- | --- | --- |
| a acquire | 4267，102408–102431 | 1批/180秒 | 原同步检测失败0.323595；采集及恢复通过；未推理 |
| b run | 同一批/源行，新run标记 | 1批/650秒 | 仅同步检测加固定低通，仍失败0.418432；源24/24正确，实收预测为空 |
| c run | 4267–4268，102408–102455 | 2批/650秒 | 48条全部同步并推理；源48/48正确，实收0/48正确 |
| c 中断acquire | 22016，528384–528407 | 1批/180秒；batch_plan后4.8秒SIGINT | 已TX start，中断退出1；未形成完整capture receipt；NX/P201恢复通过 |
| c retry/run | 22016，同一源行 | 1批/650秒，retry-failed | 旧失败整体移入attempts，新的request/session完成24条；源24/24正确，实收0/24正确 |
| c 重复run | 4267–4268 | 2批/180秒 | 4.171秒结束，9个原批次receipt/audit/prediction哈希不变，无新TX或推理事件 |

根a原始IQ中基带约−589.17kHz窄带分量占主导。根b还存在时变带内分量，早期标记受干扰，
最清晰标记在采集末尾，原实现因后面没有完整payload而排除它。
最终检测采用固定129-tap Hamming 500kHz低通，仅用于标记，不滤payload；从全窗口找标记，
必要时用已知26112点重复间隔取前一完整帧。CFO估计排除FIR边缘瞬态，未降低原质量门。
根a/b只作版本化离线同步复核，得分0.788831/0.692524，记录在c根
`offline-sync-recheck-v1.json`；没有把原始失败改写为成功，也未再推理这些旧RX。

最终c根三批标记得分为0.756716、0.741393、0.720123，估计CFO约657.50、514.35、594.76Hz。
前两批使用尾部标记定位前一完整payload，第三批标记后直接有完整payload。
三批源/实收平均相关度分别0.189643、0.165355、0.189147。
源类别为48条OOK(raw ID0)和24条QPSK(raw ID4)，均为原数据集Z=+30dB；
Z不是空口实收SNR。三批源72/72正确，实收72条全被判为ID17或18，**实收0/72正确**。
这只是2类高SNR小先导，不能外推全库正确率，也不能把同步成功等同于波形保真或准入成功。
−589kHz分量的物理来源仍未确认，不能因先前2.4/5GHz排查而把433MHz当前问题直接称为Wi-Fi。

UHD readback频率/rate/gain/BW/LO锁定通过；日志尾部有`S`，它不是时间戳化的连续发射证明。
实际marker及原生RX元数据提供批次关联，无法证明每次重复发射都无间断。
推理单次平均约37ms；首次新缓存加载/推理隔离窗口约67秒，后续单批窗口约15秒，
Spark都恢复且watchdog停止。没有在同一进程重复构造Torch backend。

## 预算与当前扩量边界

全量RX原生IQ 27,916,861,440字节，加13,958,643,712字节元数据/预测预留，共约39GiB；
累计TX上限425984秒（4.93天）。该TX时间不是整个实验耗时。
最终三批capture audit耗时11.88–12.45秒，尚不含前置检查；相邻批次实际开始相隔约15.38秒。
加上推理/模型分片开销，**全量约三周量级**，仅为小先导外推，未作全库长时验证，
代码初始10秒/批的字段明确标记unvalidated，不能当成已测承诺。

脚本可以按全量行范围执行、停止和批次续跑，但目前质量门不支持有意义的全量RF模型比较。
尚未执行2,555,904行全库，也未执行32批连续推理分片/多日soak；当前只验证了2批及1批真实分片。
下一步应先定位带内/带外分量与接收失真、用有限样本确认波形保真改善，再扩量。
不通过改模型、放宽同步门或把失败样本排除出分母获得成功。

## 停止、恢复与精确清理

正常、失败和主动中断均核对唯一接收所有者、同一sdrd哈希/PID、LO、rate、BW、
两路RX增益模式/增益/端口、全部scan mask和buffer恢复。六次尝试的NX/P201瞬时目录
最后再次核对不存在，NX无TX进程/USB持有者，P201 healthy=true，Spark状态S。
最终只读现场检查在c根`postflight.json`。

NX共删除42个暂存文件、1,347,331字节，每批audit有逐文件清单；P201目录已不存在。
AGX最终清理708个cache/lock/冗余packet文件，共40,492,354字节，三个scratch和本单元dev根已移除。
正常批次的208896字节TX源包由runner随批删除，不另存源IQ。
测试TemporaryDirectory自行清理，早期测试瞬时字节未逐项记账，因此不把最终清理数字说成
涵盖所有历史测试临时写入的精确总数。

保留74个证据文件、1,580,090字节，包括原失败/成功/取消的IQ或占位文件、原生元数据、
版本化离线复核、模型预测和恢复/续跑凭据；无权重/原始源IQ副本入Git。
每文件路径、大小、SHA-256、run/source/request/session、模型/profile/preprocess身份与
人工删除根见库存。清理完成不意味着这些必要证据被删除。

## 2026-09-10：按用户要求复查2.4GHz历史识别方法

本次只读历史文档/源码及已保留IQ，**新增RF采集0、模型推理0**。上一节0/72及旧审计保持。
基线8c51c0d；[离线复核脚本](../../jetson-agx/sdrharness/scripts/diagnose-rml2018a-campaign-filter.py)
输出[版本化逐行统计](../evidence/RML2018A_HISTORICAL_FILTER_COMPARISON_2026-09-10.json)。

先前“波形失真”描述过于笼统：现已量化一个主要问题是**额外窄带分量进入模型输入**。
三份当前实收Hann频谱的最大峰均在基带−589.160kHz，该峰±2kHz占各自全段谱功率
81.92%–83.72%。这是基带频率和窗内谱功率比例，不是Wi-Fi识别、绝对dBm或已测SNR。
分量物理来源仍未定位，也不能据此认定天线故障或ADC削顶。

历史不是“2.4GHz所有样本都成功”：

- [2440MHz早期保真度实验](B210_RX_FIDELITY_VALIDATION_2026-09-06.md)中，源ID0、
  实收ID18；仅CFO/带限均未修复。后来通过[TX LO正负偏移对照](B210_LO_OFFSET_VALIDATION_2026-09-06.md)
  证明当时有随TX LO移动的额外载波，符合LO泄漏/载波馈通。
- [固定泄漏抑制](B210_LO_REJECTION_VALIDATION_2026-09-07.md)使用TX LO+250kHz，
  AGX再用257-tap、175kHz cutoff、Kaiser beta8 FIR滤模型输入，保留DC、丢弃边缘halo；
  该次来源相关显著改善，但背景门失败，模型没有运行。
- [2455MHz 24类×3样本](B210_MULTICLASS_VALIDATION_2026-09-07.md)实际源69/72正确、
  原始实收30/72、固定FIR实收56/72；不是72/72。每条发一个1024点源并循环约10秒，
  RX40dB，固定4096点四窗共享RMS/mean-logit。滤波前验证每条源功率保留≥99%、
  归一化失真≤1%；失败/预测回退也纳入分母。

当前campaign沿用了TX LO+250kHz、TX70及2.1MS/s/BW1.5MHz，但RX50dB，
每批拼接24个不同1024点源、重复约4秒、使用pilot对齐和单窗推理；
**仅同步检测有500kHz FIR，实际模型payload没有175kHz FIR**。因此遗漏了历史有效的
模型输入滤波对照；这不是已经证实的频段或天线单因素失败。

离线复用旧`repair-b210-lo-leakage.py`的同一FIR系数（SHA-256
`d0e12014bedae088b71366497299adc3a1be0a45cf622e9cbb346d4ea0c8c8be`），
固定原audit的payload位置/CFO/相位，不重新找窗、拟合增益或根据预测调参数。
RX使用两端完整128点halo；源保真使用实际24条拼接帧及其邻居/保护区，
而非假装每条在当前TX中独自周期重复。

| 当前batch | 原始复相关均值 | 旧FIR后复相关均值 | payload接收功率被滤除 |
| --- | ---: | ---: | ---: |
| 4267 | 0.189643 | 0.758439 | 93.762% |
| 4268 | 0.165355 | 0.717607 | 94.698% |
| 22016 | 0.189147 | 0.759007 | 93.811% |

72/72源样本达到旧FIR的源保真条件：最小功率保留99.5609%、最大失真功率0.3629%。
复相关是逐1024点的非中心化复相关幅值，与旧记录的中心化四窗来源门不是同一统计量。
滤波使当前约0.18相关提高到约0.75，直接支持额外频带分量是主要污染；
仍不证明余下差异全部来自背景或滤波后分类必然正确。没有为本次滤波结果运行模型。
不能将本先导72条通过源保真推广到全库，特别是不同SNR/带宽及拼接边界。

检查包括父级audit/IQ/source行/TX帧哈希、固定行号与FIFO帧重建、旧FIR系数哈希、
独立FFT卷积对坐标/数值验证（误差小于1e−9），以及旧汇总的69/30/56计数复核。
所有计算在内存完成，无新增IQ/张量/缓存副本或硬件进程，没有开发临时目录需删除；
只增加Git中的脚本与派生统计。原保留清单74份文件全部哈希仍匹配。

复核命令只打印JSON，不更改campaign及旧证据：

```bash
cd /home/jetson/sdrharness
PYTHONDONTWRITEBYTECODE=1 OPENBLAS_NUM_THREADS=1 \
  local-assets/amc-eval/runtime/venv/bin/python -B \
  jetson-agx/sdrharness/scripts/diagnose-rml2018a-campaign-filter.py \
  --root /var/tmp/sdrharness-dev/b210-rml2018a-pilot-20260910c
```

## 2026-09-11：回到2.4GHz与SINR记录合同

基线`53424fd`，分支`codex/sdr-improvements`。用户明确报告已换成2.4GHz并要求代码使用SINR记录，
随后指出原始X已经包含源噪声/损伤，真实收发又叠加环境和硬件影响。本单元只交付参数及记录合同，
新增RF采集0、发射0、模型推理0、生产部署0；用户报告接法不等于已核实天线型号。

- 新campaign schema为`rml2018a-all-row-rf-v2`，固定2455MHz（沿用历史中心，未经本轮背景扫描），
  TX70/RX50、2.1MS/s、BW1.5MHz、峰值0.2及TX LO+250kHz保持。NX从已校验计划取中心频率，
  AGX的计划、原生接收校验和UHD readback使用同一CENTER，消除旧433.92硬编码。
- source及prediction字段`source_snr_db`原样保留Z。X本身含噪声/信道损伤，
  原标签、源/实收相关度以及对已含噪X拟合的残差均不能作为总接收SINR。
  此次不更换数据集、训练模型或添加生成器。
- capture audit及prediction加入`rx_sinr_db=null`、`rx_sinr_status=not_measured`、
  方法/测量带宽null、参考位置及模拟带宽。同步成功仍缺干净参考及已验证分量估计器；
  同步失败另记原因。没有用0dB填缺失，也没有把同步导频质量当作payload SINR。
- 汇总明确`by_source_snr_db`，另列接收SINR已测0、未测原因、待生成逐行质量记录数量及空接收SINR分组。
  当前版本拒绝带伪造数字SINR/已测状态的prediction，以及旧schema结果混入新汇总。
- 旧计划仍因schema/软件身份拒绝复用，不能更新旧plan的哈希来迁移。
  导频生成独立固定v1种子，保证旧433MHz帧仍可供离线重建；历史结果和原证据不改写。

15项`test_rml2018a_campaign.py`测试通过，包括旧433MHz频率/旧schema在UHD前拒绝、
新2455MHz参数传到模拟文件消费者、有限FIFO精确字节及子进程退出、停止/续跑、同步与
旧导频逐字节稳定性；源+30/−20dB、未同步样本均不产生实测SINR，0及30dB伪造值拒绝，
源SNR分组和失败分母保留。测试只用本地模拟文件消费者，没有连接NX/P201或调用UHD。
代码AST、文档链接及diff另经检查。新频点当前接法的实机收发与接收SINR估计均未完成。

测试临时根`/var/tmp/sdrharness-dev/rml2018a-sinr-20260911-sLitie`；各TemporaryDirectory退出前
逐文件统计并删除22个文件、75,413字节（统计最终删除时文件大小，不计覆写/管道流量），
测试FIFO已移除且模拟子进程全部退出。顶层空目录交付前移除，无本单元外部保留文件或IQ副本。
此前74份RF证据1,580,090字节保持原路径和哈希，删除依据仍见原库存；封存证据未重写。

当前建议保留RadioML主对照，先做新频段高源SNR基线再扩档；严格接收SINR曲线另须验证
干净参考/分量估计器和带宽，不能把上述字段实现勾成SINR实测或全库质量通过。

## 2026-09-11：条件有效SINR实现与2455MHz有限验证

基线`ab34c25`，用户已明确换成2.4GHz天线并要求实现SINR。完成campaign v3的
`nominal-source-plus-crossfit-link-error-v1`：在固定pilot对齐窗口上，两半窗交叉拟合复数标量增益，
以留出半窗的完整残差衡量新增链路误差，再按源Z的标称功率比例分解预测X功率。
公式、固定有效性条件和带宽见[合同](../reference/RML2018A_FULL_RF_CAMPAIGN.md#源snr与条件有效sinr估计v3)。
源标签保持原值；没有直接将相关度、EVM或RX-X残差冒充总SINR。

结果明确为**条件有效SINR，包含未建模链路失真**，要求源标称功率比例适用、
窗口内近似标量线性、源信号/噪声同增益、新增干扰与X不相关。实收无法独立证明这些假设；
尤其稳定多径、非线性和相关干扰可能偏置结果。因此`estimated`不等于仪器独立测量，
`measured_rows=0`，没有生产准入/精确物理SINR或置信区间声明。
模型输入未增加滤波、均衡或标签引导选择，RMS前积分整个2.1MHz采样复基带，模拟RX带宽1.5MHz。

源Z继续单独分组。capture audit直接保存逐行估计/invalid/未同步原因，`acquire`也写汇总；
推理沿用封存audit的质量记录，汇总拒绝不一致的prediction。接收估计按固定2dB半开区间分箱，
每箱保留采集、已推理和正确数；无效行不会从全量识别分母中删除。
新增诊断在audit/prediction各保存一次，元数据预留由每批128KiB调整为256KiB；全库元数据
27,917,287,424字节，连同IQ共55,834,148,864字节（约52GiB），累计TX上限不变。

### 检查与同步范围修正

最终23项无硬件测试通过。240次已知分量仿真覆盖源Z −20/0/10/30dB、新增链路比0/10/20dB，
混合白噪声与窄带单音；相对已知真实分量的均值误差0.00854dB，95%绝对误差0.31013dB，
最大绝对误差0.57890dB。这仅验证所模拟的假设，不能作为实机误差上限。
另补200次源+30dB/新增链路比−9dB仿真以覆盖本轮低比值区间：170次估计、30次invalid；
只对170次条件估计统计，均值误差0.09898dB、95%绝对误差0.96500dB、最大1.28019dB。
这也说明高链路比仿真的0.31dB数字不能外推到当前弱链路。
另覆盖零新增误差时不超过源Z、DC干扰计入但合法源DC保留、增益/相位缩放不变性、
增益突变/突发残差/恒定参考/零功率/非法数值拒绝、篡改功率/行记录拒绝、分组分母、
有限FIFO精确字节、停止/续跑，以及+3370/−9100Hz同步与原有错误标记/静音拒绝。

首次2455MHz两批采集仍用了433MHz遗留的±2500Hz搜索/3000Hz细化上限，均失败。
保留IQ后做固定±25kHz的离线诊断，第一批发现CFO约3368.49Hz，原门限下marker可达0.68820。
修正按原433.920MHz相对频偏容限缩放：2455MHz搜索±14.5kHz、500Hz步长，细化后上限17kHz；
初始0.55/最终0.65门限不改。新计划记录范围，旧计划的软件哈希不重写。
[离线复核脚本](../../jetson-agx/sdrharness/scripts/diagnose-rml2018a-campaign-sinr.py)使用最终固定范围，
检查旧audit/IQ/源行及Z/重建TX帧哈希，输出独立`sinr-recheck-v1.json`，不改变初次失败状态。
第一次复查得到6条条件估计、18条invalid、24条未同步；这是事后派生结果，未冒充新采集通过。
其中第一批由尾部marker定位前一重复payload，前一marker得分仅0.03924，解释仍受干扰及连续性限制。

### 两阶段实机执行

两个新campaign分别在根内保存`sinr-validation-plan.json`：每阶段固定batch4267与22016，
各24条、源Z均+30dB、源ID分别0/4；四次采集是同48个源行的重复，不是96个不同样本。
2455MHz，TX70/RX50、峰值0.2、LO+250kHz、2.1MS/s、BW1.5MHz，RX1/RX0/A_BALANCED。
每点65535ci16复数、500ms settle、1000ms点超时；每批最多4秒TX，每次runner deadline180秒。
两阶段合计TX上限16秒，4份IQ共1,048,560字节。开始可用空间809,183,944,704字节。
NX/P201精确瞬时路径在每批GO前记录，直接SIGINT/专用generation取消及原有55/58秒保护保持。

| 阶段/batch | 同步结果 | 条件估计 | invalid | 未测 |
| --- | --- | ---: | ---: | ---: |
| 初次4267 | marker_not_found 0.510534 | 0 | 0 | 24 |
| 初次22016 | marker_not_found 0.396311 | 0 | 0 | 24 |
| 最终新计划4267 | marker 0.695507、CFO3312.626Hz | 4 | 20 | 0 |
| 最终新计划22016 | marker_quality 0.577650，未达0.65 | 0 | 0 | 24 |

最终4条估计分别−7.49932、−5.99010、−6.81423、−8.86810dB；16条增益不稳定、
3条参考低于估计范围、1条残差不平稳，另24条未同步。只报告通过条件的范围，
不把它作为全部48条或整个2.4GHz的SINR分布。最终4267的payload前marker即通过门的marker，
没有使用初次派生记录的弱前marker。最后48条质量记录及同步结果由封存IQ/X/Z重新计算后逐项完全一致。
初次两份UHD日志尾部为`U`，最终两份为`S`；仍不是时间戳化的连续发射证据，
不能由此断言干扰来源为Wi-Fi、天线或LO泄漏。后续应先定位发射连续性、同步和增益波动。

新增模型推理0，未运行训练/独立locked-test/全量数据识别，生产`recognizer_available=false`。
23项测试与有限实收只完成软件和估计路径验证，未完成可靠全类别SINR测量或识别质量验收。

### 恢复、清理与保留

四批均核对同一sdrd、原生RX身份、无掉样/削顶/溢出、LO/rate/BW、两路RX模式/增益/端口、
全部scan mask/buffer恢复。最终NX身份wheeltec/serial2508504、无TX进程/USB持有者，
P201 healthy=true；NX/P201瞬时目录均不存在。没有启动模型或暂停Spark。
各根`sinr-postflight.json`保存只读最终检查。

本单元保留49份证据、1,272,022字节，含4份IQ、初次失败、版本化离线复查、最终采集审计和计划；
逐文件路径/大小/SHA-256、source/request/session、profile/preprocess及精确人工删除方法见
[库存](../evidence/RML2018A_SINR_IMPLEMENTATION_2026-09-11.json)。旧74份RF证据1,580,090字节仍逐文件哈希一致。
NX删除28份暂存文件929,524字节；AGX随批删除4份可重建源packet共835,584字节，
收尾删除11份日志/锁等85,517字节，两根scratch/owner与开发根均不存在。
单元测试TemporaryDirectory及FIFO自行清理，不把收尾数字称为覆盖所有测试历史写入的总量。
无新增IQ副本、权重或构建制品入Git，保留证据没有被说成已清空。

## 2026-09-11：停发背景与实收失败排查

用户要求检查“只有4条估计通过”是否因为没收到或SINR低。基线`104887b`，本单元先复核
此前49份封存证据，再补停发接收；没有新增发射、模型推理、生产部署或修改campaign估计门限。
脚本为[链路诊断](../../jetson-agx/sdrharness/scripts/diagnose-rml2018a-link-quality.py)，
[逐段统计](../evidence/RML2018A_LINK_QUALITY_DIAGNOSIS_2026-09-11.json)记录原始父级哈希及固定方法。

### 已确认的现象

最新OOK批次的24个源样本，实际发送窗口平均功率仅在0.008682–0.012205间变化（峰值缩放后），
接收对应窗口功率却从约22–83升到数千，最高29,122（ADC平方单位）。第13–19个窗口等强突发区间
与估计失败重合；第2、23个窗口也明显升高。不能把所有失败都解释为发送源样本本来更大。
其中15条的诊断相关峰仍在原位置0/−1样本附近，其余9条峰低且位置零散，没有支持统一帧滑移的证据。
这次±1200点局部搜索只用于诊断，未用于选择模型/SINR窗口或修改原始审计。

“gain_not_stable_across_halves”表示两半窗拟合出来的复增益不一致；强干扰、低参考强度、
信道或模型误差都可能造成它，**不能据此宣称硬件增益或AGC在变化**。
旧四份实收报告的clipped/dropped/overflow均为0，只说明当时这四次未报告这些故障。

每256点（约121.9微秒）计算平均功率，保留每次采集自身P10作为较安静时段参考：

| 采集 | RX增益 | 最大块功率相对本次P10 | 超出本次P10 10dB的块比例 |
| --- | ---: | ---: | ---: |
| 先前最新OOK实收 | 50dB | 44.39dB | 23.14% |
| 新停发背景0 | 40dB | 41.12dB | 21.96% |
| 新停发背景1 | 40dB | 37.47dB | 24.31% |
| 新停发背景2 | 40dB | 33.47dB | 23.14% |

P10不是校准热噪声底；上表只作各段内部动态范围比较，不把不同增益/时刻的ADC绝对功率直接对比。
停发时仍有强突发，说明这类污染不依赖我们本次RadioML发射。2.4GHz Wi-Fi是候选来源，
但本次没有协议解码、关闭可疑设备或负载对照，不能确认具体设备，也不完全排除接收内部因素。
原TX日志的U/S只有非时间戳标记，不能证明连续无丢包，发射连续性仍有未测边界。

![两次独立采集内的功率突发](../evidence/RML2018A_LINK_BACKGROUND_BURSTS_2026-09-11.png)

图中上下不是同时采集，增益不同；纵轴分别相对各自P10归一化，虚线是+10dB统计线，不是模型门限。

### 滤波与带宽重叠

四份旧实收的最强谱线附近±2kHz仅占整段Hann谱功率约0.29%–4.40%，不符合此前433MHz
单根谱线占约82%的情况；不能直接套用“主要就是一根LO泄漏”的解释。
新停发背景约20.1%–22.3%的整段Hann谱功率落在±175kHz内，支持有效带宽内仍有背景污染。

对唯一已同步的最新OOK24条，固定原始位置/CFO，复用257-tap、175kHz Kaiser FIR，
并用实际拼接帧及邻居检查源保真：最小功率保留99.6188%，最大源失真0.2797%。
原始/滤波后平均中心化相关为0.15559/0.34292。滤波确有改善，但仍不足以说明链路已经修复。
滤波后源噪声比例不再能直接按原Z分解，因此未把这一对照写成新的滤波后SINR或识别率。
未对未同步QPSK强行生成24条payload，也未将这24条源保真门推广到全库。

### 停发接收、失败、恢复和保留

P201及NX使用既有技能/受保护凭证；核对NX身份wheeltec、serial2508504、无TX进程/USB持有者，
P201单一sdrd/PID/哈希/监听和空闲mask/buffer。NX仅作只读空闲检查，未初始化UHD或发射。
两个背景计划各注册2455MHz、2.1MS/s、BW1.5MHz、单RX1/RX0/A_BALANCED、每点65535ci16复数、
500ms settle、1000ms点超时、三个频点请求、最大786,420字节、总deadline120秒、预计30秒，
并在运行前保存全部generation、精确P201瞬时路径、AGX根和可用空间（均大于809GB）。
停止走SIGINT/SIGTERM及专用generation cancel，随后核对完整射频状态恢复。

首次RX50仅尝试第一点，即被Controller以`summary_clipped`拒绝；该错误来自对原始ci16的分析，
不是本轮SINR估计器失败。没有有效IQ保留，剩余两点未执行。原audit、错误日志及当时脚本字节
单独保留，未改写为成功。后以新根、RX40重新登记三个短接收，三次均通过原生元数据/身份/
无削顶掉样溢出检查，保留786,420字节IQ。降低增益只避免此次保护拒绝，不证明SINR提升。

四个实际接收尝试（1次保护失败、3次成功）均已恢复；两个audit均保存radio_before/after、
healthy=true和NX空闲检查，全部登记P201路径不存在。失败时未关闭削顶保护或放宽元数据验证。
脚本复用已有Controller及原生检查；没有另起collector，也未修改设备部署配置或实验默认TX增益。

本轮外部保留20份文件、823,070字节，逐文件SHA-256/用途/source-request-session/处理身份/
人工删除方法见[保留清单](../evidence/RML2018A_LINK_QUALITY_EVIDENCE_2026-09-11.json)。
收尾删除开发目录中的4个日志/中间JSON/绘图缓存文件，共190,808字节，开发根已不存在；
NX没有暂存文件，P201瞬时路径已清除。原49份证据全部哈希不变，无新增IQ副本入Git。
JSON/AST/本地链接/diff、原生采集检查及图表视觉检查通过；此诊断单元不重跑模型或无关回归。

当前更合理的下一步是有限扫描2.4GHz占用、选择较空闲位置，或使用足够衰减的受控链路建立基线，
随后再验证固定滤波及识别。仅继续增加增益或换一个数字滤波器，不能解决所有同频突发污染。

## 2026-09-11：八频点背景比较与候选复测

基线`ede5878`。用户“继续”沿用先比较2.4GHz背景、再独立确认候选的单元；
外部SDR始终停发，P201 RX40、2.1MS/s、BW1.5MHz、RX1/RX0/A_BALANCED。
在既有[链路诊断脚本](../../jetson-agx/sdrharness/scripts/diagnose-rml2018a-link-quality.py)增加
`survey`，复用原生Controller/背景元数据校验/统计，不新增collector或生产TX能力。
旧诊断入口与证据保留；之前源码身份可由`ede5878`复原，不改写旧附件哈希。

### 预设范围与选择方法

发现阶段固定2405/2415/2425/2435/2440/2455/2465/2478MHz，正序、倒序、正序各一轮24点。
每点65535个ci16复数（262,140字节）、500ms settle、1000ms点超时；每次仅观察约31.2ms，
每个候选发现阶段合计约93.6ms，不能称连续覆盖或长期占用率。8个1.5MHz接收窗口之间有未测频率。

从有3次有效捕获的替代频点中，按最差175kHz FIR峰值RMS、最差p95、最差原始RMS依次排序。
2455MHz作为当前基准，不作为替代候选。任何一次削顶使该频点不能入选；失败原样保留。
选定后以“基准/候选、候选/基准、基准/候选”6次新接收作确认，不根据确认结果重新挑选。
沿用窄带背景参考条件：三个候选确认都须p95≤5、峰值≤8 ADC RMS（均须大于0）。
这是预设背景安静条件，不是仪器SINR、分类阈值或所有RadioML样本的带宽合同。

两个阶段的完整规则、有限计划、增益/频率/每点generation、精确AGX/P201路径、空闲及空间检查
均在各自audit中于接收前登记。最多30点、IQ上限7,864,200字节，两阶段各600秒deadline；
根分别为`/var/tmp/sdrharness-dev/rml-band-survey-20260911`及同名`-confirm`。
SIGINT/SIGTERM走专用generation取消及原恢复流程。遇到已经结束并恢复的`summary_clipped`
仅记录失败、跳过其IQ并继续预登记列表，其他错误仍中止；没有降低增益或关闭保护混入同组。

### 实际结果

发现24次中22次有效、2次削顶：倒序轮的2478MHz及2415MHz，均被排除。
其他频点按三轮最差滤波峰值从低到高为：2440MHz 65.24、2405MHz 68.31、2435MHz 81.90、
2455MHz 98.59、2465MHz 99.53、2425MHz 213.98 ADC RMS。
2405MHz另两次峰值接近8，但其中一次达68.31，说明不能只取最安静的一次。
发现阶段排名最好的是2440MHz，本身最差值已经高于安静参考；仍按预定规则独立确认。

| 确认对 | 2455MHz滤波峰值RMS | 2440MHz滤波峰值RMS | 候选相对基准峰值降低 |
| --- | ---: | ---: | ---: |
| 1 | 515.67 | 62.79 | +18.29dB |
| 2 | 29.76 | 80.86 | −8.68dB |
| 3 | 23.80 | 61.88 | −8.30dB |

六次确认均未削顶，但2440MHz三次p95为14.08/40.48/13.90，三次峰值都远高于8，
安静条件失败；三对中两对比2455MHz更差。因此本轮**没有得到通过独立复测的稳定替代频点**，
不把实验默认改成2440MHz，也不声称整个2.4GHz没有可用位置。

![发现阶段与独立确认](../evidence/RML2018A_BAND_SURVEY_2026-09-11.png)

图中虚线仅为峰值RMS参考8；削顶点没有被画成某个有限测量值。
结果含原始带宽和固定175kHz FIR统计，不能因窄带排名较好就宣称当前未滤波模型payload合格。
本次无发射、无模型推理/训练，未修改冻结profile、SINR估计器或生产部署。

### 验证与清理

3项选频测试覆盖“不能按偶然安静轮入选”、削顶排除、不完整发现拒绝、固定确认顺序、
失败保留和不重新选择。28份有效原始IQ通过既有原生身份/频率/增益/无削顶掉样溢出校验，
所有统计、候选排名和确认结果重新计算完全一致；源码AST、文档链接、JSON/diff及图表视觉检查通过。

两阶段最终均核对原LO/rate/BW、两路RX模式/增益/端口、scan mask/buffer恢复，
同一sdrd、P201 healthy=true、NX wheeltec/serial2508504无TX进程或USB持有者，
全部登记P201瞬时路径不存在；最终检查在确认根的`postflight.json`。

保留150个文件、7,509,332字节，其中28份IQ共7,339,920字节，支持复核跨轮排名和确认；
没有IQ副本。逐文件路径/SHA-256、request/session、预处理身份和精确人工删除方法见
[结果及库存](../evidence/RML2018A_BAND_SURVEY_2026-09-11.json)。
收尾删除开发根内2个日志/绘图缓存文件141,432字节，开发根已不存在；NX未暂存文件，
P201只留瞬时数据并已移除。旧实验记录及原始附件不因这次选频而重写。

下一步优先用足够衰减的受控连接或屏蔽环境建立识别基线；若继续空口选频，应另登记
更长观察时间与覆盖范围，不能无限重复短采集直到碰到一段好结果。

## 2026-09-11：发射增益与接收增益有限对照

基线`30f3c3c`，用户批准“增大发射增益、减少接收增益”，随后继续执行已登记的条件阶段。
频率保持2455MHz，使用用户已确认的2.4GHz天线，型号/性能未核实；P201仍为RX1/RX0/A_BALANCED。
本单元只完成两组增益的短时采集、同步/条件SINR及保真比较，无模型推理/训练或生产部署。

### 设备范围、代码与有限计划

NX上`uhd_usrp_probe --args type=b200,serial=2508504`在30秒timeout内完成，
实际TX前端PGA报告`0.0 to 89.8 step 0.2 dB`。这是驱动格式化增益范围，不是校准输出功率，
不宣称设置80dB等于80dBm。探测未启动TX流，前后核对wheeltec身份、serial及进程/USB空闲。

campaign沿用v3及原同步/质量门，只增加有限增益选项：TX70/80、RX40/50；
`plan`默认70/40，执行命令拒绝增益覆盖。TX helper、UHD实际回读、RX计划/元数据与质量行
沿用同一登记值，summary也保存两个增益。修改软件身份必须新根，不改写旧计划或失败审计。

先登记70/40两批；仅当两批均通过原生无削顶/掉样/溢出与恢复检查，且仍未同步或SINR无效/为负，
才执行80/40两批。`gain-comparison-plan.json`在首次采集前记录全部范围，
`tx80-condition.json`保存进入第二阶段的实际判定。两阶段各用批次4267及22016：
源行102408–102431（ID0/OOK）及528384–528407（ID4/QPSK），原始Z均+30dB，名称仍provisional。
共48条独立源行、96次行观测，不是96条不同源样本。

每批rate2.1MS/s、TX/RX BW1.5MHz、TX峰值0.2、LO偏移+250kHz；
RX65535个ci16复数/262140字节、settle500ms、点timeout1000ms、runner deadline180秒。
全部最多4次捕获、1,048,560字节IQ；每批最多4秒样本量，总16秒/33,527,808个TX样本，
不代表已证明RF连续发射时长。源包瞬时上限208896字节。登记预计约180秒壁钟，
实际跨对话中断及完整源文件哈希耗时另计；登记可用空间809,171,951,616字节，逐批再次检查。
两个AGX根为`/var/tmp/sdrharness-dev/b210-rml2018a-tx70rx40-20260911`及同名`tx80rx40`根。
每批在GO前记录精确NX/P201瞬时路径与generation，SIGINT/SIGTERM走既有专用取消/停止及恢复。

### 实际结果及解释

| TX/RX增益dB | 批次/源类别 | 同步 | 条件SINR估计/无效/未测 | 有效值范围dB | 有效值均值dB |
| --- | --- | --- | --- | --- | --- |
| 70/40 | 4267 / OOK | 失败，marker 0.4440 | 0 / 0 / 24 | — | — |
| 70/40 | 22016 / QPSK | 失败，marker 0.1837 | 0 / 0 / 24 | — | — |
| 80/40 | 4267 / OOK | 成功，marker 0.8784 | 22 / 2 / 0 | −2.598～−0.205 | −1.179 |
| 80/40 | 22016 / QPSK | 成功，marker 0.8703 | 20 / 4 / 0 | −4.520～+0.224 | −1.114 |

80/40共42条估计，六条invalid也保留：三条两半窗拟合增益不一致、两条残差不平稳、
一条参考/残差比低于估计范围。**六条不是未收到：两批已同步，只是这些窗口不能可靠给出当前条件估计。**
70/40也有完整IQ，未找到合格标记，不能解释成接收机没有收到任何射频能量。
QPSK的最佳标记位于尾部，按既有帧长度取前一完整payload，前标记得分0.8570；
两批CFO分别约3552.24/3563.76Hz，未降低同步门或用类别挑选对齐。

对所有已同步窗口（含invalid）按原标记对齐，未滤波源/实收绝对归一化相关度均值为
OOK0.6093、QPSK0.5731；中心化平方相干度均值0.2409/0.3680。
二者仅是波形保真指标，不当作SINR；原始未滤波payload仍有明显误差。
本次80/40观察到更好的同步及更多可用估计，支持提高期望信号强度这一方向，
但两阶段是不同时间、不同run导频的短捕获，不能把差异全部归因于10dB TX设置变化，
也不能把历史70/50当成同时对照来证明降低RX增益改善了SINR。

四次P201捕获均无削顶/掉样/溢出且恢复；UHD日志按批依次有`U/S/S/U`尾部标记。
有限FIFO全部字节写完及子进程退出已核实，但这些不是时间戳连续性证据，发射连续性仍待定位。
当前SINR含信道/硬件失真及新增干扰，不能据负值断言全部来自Wi-Fi；Wi-Fi身份仍未确认。
本轮不足以启动全量识别或宣称接收质量合格；可先复用已同步IQ做固定滤波保真及有限工程识别对照，
再决定是否必须使用有衰减的受控连接/屏蔽环境。

### 验证、恢复与清理

28项相关测试通过，包含新增增益拒绝及TX80有限FIFO精确字节/退出；原SINR240次仿真仍通过。
四份IQ的capture-complete/audit/IQ/元数据/源行哈希、实际TX增益与TX日志哈希均重核；
全部同步成功/失败及逐行条件质量由原始IQ重算完全一致，无源IQ副本。
最终只读核对同一sdrd PID5909及哈希、原LO/rate/BW、两路RX模式/增益/端口、
scan mask/buffer全部恢复，P201 healthy=true，NX无TX进程/USB持有者，全部登记瞬时路径不存在。

外部保留51份文件、1,264,484字节，其中4份IQ共1,048,560字节；包含两阶段计划、
失败及成功audit、驱动范围、质量重算脚本/结果、测试日志与清理记录。
精确路径/SHA-256、source/request/session、profile/处理身份及人工删除方法见
[保留清单](../evidence/RML2018A_GAIN_COMPARISON_2026-09-11.json)。
NX自动删除28份暂存文件931,338字节；AGX自动删除4份源包835,584字节；
收尾删除10份日志/重复输出/空owner锁42,621字节及三个空目录，开发根与scratch/锁均不存在。
测试内部自行删除的瞬时写入流量未计入收尾字节数；旧RF证据不动，保留包不称为全部清理。
本单元代码在AGX实际运行，NX helper仅瞬时暂存，P201/Controller及生产模型配置没有更换。

## 2026-09-11：固定滤波与48条工程识别对照

基线`5fcc093`。沿用用户“继续”与当前执行选择，复用TX80/RX40的两个已同步批次4267/22016，
48条源Z=+30dB，源ID0/OOK及ID4/QPSK各24条。新增TX/RX均为0，无训练、调参或生产部署。
父级51份增益证据保持原字节，诊断结果单独保留；没有把预测写入已封存campaign。

扩展既有`diagnose-rml2018a-campaign-filter.py`支持显式1–3个完整批次和可选派生根推理。
固定历史257-tap、175kHz cutoff、Kaiser beta8系数，SHA-256仍为
`d0e12014bedae088b71366497299adc3a1be0a45cf622e9cbb346d4ea0c8c8be`。
RX在完整捕获上用valid FIR及128点halo，保留原标记确定的位置/CFO/相位；
源保真用实际拼接帧的邻居/保护区，不将每条假装为周期信号，不按标签重新对齐或减DC。

### 波形及模型结果

| 源类别 | 原始/滤波实收相关度均值 | 最小源功率保留 | 最大源失真功率 | 滤除的payload功率 |
| --- | --- | --- | --- | --- |
| OOK | 0.6093 / 0.8653 | 99.6188% | 0.2797% | 70.3191% |
| QPSK | 0.5731 / 0.8613 | 99.6894% | 0.3014% | 83.9371% |

相关度是非中心化复相关幅值，不能作为SINR。48/48源行达到既定保真门（功率≥99%、失真≤1%），
所以按预登记的四组输入执行全部48条单1024窗工程识别，不排除原SINR invalid的6条。
两批原始谱主峰都约+253.596kHz，接近TX LO偏移+250kHz加CFO；这是频率位置相符，
不能由此独立识别该分量的物理来源，也不能证明被滤除的全部功率都是LO泄露。

| 输入 | OOK正确/24 | QPSK正确/24 | 合计正确/48 |
| --- | ---: | ---: | ---: |
| 原始源 | 24 | 24 | 48 |
| 滤波源 | 24 | 24 | 48 |
| 原始实收 | 24 | 17 | 41（85.42%） |
| 滤波实收 | 3 | 0 | 3（6.25%） |

逐条比较：38条由正确变错误、0条由错误变正确、3条两者正确、7条两者错误。
滤波OOK的21条误判为ID2/8ASK；滤波QPSK的13条误判为ID16/256QAM，其余分散在其他ID。
文本名称沿用server-v1、provisional；源行身份来自原Y，结果仅为工程对照。

本轮说明“相关度提高”和“该冻结模型识别改善”并不等价。源滤波控制48/48正确，
但真实接收中仍有未消除的误差，模型对滤波后输入的误判原因尚未确定；
不能直接断言滤波去掉了有用信号、模型依赖干扰或所有滤波都无效。
旧2455MHz的56/72结果采用不同源样本及四窗重复发射流程，不能保证当前单窗拼接流程同样改善。
当前**不将175kHz FIR接入campaign payload**，也不因41/48正确就认定全库或物理链路合格。
较低条件SINR与部分正确识别可以共存；该估计包含链路失真，不是分类的硬性通过门。
滤波改变源噪声功率分配，滤波后SINR保持null，原条件SINR只附在未滤波父级输入记录中。

### 实现、验证和边界

推理前在派生根保存固定批次、源保真门、四输入/192窗口及2warmup、650秒deadline的计划。
任一源行不满足保真门时整组不启动GPU；允许的最多3批对应288实验窗口。
复用冻结epoch-10、FP16 autocast/FP32权重、同一单窗复数RMS；模型身份及每条24维logits、
数字预测、归一化输入SHA-256保存，无confidence阈值/温度拟合。生产recognizer_available=false。
实际192次实验推理及2次warmup完成，GPU租约一次获取/释放，持有约23.43秒；
仅暂停当时空闲的Spark PID1321，原PID/start_ticks已恢复，独立恢复watchdog已停止。
本轮未访问NX/P201，没有声称重新测量射频状态。

4项测试覆盖固定带外tone抑制与类别标签无关的完整行保留（合成测试每批24行）、
宽带源被保真门拒绝、非法/重复批次在IO前拒绝、IQ哈希篡改拒绝。
实际数据复核独立FFT卷积、全部源行/TX帧哈希、原同步重算、192输入哈希和全部预测计数。
初次复核对等价复数相位表达式要求逐位相等而失败，最大差仅2.84e−14 ADC单位；
按1e−10数值容差核对后，原始实收48条归一化模型输入仍与campaign路径**逐字节哈希一致**，
未改变输入、质量门或重跑模型。所有准备统计及四组输入均可从原始证据复现。
交付源码仅进一步修正结果语义文字为“登记批次”，实际执行源码字节单独保留，处理算法未改。

### 保留与清理

派生根`/var/tmp/sdrharness-dev/rml-filter-tx80rx40-20260911`保留9份文件282,817字节，
包括计划、准备统计、预测、GPU恢复、执行源码、复核脚本/结果及测试/清理记录，无IQ或张量副本。
父级51份文件全部大小及SHA-256复核不变；原始实验仍按其原清单保留。
收尾删除4份重复输出/空锁27,801字节和3个目录，开发根、scratch、GPU锁均不存在；
测试内部瞬时流量未计入收尾字节数。精确路径/source/request/session、模型及预处理身份、
哈希/大小与人工删除方法见[本轮保留清单](../evidence/RML2018A_FILTER_INFERENCE_2026-09-11.json)。
下一步先利用既有IQ定位滤波后的误判及残余偏置/相位/归一化影响；不以同批反复调参后的
结果冒充独立验收，扩类/扩源SNR和全库仍须后续有界验证。

## 2026-09-11：滤波误判的有限归因诊断

基线`0d6857d`。用户继续推进“先查滤波后为何误判”，本单元只使用原TX80/RX40的
批次4267、22016，共48条源Z=+30dB、OOK/QPSK各24条。新增射频操作0，不扩大源行、训练、
微调或独立准入。前述41/48未滤波与3/48滤波基线在新进程中复现。

在同一离线脚本增加两种互斥诊断入口，复用原FIR/完整halo/固定同步位置、冻结模型及GPU恢复流程。
每阶段在自己的新根、模型运行前保存固定变体和650秒预算；第一阶段十组共480实验窗口，
发现简单校正仍失败后，第二阶段固定八组共384窗口，各有2次warmup。
共864次实验窗口是同48条的18组派生输入，不是864条独立源样本。
全部行保留，未依据正确率换窗口、滤波参数或剔除原SINR无效行。

### 第一阶段：简单校正不足以恢复识别

对滤波源x与实收y拟合`y=h*x+b`，显式记录源辅助的复增益与偏置。
频偏诊断使用8个固定128点的中心化仿射拟合，按源能量加权拟合展开相位斜率；
超±5kHz记无效并不施加频率校正。本组48条均在范围内，但这不是已独立校准的CFO测量。
源相位、源能量和拟合偏置只有知道发射X才可得到，不能用于生产未知信号识别。

OOK各窗残余CFO诊断中位约51.44Hz、范围−196.66～317.49Hz；QPSK中位28.97Hz、
范围−93.81～82.37Hz。拟合偏置幅值/滤波RMS中位分别5.30%及3.76%。
滤波RMS/原始RMS中位0.6968/0.7143，部分强突发窗更低，不能把整批功率比当作逐窗归一化比例。

| 固定输入变体 | OOK正确/24 | QPSK正确/24 | 合计/48 |
| --- | ---: | ---: | ---: |
| 未滤波实收（基线） | 24 | 17 | 41 |
| 滤波实收（基线） | 3 | 0 | 3 |
| 滤波实收沿用原始RMS | 3 | 0 | 3 |
| 源波形按相同RMS比例缩小 | 11 | 0 | 11 |
| 原始源单位RMS | 24 | 24 | 48 |
| 滤波实收仅修正拟合相位 | 5 | 0 | 5 |
| 仅移除拟合偏置 | 4 | 0 | 4 |
| 修正相位并移除偏置 | 4 | 0 | 4 |
| 仅修正拟合频偏 | 3 | 0 | 3 |
| 修正拟合相位与频偏 | 5 | 0 | 5 |

尺度对照故意保留非单位RMS，经过同一模型接口时不再归一化；合成测试验证不会把尺度变量消掉。
源缩小后的11/48显示冻结模型对输入尺度敏感，支持继续遵守单位RMS合同；
但实收沿用原始RMS仍为3/48，因此不能用这一尺度解释单独修复当前回退。
相位/频偏/偏置也仅改善到3–5/48。这只证明这些固定、简化校正在本组不足，
不能彻底排除复杂时变频率误差或更复杂信道影响。

### 第二阶段：幅度与相位误差的分量对照

固定使用同一已知源拟合h、b，令`yc=(y-b)/h`、`removed=raw-filtered`。
幅度误差单独保留的输入是`abs(yc)*exp(j*angle(x))`，相位误差单独保留是
`abs(x)*exp(j*angle(yc))`，之后各自单位RMS。它们替换了真实接收的一部分信息，
包括使用源在低幅度时的噪声相位，因此只作诊断构造，不是可部署校正。

| 固定分量变体 | OOK正确/24 | QPSK正确/24 | 合计/48 |
| --- | ---: | ---: | ---: |
| 原始源 | 24 | 24 | 48 |
| 滤波实收 | 3 | 0 | 3 |
| 仅FIR互补成分removed | 2 | 0 | 2 |
| 已知滤波源×h加removed | 24 | 20 | 44 |
| 保留实收幅度误差、换回源相位 | 23 | 9 | 32 |
| 保留实收相位误差、换回源幅度 | 22 | 4 | 26 |
| 已知滤波源加b/h | 21 | 23 | 44 |
| 已知滤波源×h加b | 22 | 23 | 45 |

简单仿射源控制仍45/48，而真实实收的时变误差使结果明显更差；幅度/相位分别替换后
均有改善，支持二者共同影响模型的解释，QPSK对照仍明显较差。
不能据此量化二者独立因果贡献、把32/48或26/48当成新接收成绩，或认定物理根因已查清。
removed是固定FIR的互补输出，含信号带外分量、失真及滤波边界响应等，不是纯Wi-Fi/纯LO干扰；
它单独2/48也不能证明模型完全不依赖带外特征。物理来源、多径/硬件非线性与环境干扰尚不能分离。

结论：继续保持未滤波payload和原单位RMS；不加入本轮源辅助校正，不继续在同48条上找最优参数。
下一单元先登记新的24类高源SNR有限先导，使用显式2455MHz、TX80/RX40，保留同步失败和所有分母，
以新行检查当前基线的类别覆盖。全量执行与独立物理SINR校验仍未完成。

### 验证、恢复和保留

8项测试通过，新增测试核对相位/偏置/尺度独立性、固定分块CFO符号/范围与无效no-op、
分量替换和FIR互补输入；原滤波源保真/篡改/批次预算测试继续通过。
864个实际模型输入的SHA-256、shape/dtype/有限性、逐行logits/数字ID及所有计数重核；
两个阶段准备统计/拟合/输入完整重算一致。基线输入哈希及top-1与前次一致，
不同推理序列的FP16 logits最大绝对差0.015625，不能声称所有logits逐位相同。
原60份父级证据逐文件大小/哈希保持；首阶段执行源码已单独封存，后续只扩展分量入口，原算法未改。

冻结epoch-10、FP16 autocast/FP32权重；两次GPU租约均释放，Spark同一PID1321/start_ticks恢复，
两个独立恢复watchdog均停止，无诊断进程残留。未访问NX/P201、没有新IQ/张量副本或生产修改。
两个派生根为`/var/tmp/sdrharness-dev/rml-filter-regression-20260911`及
`/var/tmp/sdrharness-dev/rml-filter-components-20260911`，共保留13份文件1,010,425字节，
用于复核两轮固定计划、预测、源辅助边界、GPU恢复与清理。
收尾删除6份重复输出/空锁7,650字节及5个目录，开发根与两套scratch/GPU锁均不存在；
测试内部自行清理的瞬时流量未计入收尾字节。逐文件路径/SHA-256/source-request-session、
模型/预处理身份和精确人工删除方法见[保留清单](../evidence/RML2018A_FILTER_REGRESSION_2026-09-11.json)。

## 2026-09-11：24类高源SNR有限先导

基线`6fbd815`。用户继续当前执行选择：2455MHz、TX80/RX40、未滤波payload，
从前两类诊断扩展到原始24类，每类固定24条源Z=+30dB，共576条；冻结模型工程对照，
不训练/微调，不作为独立locked-test准入，也不启动全库。

### 预登记及脚本改动

在既有campaign CLI增加`--batch-indices`，允许1–32个不重复批次并保持指定顺序，
不能与非默认连续范围混用或传给plan；连续全库范围及32批GPU分片规则保持。
这使离散类先导可先统一采集，再用一个冻结模型实例识别，避免每类反复加载。
软件身份变化后新建campaign，原plan/失败/诊断记录没有改写。
37项相关测试通过，新增显式列表顺序、越界/重复/空列表/超过32及范围歧义拒绝；
原有限FIFO、同步、SINR、滤波诊断测试保持通过。

完整HDF5 SHA-256及形状/源身份再次核对。固定每类Z30块中点后首个完整24行批次，
直接核对每条Y/Z和X哈希，不看模型输出选行；与现存campaign的72条不同源行无交集。
“新行”仅指未出现在这些已保留campaign source.json成员中，不宣称从未用于历史训练或其他工程实验。
24个批次、全部576行、源哈希及排除依据在首次GO前保存于`pilot-plan.json`。
按原始ID0–23依次执行，类别与时间次序因此混杂，单次每类结果不能当作长期稳定性测量。

新根`/var/tmp/sdrharness-dev/b210-rml2018a-24class-20260911`；rate2.1MS/s、TX/RX BW1.5MHz、
TX峰值0.2、LO offset+250kHz、P201 RX1/RX0/A_BALANCED，用户已确认2.4GHz天线但型号/性能未核实。
每批RX65535个ci16复数262140字节、settle500ms、点timeout1000ms；
最多24次捕获、6,291,360字节IQ，有限TX样本量上限96秒/201,166,848点，单批源包208896字节。
RF阶段deadline900秒、预计含模型约600秒；GPU一片最多1152个源/实收实验窗口加2warmup，
子进程deadline650秒。登记AGX可用空间809,278,160,896字节，逐批再次检查。
每批GO前发出精确NX/P201路径及generation，SIGINT/SIGTERM走专用RX取消与已标识TX owner停止。
同步失败保留并继续固定名单；硬件/原生完整性/恢复错误停止后续射频，不自动重试或改参数。
NX和P201沿用既有技能、严格主机密钥及受保护凭证；没有更换设备制品或生产配置。

### 实际结果

24/24批次均同步成功，576/576条均产生源及实收预测；原生检查无削顶、掉样或溢出。
冻结模型源正确543/576（94.27%），未滤波实收正确207/576（35.94%）。
条件SINR有411条估计、165条invalid，范围−7.467～+5.307dB；未同步/未测0条。
所有invalid行继续进入推理及准确率分母，源Z仍为+30dB，不作为接收端SINR。

| ID / 类别（provisional） | 源正确/24 | 实收正确/24 | 条件SINR估计有效行/24 |
| --- | ---: | ---: | ---: |
| 0 / OOK | 24 | 21 | 20 |
| 1 / 4ASK | 22 | 12 | 17 |
| 2 / 8ASK | 24 | 7 | 6 |
| 3 / BPSK | 24 | 24 | 23 |
| 4 / QPSK | 24 | 7 | 6 |
| 5 / 8PSK | 24 | 8 | 21 |
| 6 / 16PSK | 24 | 0 | 19 |
| 7 / 32PSK | 24 | 5 | 23 |
| 8 / 16APSK | 24 | 4 | 16 |
| 9 / 32APSK | 24 | 6 | 23 |
| 10 / 64APSK | 24 | 0 | 13 |
| 11 / 128APSK | 24 | 1 | 12 |
| 12 / 16QAM | 24 | 0 | 21 |
| 13 / 32QAM | 24 | 1 | 21 |
| 14 / 64QAM | 23 | 1 | 24 |
| 15 / 128QAM | 23 | 0 | 24 |
| 16 / 256QAM | 19 | 1 | 16 |
| 17 / AM-SSB-WC | 15 | 10 | 0 |
| 18 / AM-SSB-SC | 21 | 13 | 0 |
| 19 / AM-DSB-WC | 24 | 20 | 21 |
| 20 / AM-DSB-SC | 12 | 8 | 21 |
| 21 / FM | 24 | 20 | 20 |
| 22 / GMSK | 24 | 20 | 20 |
| 23 / OQPSK | 24 | 18 | 24 |

![24类源与实收对比](../evidence/RML2018A_24CLASS_PILOT_2026-09-11.png)

BPSK24/24、OOK21/24；高阶PSK/APSK及各档QAM多数只有0–6/24，不能用前两类41/48推广全类。
AM-SSB-WC源15/24、AM-DSB-SC源12/24，说明冻结模型在部分源类别上本身也有限制；
不能把所有错误归因于空口干扰。源与实收都使用固定单窗RMS，未增加FIR、源辅助校正或模型调参。
QPSK新24条仅7/24，与前一批17/24的来源/时间不同，不能将差异解释为单因素硬件退化。
这次完成了24类高源SNR工程覆盖，尚无长期/跨日/多SNR重复，也没有事先登记生产准确率准入门；
报告描述实测结果，不追补阈值将其判成生产通过，不完成全量2555904条或独立物理SINR校验。

### 验证、恢复及清理

1152个实际模型输入哈希、24份IQ/元数据/源行/有限TX帧哈希、原同步/CFO、逐行条件质量、
预测logits/数字ID/名称与所有计数重核通过；无额外模型重跑。原始分类矩阵保存在summary及逐批预测中。
冻结epoch-10、FP16 autocast/FP32权重；本轮1152个实验窗口加2warmup，单GPU分片退出。
只在空闲时暂停Spark，最终同PID/start_ticks恢复、独立watchdog停止，GPU租约释放。
最终核对P201同一sdrd PID5909及哈希、原LO/rate/BW、两路RX模式/增益/端口、所有mask/buffer恢复，
healthy=true；NX wheeltec/serial2508504无TX进程/USB持有者，24组两端瞬时路径均不存在。
UHD尾部标记16批为U、8批为S，有限字节全部写完不证明RF时间戳连续性，不能把它写成无发射异常。
原分析/恢复保护未放宽，AGX脚本实际运行，NX仅瞬时暂存helper；Controller/P201/生产模型配置未替换。

保留273份文件9,482,313字节，其中24份原始IQ共6,291,360字节，无源IQ长期副本；
包含计划、行号与排除依据、全部失败/估计无效分母、模型预测、恢复和复核脚本。
逐文件路径/SHA-256、source/request/session、模型/预处理身份、图表哈希及精确人工删除方法见
[先导保留清单](../evidence/RML2018A_24CLASS_PILOT_2026-09-11.json)。
NX自动删除168份暂存文件5,588,424字节；AGX自动删除24份源包5,013,504字节。
收尾删除359份隔离Triton编译缓存、日志、绘图缓存和空锁20,321,999字节，56个目录移除；
开发根、scratch、GPU及owner锁均不存在。测试内自行清理的瞬时流量未计入收尾字节；旧证据不动。
绘图时AMC venv没有matplotlib，改用已安装的系统matplotlib3.5.1，无依赖安装；修正页脚重叠后视觉检查通过。
JSON/AST、文档本地链接、图表数据和Git diff检查通过，生产recognizer_available仍为false。

## 2026-09-11：AGX本机B210天线收发48条

基线`063262f`。用户要求实际发射并确认两边接天线；本次不是衰减器同轴连接。
显式新计划AGX USB、2455MHz、TX80/RX40、2.1MS/s、TX/RX BW1.5MHz、峰值0.2、
TX LO offset250kHz；P201仍为RX1/RX0/A_BALANCED。
复用此前诊断的源行以核验主机迁移后的实际路径，不能把本次当成独立测试集。

执行前登记`pilot-plan.json`，完整数据集SHA-256和运行时/软件身份再次通过：
批次4267（102408–102431，OOK）及22016（528384–528407，QPSK），均Z30，共48条。
每批有限8381952点、321次重复帧，约3.9914秒，总TX上限8秒；
P201每批65535复数ci16，合计524,280字节，settle500ms、每点deadline1000ms。
AGX当时可用809,260,896,256字节，输出预留64MiB；acquire deadline240秒，
推理deadline720秒，最多96个实验输入＋2warmup。精确AGX/P201路径在逐批audit中于GO前登记；
沿用runner信号取消、TX watchdog、逐批唯一所有者和恢复检查，没有扩大增益、重试或改变门限。

| 类别 | 接收/同步 | 原始源识别 | 实收识别 | 条件SINR范围 | 条件SINR中位数 |
| --- | ---: | ---: | ---: | ---: | ---: |
| OOK，ID0 | 24/24 | 24/24 | 24/24 | +0.13～+1.72dB | +1.12dB |
| QPSK，ID4 | 24/24 | 24/24 | 21/24 | −6.76～+2.09dB | +0.38dB |
| 合计 | 48/48 | 48/48 | 45/48（93.75%） | −6.76～+2.09dB | — |

48条全部通过条件SINR估计器有效性检查，invalid/未同步为0；这不是48条都高质量。
QPSK行528392、528393、528394误判为8PSK，其条件SINR分别约−5.38、−5.41、−6.76dB。
错误集中在这段低条件SINR窗口，但不能仅凭此认定Wi-Fi或其他特定物理来源。
两批RX均无报告削顶、丢样、溢出；导频分数约0.888/0.892，CFO约+3.273kHz。
有限TX每批67,055,616字节写完、child正常停止，但UHD尾部分别有`S`/`U`，
仍无RF时间戳连续性证据；不能把本轮记为全库质量通过、独立物理SINR校验或有线验收。
本轮没有主动注入取消故障，不代替AGX实际取消路径验收。

复核脚本只读重算两份IQ/元数据/源帧哈希、同步/CFO和48条质量，
检查96个模型输入哈希、logits数字ID/名称及计数，通过；无额外RF或模型重跑。
冻结epoch10、FP16 autocast/FP32权重、未滤波payload；Controller/P201/生产配置均未替换。
实测P201同一sdrd PID5909及哈希、LO/rate/BW、两路增益模式/增益/端口和mask/buffer恢复，
health健康；AGX B210 USB3无持有进程，两批AGX/P201暂存路径不存在，NX未参与。
Spark PID1321/start_ticks1515恢复，watchdog停止、GPU租约释放，本单元进程退出。

保留32文件922,403字节，其中两份IQ524,280字节，含预登记、预测、恢复与复核记录。
精确路径、SHA-256、source/request/session和人工删除命令见
[本轮证据清单](../evidence/RML2018A_AGX_ANTENNA_PILOT_2026-09-11.json)。
AGX自动删除14份TX暂存466,067字节及2份源包417,792字节；
收尾删除353份缓存/空锁20,143,234字节和54目录，scratch及owner锁确认不存在。
共享数据集与旧实验保留包不动，未新增源IQ长期副本，生产recognizer_available保持false。

## 2026-09-12：同轴衰减、接收幅度与主动停止

基线`4268fde`。用户先确认AGX USB3上的B210 RF A TX/RX，经30dB＋20dB串联衰减器及15cm SMA线接P201 RX1，
随后按停发后的提示取下20dB、只保留30dB。衰减器规格DC–8GHz、50Ω、2W由用户提供，未作仪器校准。
所有操作都由AGX执行，NX未参与。中心2455MHz、2.1MS/s、BW1.5MHz、峰值0.2、TX LO offset250kHz不变；
RX身份RX1/RX0/A_BALANCED，数据集仍共用原文件。

每阶段先记录有限计划、源/软件身份、空间及恢复边界；精确generation和AGX/P201暂存路径在GO前保存。
每个正常TX最多8381952点、约3.9914秒，每次RX65535个复数ci16、262140字节，settle500ms、点deadline1000ms。
共10次正常发送、2次主动取消尝试，TX总上限48秒；另一次停发背景RX。
正常尝试重复同48个源行，全部Z30，不是240条独立源样本；取消另用批次4268并单列失败。
接收/增益分阶段计划和连接确认均保留，不重写旧plan中的历史over-air工程语义字段；本轮物理接法是同轴。

### 幅度、同步与识别

| 衰减 | TX/RX增益 | 实测 |
| --- | --- | --- |
| 50dB | TX0/20/40/60/80，RX20 | 各一批OOK均未同步，分量RMS约1.06–1.10计数、峰值5；TX80标记0.371仍低于0.55门限 |
| 50dB | TX80/RX40 | 两批48条全同步，分量RMS约1.96/2.00；源48/48，实收43/48 |
| 30dB | TX70/RX40 | 一批OOK同步，RMS6.06、峰值21；条件SINR中位数约0.045dB，未达到预设余量，不做模型选档 |
| 30dB | TX80/RX40 | 两批48条全同步，RMS16.67/17.67、峰值56/55；源48/48，实收45/48（93.75%） |

50dB下RX40停发背景RMS1.445计数；接收增益提高后才检测到数据。
减少衰减提高了幅度，但没有消除新增链路误差。30dB最终OOK24/24、QPSK21/24，
条件SINR分别+0.10～+1.64dB、+1.88～+3.07dB，中位数+1.06/+2.34dB。
两组推理共192个实验窗口＋4warmup，使用冻结epoch10、FP16 autocast/FP32权重和未滤波payload。
所有有效RX均无报告削顶、丢样、溢出；失败和取消单列保留，没有降低同步/SINR门或按模型正确率选档。
未达到诊断预设的10dB条件SINR余量，不能把本轮标记为全库质量通过或独立物理SINR校准。

对保留IQ只读作全窗居中标量拟合与固定频带FFT检查，30dB下两类残差的中位能量占比
约98.40%/98.33%落在+250kHz±10kHz；50dB时约46.34%/45.25%。
位置与已设置的TX LO偏移一致，提示LO泄漏/相关杂散需要单独确认；频带位置本身不能确定硬件来源。
这也说明低条件SINR不能全部解释为Wi-Fi背景。该诊断不改原crossfit质量、输入或预测，不新增payload滤波。
192个模型输入哈希/预测计数、源帧和原生IQ/元数据、同步及逐行质量重算通过，无额外模型重跑。

### 停止失败、修正与复测

首次在观察到RX Controller后50ms向runner发送SIGINT：P201恢复、TX进程退出，但helper的
`packet.fifo`残留，原清理只接纳普通文件，因此严格拒绝并未写restored=true。
原失败audit保留；随后核对无进程/USB持有者、恢复状态、源包哈希，以补充记录完成精确清理。

修正为先给已识别TX owner发INT并等待8秒，让helper关闭FIFO/停止UHD/保存回执，超时才分级终止；
helper的有界finally不再被重复停止信号打断。恢复后仅允许删除名为packet.fifo且类型确为FIFO、
经fuser确认无持有者的残留；普通文件allowlist、未知文件/符号链接拒绝和进程消失门保持。
新增TX20/40/60中间档仍只在新plan登记，GO、有限字节、时长及二进制哈希门不变。
47项测试通过，含真实子进程延迟清理、占用FIFO拒绝、未知FIFO/符号链接拒绝及中间档有限GO测试。
新根主动停止复测通过：在TX已开始且RX Controller运行时发INT，runner预期非零退出，
TX/helper/RX进程、暂存及FIFO均清除，P201完整恢复；没有重新发射已完成的识别样本。

最终同一sdrd PID5909及哈希、LO/rate/BW、两路RX模式/增益/端口、所有mask/buffer和health恢复；
B210 USB3无持有者，12组TX/P201暂存及背景暂存不存在，Spark恢复、watchdog退出、GPU租约释放。
UHD尾部S/U仍保留，不能由写完FIFO推定RF时间戳连续性。本轮源码直接执行，生产Controller/P201/模型配置未替换。

保留195文件4,224,203字节：11份非空IQ共2,883,540字节，另有2个取消时的零字节IQ存根及元数据，
包括全部失败、连接/计划、源软件快照、预测、固定频带诊断、停止恢复、测试与清理记录。
开发清理724文件40,334,376字节、115目录；源包12份2,506,752字节；TX暂存84项2,796,460字节（含1个零字节FIFO）。
scratch/owner锁及所有本单元瞬时路径均已核对不存在；原数据集和旧实验保留包不动。
完整路径、source/request/session、软件/模型身份、SHA-256和精确人工删除命令见
[同轴证据清单](../evidence/RML2018A_CABLE_GAIN_2026-09-12.json)。生产recognizer_available保持false。

## 2026-09-12：20dB衰减与匹配增益对照

基线`4d0f143`。用户询问衰减量是否过大，随后明确把30dB换成20dB；当前接法为
AGX USB3 B210 serial2508504 RF A TX/RX → 20dB衰减器及15cm同轴 → P201 RX1。
程序和生产配置不改，沿用已修复停止路径；不重复已完成的主动取消或单测。

执行前登记新两阶段有限计划：先TX70/RX40，名义上与30dB下TX80/RX40保持相近输入；
两批均同步、恢复、无削顶/丢样/溢出且分量峰值≤128计数后，才执行TX80/RX40对照。
固定2455MHz、2.1MS/s、BW1.5MHz、峰值0.2、TX LO offset250kHz、settle500ms、点deadline1000ms。
每阶段批次4267/22016、各48条Z30，共4次有限TX，上限16秒；RX共1,048,560字节。
同48个源行被重复对照，不是96条独立源样本。预留128MiB，初始可用809,219,350,528字节；
每次acquire上限120秒、每阶段infer上限720秒，精确generation/两端暂存路径于GO前记录。

| 衰减 / TX / RX | OOK实收正确 | QPSK实收正确 | 合计 | OOK/QPSK分量RMS计数 | 条件SINR中位数dB |
| --- | ---: | ---: | ---: | --- | --- |
| 历史30 / 80 / 40 | 24/24 | 21/24 | 45/48 | 16.67 / 17.67 | +1.06 / +2.34 |
| 当前20 / 70 / 40 | 24/24 | 23/24 | 47/48（97.92%） | 18.10 / 19.32 | +0.64 / +1.64 |
| 当前20 / 80 / 40 | 24/24 | 21/24 | 45/48（93.75%） | 52.00 / 56.44 | +1.25 / +2.33 |

本轮四批全部同步、96行均有条件SINR估计，原始源识别两阶段均48/48；无报告削顶/丢样/溢出。
TX70的峰值57/60满足已登记升档门，TX80实测170/177。TX70条件SINR范围−0.22～+2.45dB，
TX80为+0.38～+3.11dB。两条预测的差别不足以确定最佳增益，未按模型结果选滤波或冻结参数。

相对历史30dB/TX80，20dB/TX70的总RMS电平高约0.71/0.78dB，参考拟合功率高约0.52/0.50dB，
与增益减10dB、衰减减10dB大致抵消相符；历史比较存在时间差，不能代替功率/衰减校准。
在20dB接法内，TX70→80时参考拟合功率提高9.40/9.60dB，+250kHz±10kHz残余功率也提高8.83/8.96dB。
该频带在残差中的中位占比，TX70为98.49%/97.62%，TX80为99.30%/98.67%。
这支持残余分量与发射设置相关，不能独立确认其物理来源就是LO泄漏；也没有证明继续减衰减能消除它。
固定频带诊断不改原SINR、payload或模型输入，质量门仍未满足，不推进全库或生产准入。

192个实际模型输入哈希、四份IQ/元数据/源帧、同步/CFO、逐行质量、全部预测和计数复核通过，
冻结epoch10、FP16 autocast/FP32权重；共192实验窗口＋4warmup，无额外模型重跑。
历史30dB记录只读，按原保留清单验证文件哈希；其DSP代码与本轮相同，runner停止修正不改变旧IQ解释。
最终P201同PID5909及哈希、LO/rate/BW、两路增益/模式/端口和mask/buffer恢复，health健康；
四组AGX/P201暂存不存在，B210 USB3无持有者；两次Spark同PID/start_ticks恢复、watchdog退出、GPU租约释放。
UHD四批尾部均有S，仍不宣称RF时间戳连续性通过；NX操作和生产部署均为0。

保留69文件1,701,886字节，其中4份IQ共1,048,560字节，含计划、连接、升档门、预测和固定频带对照。
删除开发缓存/锁/重复日志708文件40,302,523字节、108目录；另删除4源包835,584字节及28份TX暂存933,206字节。
所有本单元scratch/owner锁已确认不存在；共享数据集、旧实验IQ及封存记录不动。
逐文件路径/SHA-256、source/request/session、模型/软件身份和精确人工删除命令见
[20dB证据清单](../evidence/RML2018A_CABLE20_2026-09-12.json)。生产recognizer_available保持false。

## 2026-09-12：干净单音与LO跟随分量

基线`3fa610b`。用户要求继续查清低SINR原因，沿用AGX B210 RF A TX/RX经20dB衰减器及15cm SMA
接P201 RX1。新增独立诊断schema/入口，复用有限TX helper与Controller；普通RML的+250kHz、峰值0.2
约束不变。新单音不读取原数据集、不含人为添加噪声，不运行模型或修改生产配置。

登记2455MHz中心、98437.5Hz数字单音、2.1MS/s、BW1.5MHz、TX70/RX40；
正LO、负LO、数字幅度减半、正LO重复4组，前后各一次停数据流背景。
4次TX共33,599,488样本、上限16秒；6次RX各65535点，共1,572,840字节。
登记时可用空间809,213,292,544字节，预留64MiB；settle500ms、每点deadline1000ms、
Controller外层15秒、整体360秒、分量峰值≤512计数。具体generation、请求/会话和两端暂存路径均在GO前记录。

| 条件 | 有效单音频率kHz | LO相关强峰kHz | 有效单音/强峰功率比dB |
| --- | ---: | ---: | ---: |
| LO +250kHz，幅度0.1 | +102.198 | +253.761 | +1.41 |
| LO −250kHz，幅度0.1 | +102.204 | −246.235 | +0.40 |
| LO +250kHz，幅度0.05 | +102.216 | +253.778 | −5.45 |
| LO +250kHz，幅度0.1重复 | +102.228 | +253.790 | +0.63 |

有效单音包括约3.76–3.79kHz两机频偏。LO偏移改变−500000Hz，强峰实际移动−499995.66Hz；
扣除各组单音估计频偏后，强峰相对预期LO位置误差均≤1.25Hz。
正负LO间有效单音功率仅差+0.09dB，说明此强峰跟随的是LO，而非目标单音。
数字幅度减半时，有效单音降5.77dB，强峰反升1.09dB；幅度恢复重复组分别为初始组的+0.18/+0.96dB。
这组证据强烈支持TX LO相关泄漏/杂散主导此前残差，不能继续将其全部归为空口Wi-Fi或数据集自带噪声。
具体模拟器件、数字路径或校准状态尚未定位，也没有完成抑制或调制信号修复。

分析只用前32768点寻找频率，在后32767点积分±1kHz频带；纯单音＋LO拟合用于解释误差，
没有修改原IQ、原SINR估计或模型输入。不同组拟合后仍有不同程度残差，不能按拟合改善量宣称链路已校准。
停数据流的前/后控制中，+254kHz附近仍有较弱峰，频带功率约51.46/6.34 ADC²；
活动正LO组相对两者约高6–7/15–17dB。负LO峰相对两个控制均高约53dB。
因此“进程退出/USB无持有者”不是“射频能量完全为零”的证据，背景记录继续保留，未将其归零。

4项注册源/硬件拒绝/频谱归因测试与44项相关回归通过（含240次既有SINR仿真）。
6份原生IQ/元数据、逐组参数、源字节预算、UHD实际命令/LO偏移、清理和分析重算通过；
独立FFT积分也复核了后半窗频带功率。首次绘图因模型venv缺matplotlib未完成，改用现有系统Python绘图，
无安装、重采或模型重跑。所有采集无报告削顶/丢样/溢出，峰值依次12/37/38/30/38/8计数。

最终P201同PID5909/固定哈希、LO/rate/BW、两路增益/模式/端口和mask/buffer完整恢复；
health健康RX1/RX0/A_BALANCED，全部6组P201暂存和4组AGX TX暂存不存在，B210 USB3无持有者。
本轮实测成功路径恢复；helper停止回归通过，专用入口没有另外增加实机主动取消一轮。
UHD尾部分别U/U/S/U，仍不宣称RF时间戳连续性通过。数据集访问、模型推理、NX操作及生产部署均为0。

删除37个临时文件共323,412字节，包括4个源包32,768字节、28份TX暂存138,008字节、
4份开发缓存/重复日志146,318字节及1份被拆分替代的临时复核脚本6,318字节；开发目录及缓存已验证不存在。
保留60文件1,948,921字节，其中6份IQ为1,572,840字节，另含计划、日志、复核脚本及频谱图。
精确路径/SHA-256、source/request/session、软件身份及人工删除命令见
[单音证据清单](../evidence/B210_CABLE_LO_REFERENCE_2026-09-12.json)。共享数据集和以往封存记录不动。
识别能力仍未准入，recognizer_available=false；下一单元按推进文档登记幅度/硬件增益配对或适用校准对照。

## 2026-09-12：数字幅度与硬件增益配对

基线`e17e8d4`。用户批准继续验证提高数字幅度、降低硬件TX增益的配对方案。
沿用20dB衰减器及15cm SMA，AGX B210 RF A TX/RX接P201 RX1；2455MHz中心、
98437.5Hz单音、LO偏移+250kHz、2.1MS/s、BW1.5MHz、RX40保持。
既有诊断入口新增独立`gain-pair`计划，A=幅度0.1/TX70，B=幅度0.316227766016838/TX60；
数字功率名义+10dB与硬件增益−10dB抵消。普通RML峰值0.2和默认参数不变。

新根登记前后停数据流控制及A1/B1/A2/B2共6次RX、4次TX；最多16秒、33,599,488发射样本，
接收6×65535点ci16共1,572,840字节。登记可用809,209,798,656字节，预留64MiB；
settle500ms、单点deadline1000ms、Controller外层15秒、整体360秒。
每点原生质量和分量峰值≤512计数、P201恢复通过后才继续；两端精确暂存路径和generation在GO前登记。
效果标准预先固定：两次配对各自都须满足有效单音功率变化绝对值≤1dB、LO频带功率下降≥6dB。

| 条件 | 有效单音功率ADC² | LO分量功率ADC² | 单音/LO比dB |
| --- | ---: | ---: | ---: |
| A1：0.1/TX70 | 321.57 | 292.48 | +0.41 |
| B1：0.316227766/TX60 | 329.17 | 53.29 | +7.91 |
| A2：0.1/TX70 | 322.47 | 291.35 | +0.44 |
| B2：0.316227766/TX60 | 332.21 | 54.50 | +7.85 |

两次A→B有效单音分别变化+0.101/+0.129dB，LO分量下降7.394/7.280dB，
单音/LO比改善7.496/7.409dB，均通过预登记条件。说明在这组纯单音条件下，可以保持有效信号电平并减弱LO相关分量；
泄漏仍存在，未达到“完全消除”，也没有把单音功率比改名为调制数据的接收SINR。
评估沿用前32768点估计频率、后32767点±1kHz积分与解释性拟合，无payload滤波或模型输入修改。
前/后停流控制的同频弱峰功率约6.47/1.16 ADC²，继续保留，未假定B210无持有者意味着射频能量为零。

首轮新增测试错误地要求拒绝两个诊断schema共享且合法的基线波形；修正该测试判断后，
6项诊断测试及44项相关回归通过（含240次既有SINR仿真），此时尚未发射。
实机6份IQ/元数据、全部源字节与UHD实际增益/LO命令、逐组参数和恢复、频谱及两次判据重算通过，
独立FFT积分再次复核后半窗功率。无报告削顶/丢样/溢出，峰值依次9/39/29/40/30/6计数。
UHD四组尾部均为U，仍不宣称RF时间戳连续性通过；没有另增实机主动取消测试，沿用既有有限停止路径及回归。

最终P201同PID5909/固定哈希、LO/rate/BW、两路RX增益/模式/端口、scan mask/buffer恢复，
health健康RX1/RX0/A_BALANCED；6组P201暂存与4组AGX TX暂存均不存在，B210 USB3无持有者。
数据集访问、模型推理、NX操作、手动校准操作和生产部署均为0；recognizer_available=false。

清理38文件321,801字节：4个源包32,768字节、28份TX暂存139,828字节，
6份开发缓存/重复日志149,205字节及3个开发目录。保留62文件1,910,708字节，其中6份IQ共1,572,840字节，
另含预登记、首次测试失败及修正后日志、复核脚本、频谱图和恢复依据。
精确路径、source/request/session、软件身份、SHA-256及人工删除方法见
[配对证据清单](../evidence/B210_CABLE_LO_GAIN_PAIR_2026-09-12.json)。共享数据集与旧封存记录未改写。
下一单元验证少量RadioML调制波形的配对效果；不能仅凭单音结果改变全量源幅度、冻结模型或生产准入状态。

## 2026-09-12：RadioML调制波形的幅度增益配对

基线`6b63746`。用户要求继续验证调制波形，保留20dB衰减及15cm同轴，AGX B210 RF A TX/RX接P201 RX1。
新建A/B两个计划，A=标准峰值0.2/TX70，B=峰值0.632455532033676/TX60；固定2455MHz、LO+250kHz、
2.1MS/s、BW1.5MHz、RX40。B使用只接纳批次4267/22016的独立TX包及有限`gain-pair-pilot`配置，
普通全库峰值仍为0.2。导频与payload一起缩放，接收同步、条件SINR、RMS和冻结模型处理保持。

共享HDF5身份校验通过；OOK ID0的102408–102431行、QPSK ID4的528384–528407行均为源Z30。
同48条重复作A/B对照，不是96条独立源样本。数字导出逐行反缩放误差功率比例<8×10⁻¹⁶，源波形未改变。
按OOK A/B、QPSK A/B交替执行，共4次TX，上限16秒、33,527,808样本；4次RX共1,048,560字节。
登记可用809,206,845,440字节，预留128MiB；settle500ms、每点deadline1000ms、每批acquire外层120秒、
每阶段infer外层720秒，分量峰值≤512计数及完整恢复后才继续；精确generation/暂存路径于各次GO前记录。
预登记配对效果目标：两类参考拟合功率中位数变化绝对值≤1dB、+250kHz±10kHz残差功率中位数下降≥6dB、
条件SINR中位数改善≥5dB。原定10dB波形余量目标、同步/估计有效性和生产准入条件不降低。

| 配置 | OOK条件SINR中位数 | QPSK条件SINR中位数 | OOK实收正确 | QPSK实收正确 | 合计 |
| --- | ---: | ---: | ---: | ---: | ---: |
| A：0.2/TX70/RX40 | +0.77dB | +2.08dB | 24/24 | 22/24 | 46/48（95.83%） |
| B：0.632455532/TX60/RX40 | +7.90dB | +9.26dB | 24/24 | 24/24 | 48/48（100%） |

四批均同步，96行均有条件SINR估计，没有invalid/未同步行；两阶段原始源预测均48/48。
B的OOK范围6.91–8.40dB、QPSK范围8.81–10.02dB，只有1/48达到10dB，不能把本轮配对通过说成全部余量合格。
两类参考拟合功率中位数变化+0.43/+0.10dB，固定+250kHz残差频带下降6.82/7.48dB，
条件SINR中位数改善7.12/7.18dB，均通过配对效果目标。源/实收相关度中位数由0.739/0.786提高至0.928/0.946。
B残差仍约94.8%/91.1%集中在该LO频带，泄漏尚未消除。仅两条预测改善不足以确定最优全库参数，
这48条工程结果不作为独立准确率或locked-test准入成绩。

47项campaign/消费者测试与6项单音合同回归通过（含240次既有SINR仿真）。
4份原生IQ/元数据、源行/标签/导出/增益命令、同步、全部逐行质量及频带功率复核通过；
192个实际模型输入哈希、logits/ID/名称映射与汇总计数一致。冻结epoch10、FP16 autocast/FP32权重，
共192实验窗口＋4warmup，无额外模型重跑、训练或输入滤波。峰值按采集顺序为60/55/58/54计数，
无报告削顶/丢样/溢出；UHD四批尾部均U，仍不宣称RF时间戳连续性通过。

最终P201同PID5909/固定哈希、LO/rate/BW、两路增益/模式/端口及scan mask/buffer完整恢复，health健康；
全部4组P201和AGX TX暂存不存在，B210 USB3无持有者。两次Spark均同PID1321/start_ticks1515恢复，
独立恢复watchdog退出、GPU lease释放，owner及GPU文件锁已核对空闲。没有NX操作、手动校准或生产部署，recognizer_available=false。

清理746文件42,420,738字节：开发缓存/锁/重复输出714文件40,636,488字节、111目录，
另有4源包835,584字节和28份TX暂存948,666字节；全部本单元非保留路径确认不存在。
保留72文件1,970,703字节，其中4份IQ共1,048,560字节，另含计划、预测、恢复、复核脚本及对照图。
具体路径/source/request/session、模型/软件身份、SHA-256和人工删除方法见
[调制配对证据清单](../evidence/RML2018A_MODULATED_GAIN_PAIR_2026-09-12.json)。未改写共享数据集或旧封存记录。
后续以新有限计划扩展24类高源SNR覆盖；本次不完成低源SNR、全库执行、物理SINR校准或生产识别准入。

## 2026-09-12：保护间隔辅助LO相消

基线`afb7cdc`。用户要求继续处理且希望提高不止10dB；本单元复用上一轮B组两批原生IQ，
不重新发射、采集或改变接线。父采集为2455MHz、20dB同轴衰减、TX峰值0.632455532/TX60/RX40、
2.1MS/s、BW1.5MHz、TX LO+250kHz；OOK102408–102431与QPSK528384–528407行，均源Z30。
原生IQ、同步、原SINR、源/实收预测及封存字节保持，独立派生根不存IQ/张量副本。

新实验模块以原导频CFO约束LO频率，只从重复帧静默保护区拟合加性单音并相消。
每区剔除首尾各64点，前192点训练、后192点验证，不用源X、类别、Z或模型输出来拟合；
源X仅用于处理后的保真/条件SINR评价。两批分别有3/2个完整保护区，训练与留出各576/384点。
保留原payload时间、CFO、相位及RMS处理；相消器不对payload开陷波或低通。
完整条件、跳过原因与参考面见[接口说明](../reference/RML2018A_FULL_RF_CAMPAIGN.md#保护间隔辅助lo相消离线实验)。

探索阶段的±1500Hz宽搜索失败：保护区预测抑制仍有13.44/11.08dB，但错误选择了约80.42Hz帧率的
频率别名，payload SINR中位数反降至4.70/5.91dB。不能仅以保护区拟合好就称相消成功。
保留原预登记/代码/输出后，另登记以原导频CFO为先验的±30Hz搜索，并加入相消后已知导频两半
相关度≥0.95、剩余CFO≤25Hz检查。这个修正来自稀疏采样歧义，不使用模型预测挑选参数。
频率到边界、留出抑制不足10dB、幅相不稳或导频不确认时原样返回，不删除失败分母。

最终拟合LO频率253759.061/253803.925Hz，留出抑制13.62/13.95dB；
相消后导频残余CFO6.93/6.76Hz、两半相关度0.99886/0.99912，均通过预登记质量条件。

| 指标 | OOK（24行） | QPSK（24行） |
| --- | ---: | ---: |
| 原始条件SINR中位数 | 7.90dB | 9.26dB |
| 后处理条件SINR中位数 | **19.75dB** | **19.36dB** |
| 后处理范围 | 19.06–20.80dB | 18.29–19.95dB |
| 原始→后处理相关度中位数 | 0.92780→0.99529 | 0.94637→0.99499 |
| 源参考拟合功率变化 | −0.0043dB | −0.0010dB |
| +250kHz±10kHz残差频带抑制 | 19.90dB | 23.91dB |
| 后处理剩余误差中LO频带占比 | 16.86% | 4.09% |
| 冻结识别：原始→后处理 | 24/24→24/24 | 24/24→24/24 |

48/48后处理估计有效且均高于18dB，两个中位数均达到本单元预登记≥15dB目标；
仅9/48达到20dB，不宣称全体有20dB余量。原始源预测也48/48，源/原始实收96个输入只复核、未重跑；
新增48个相消后模型窗口及2个warmup，固定epoch10、FP16 autocast/FP32权重，识别仍48/48。
方法冻结后只执行一次模型对照，不按预测再调参数；同48条工程回放不是独立准确率或24类验证。
LO已不占剩余误差的大多数，其他同步/采样时序、信道响应、量化及硬件失真尚未单独归因。

55项campaign/消费者/相消回归通过（含240次原SINR仿真及10个相消噪声种子）；
最终序列化修正后8项相消测试另行通过。已知同频有效payload保留、无泄漏原样返回、
修改payload不影响拟合参数、错误帧率别名先验/频率出界/幅相不稳定均有验证。
首轮仿真的任意0.15Hz参数精度断言过紧，改为直接检查已知泄漏在未参与拟合payload内的残余功率
（抑制>29.9dB）；未放宽运行质量门。首次infer在JSON往返校验时发现tuple/list不一致而拒绝，
尚未获得GPU租约或执行模型；修正序列化、新派生根重新分析/精确复核后才执行上述唯一模型对照。

原生seal、原同步/逐行质量和派生报告逐值复现，96个父模型输入及48个新输入哈希、24类logits/ID和
48/48计数复核通过。模型身份与父记录相同，checkpoint SHA为
`a3c3e41ba9732171d65b023d7d9f1d334d88b7ca28be3d939b0536878c168054`。
记录可用空间809,199,857,664字节（首次推理预检），实际推理外层680秒、内部分类580秒上限；
GPU租约持有约61秒，Spark同PID1321/start_ticks1515恢复，独立恢复watchdog退出、锁确认空闲。
无P201/B210/NX操作、射频状态变更或生产部署，`recognizer_available=false`；新增方法尚未接入campaign采集。

后处理质量使用独立`received_payload_after_guard_lo_cancellation_before_rms`参考面，
保留原Z及相同交叉拟合功率公式，不改变原始SINR，不声称物理RF SINR或硬件校准已提高。
两类既有高源SNR回放不足以证明新采集、高阶调制或低源SNR保真，保护区也不能发现全部payload内部漂移。
下一单元按推进文档登记新有限接入/采集计划，不重发旧两批增益配对或直接运行全库。

精确清理357文件20,480,138字节、56目录；两处scratch及4个非保留重复输出确认不存在。
保留22文件531,924字节，包括失败诊断、预登记、最终报告/预测、图表及恢复/清理清单；新增IQ副本0。
精确父source/capture/request/session、模型/软件身份、逐文件SHA-256及人工删除方法见
[相消证据清单](../evidence/RML2018A_GUARD_LO_CANCELLATION_2026-09-12.json)。
对照图保留于`/var/tmp/sdrharness-dev/rml-lo-cancel-validated-20260912/sinr-lo-cancellation.png`。

## 2026-09-12：相消后剩余误差与导频时序诊断

基线`26f5ab8`。用户要求推进进一步提高SINR。本单元复用上述两批相消父记录及原生IQ，
固定2455MHz、20dB同轴、峰值0.632455532/TX60/RX40等原采集事实；新增RF0、模型0、IQ副本0。
没有重做原配对/相消实验或开启新接收校正，原始及相消后SINR分别仍为既有记录。

预登记9个固定正向模型，每行前512点拟合、后512点评价后交换，48行共432模型/864fold。
对原X统一缩放，中心化只用于拟合；评价保留源DC和全部残差。比较标量、时序一阶导数、
线性复数时间增益、接收镜像、发射镜像、接收DC、三次非线性、固定5抽头信道及组合。
RX镜像按−2CFO旋转，TX镜像位于LO反射的+500kHz；不是简单对所有镜像使用`conj(X)`。
列归一化、`rcond=1e-6`、满秩/条件数≤10⁶；失败保留，不通过增大拟合自由度反复选参。

以下是相对标量基线的**留出误差功率下降中位数**，不是实际接收SINR提高，也不能逐项相加：

| 正向诊断项 | OOK | QPSK |
| --- | ---: | ---: |
| 时序导数 | +0.13dB | **+2.31dB** |
| 线性时间增益/频偏近似 | −0.64dB | −0.11dB |
| RX镜像 | +0.31dB | +0.13dB |
| TX镜像 | +0.10dB | +0.07dB |
| RX DC | +0.08dB | +0.04dB |
| 三次非线性 | −0.01dB | −0.01dB |
| 固定5抽头信道 | −0.21dB | +2.27dB |
| 时序/时间增益/镜像/DC/三次项组合 | +0.52dB | +2.96dB |

432模型均可估计，无秩/条件失败；保留所有变差项。时序项48/48均降低误差，但预设≥3dB
显著解释标准没有在任一类别中位数达到；组合仅QPSK11/24行达到3dB。不能将2.96四舍五入后记为通过。
5抽头信道未提供超过时序项的效果，不支持立即增加均衡复杂度；镜像/三次非线性在这两批中不是主要解释。

时序导数/增益比给出的延迟中位数为OOK0.1235点、QPSK0.3285点；全24行趋势约−1.815/−1.967ppm，
趋势残差RMS0.0174/0.0105点。只用首12行拟合趋势，后12行预测RMS0.0206/0.0190点；
首12行斜率−2.359/−2.997ppm，说明不能把全窗趋势当作精确校准的时钟误差。
标量增益相位趋势给出剩余CFO约−1.64/+13.04Hz，其每1024点窗口贡献有限。
分数延迟、信道群时延与频响拟合偏差仍可能混合，未确认具体硬件时钟的物理ppm。

另只用已知重复导频做独立观察：对参考导频和相消后接收同时施加固定129抽头500kHz检测滤波，
剔除边界，拟合参考及导数，不读payload X。OOK3个导频延迟为0.1950/0.1588/0.1161点，
QPSK2个为0.3622/0.3260点；导频趋势约−1.510/−1.389ppm。预测payload延迟与源辅助诊断
相差0.0213/0.0191点RMS。它提供导频驱动校正的候选依据，尚未实施重采样、校正或新模型识别。

标量基线残差中位数3.458/5.199 ADC计数²，组合后为3.035/2.693计数²；
父记录的静默留出区残差为2.554/2.158计数²，已与组合后同一量级。
静默区残差还包含LO预测误差、杂散和其他附加噪声，不能直接当成校准热噪声或精确噪声底。
原X在±175kHz内的功率中位数均约99.92%；标量残差在该区外的比例为73.79%/44.89%。
这个固定频谱掩码只作统计，本次没有滤波；此前175kHz FIR识别回退的证据保持，不据此自动启用它。

6项已知分量/失败测试通过，含5个时序噪声种子、10个纯加噪种子、RX/TX镜像区分、DC保留、
秩失败/非有限拒绝，以及改变payload不改变导频估计的验证。原生IQ/父库存/相消报告软件、
48个相消输入哈希、原始及后处理质量逐值复核，432模型/864fold和导频结果可精确重放。
首个正式运行因使用fc32导出参考不能逐值复核原X误差基线而拒绝；保留失败日志/计划，
改用原X统一缩放作为全部拟合参考，仍独立验证实际发包哈希，在新根完成验证，未放宽误差复核门。

执行预算每次≤180秒、结果≤16MiB；实际最终JSON744,891字节。使用本单元scratch，
无GPU租约、Spark暂停、P201/B210/NX操作、训练或生产部署，`recognizer_available=false`。
剩余归因单元已完成，不完成导频时序校正、25dB目标、24类新采集、物理SINR校准或全库执行。
下一单元按推进文档验证导频驱动的分数时序校正，带宽/噪声处理仍需单独保真与识别对照。

精确清理3文件544,571字节、2目录，scratch及重复诊断大JSON/输出确认不存在；
保留16文件944,086字节，包含预登记/失败、6项测试、最终全量诊断/导频/图表及清理清单。
具体source/capture/request/session、软件/父profile身份、逐文件SHA-256及人工删除方法见
[剩余误差证据清单](../evidence/RML2018A_LINK_RESIDUAL_2026-09-12.json)。
图表位于`/var/tmp/sdrharness-dev/rml-link-residual-verified-20260912/residual-diagnosis.png`。

## 2026-09-12：导频时序校正与八类新采集

基线`05bfac0`。用户要求继续并多测几个调制；新增独立有限`timing-multiclass-pilot`，
保留旧两批profile。AGX USB3 B210 serial2508504 RF A TX/RX经20dB衰减器及15cm同轴接P201 RX1，
固定2455MHz、LO+250kHz、2.1MS/s、BW1.5MHz、峰值0.632455532、TX60/RX40。
共享HDF5完整SHA-256/布局验证，8类各24行Z30与发包反缩放/标签检查通过；原X/Z和冻结模型保持。
对应批次为4283/22032/26470/57531/66406/75280/93030/101904，具体行号与源哈希见证据清单。

新增仅导频驱动的分数延迟/漂移校正：每个导频前半区训练、后半区只验证；
经最大延迟0.6点、漂移10ppm、拟合RMS0.04点、验证误差0.08点等门后，以129抽头Kaiser8窗sinc插值。
源X、Z和类别不进入接收DSP，原始/LO/时序后的质量分别记录，不把源辅助诊断当作实际校正。
独立16倍过采样合成覆盖正负延迟及漂移，检测到缺失/越界导频原样返回；
带内复数单音失真<10⁻⁸、白噪声功率变化绝对值<0.1dB测试通过。插值并非严格全通，
后处理SINR仍是含链路误差的条件估计，不是物理RF校准值。完整合同见
[八类接口](../reference/RML2018A_FULL_RF_CAMPAIGN.md#八类新采集与导频时序校正)。

8批共192行均完成并同步；单批4秒上限，总TX≤32秒、67,055,616个复数样本，
8份原生IQ共2,097,120字节。每批settle500ms、65,535点、point deadline1000ms，
外层单批120秒、整体1000秒，精确AGX/P201暂存路径与generation在每次GO前记录。
原生接收无报告削顶/丢样/溢出；分量峰值按批次为52/53/51/51/60/54/54/57计数，均低于512实验门。
每批完成正常有限发射退出、同一P201守护进程和完整射频状态恢复后才继续；没有自动重试。

| 类别（每类24行） | 原始SINR中位数 | LO后 | 时序后 | 源正确 | 原始实收正确 | LO后正确 | 时序后正确 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| OOK | 8.33 | 20.25 | 20.38 | 24 | 24 | 24 | 24 |
| QPSK | 8.82 | 18.05 | 21.61 | 24 | 24 | 24 | 24 |
| 8PSK | 9.01 | 21.14 | 21.67 | 24 | 24 | 24 | 24 |
| 16QAM | 8.03 | 18.97 | 19.68 | 24 | 24 | 24 | 24 |
| 64QAM | 7.63 | 7.63（跳过） | 7.63（跳过） | 24 | 5 | 5 | 5 |
| 256QAM | 7.51 | 17.13 | 19.88 | 21 | 2 | 22 | 22 |
| AM-DSB-SC | 8.93 | 19.78 | 21.44 | 14 | 6 | 15 | 15 |
| GMSK | 11.47 | 19.64 | 21.71 | 24 | 24 | 24 | 24 |
| 合计正确 / 192 | — | — | — | **179** | **133** | **162** | **162** |

SINR单位dB，仅对估计有效行取中位数。原始/LO/时序的有效估计行数为192/191/190，
≥20dB行数为0/48/126；AM-DSB-SC行2232733在LO/时序后残差不稳定，2232735仅时序后不稳定，
都保留null/原因和识别分母，没有删去。这7个通过处理的批次中，166条两阶段均有效行的
SINR均提高；另外2条估计无效。时序相对LO在QPSK/256QAM/AM-DSB-SC/GMSK中位数提升约
3.55/2.75/1.67/2.07dB，OOK/8PSK/16QAM约0.13/0.53/0.71dB。

64QAM这批静默留出区LO抑制仅5.98dB，低于原10dB门；残差19.159计数²，
`guard_holdout_not_predictable`导致LO和时序均跳过，三个实收变体使用相同原始输入，24行全部保留。
没有据此降低门、按预测挑选参数或追加重发。仅这一批不足以认定64QAM不可用；
当前更需要检查静默区瞬时扰动/传输时序，不能直接归于调制类型或Wi-Fi。
其余7批时序漂移估计约−1.73～−1.32ppm，留出导频误差最大约0.0066点，全部通过原门。

模型固定epoch10、FP16 autocast/FP32权重，source/raw/lo/timing四组共768个实验窗口及2个warmup，
只执行一次对照。LO→时序在256QAM有1条纠正和1条回退，总数保持162/192；
波形指标提高不自动意味着识别率提高。AM-DSB-SC源本身14/24，说明还存在模型的工程识别限制，
不能把全部错误归为RF。此为同192条的工程对照，不作独立locked-test准确率或生产准入成绩。

66项campaign/消费者/时序回归通过（含240次原SINR仿真）；新schema/源行/增益/预算拒绝、
旧两批profile隔离、插值/缺失导频及payload不参与时序估计均有检查。
8份IQ/元数据/发包/同步、576份逐行实收质量及768个实际模型输入哈希、24类logits/ID/计数精确复核。
checkpoint SHA仍为`a3c3e41ba9732171d65b023d7d9f1d334d88b7ca28be3d939b0536878c168054`。
UHD尾标志按批次为S/S/U/U/S/U/U/U；原生RX无报告丢样不证明TX射频时间戳连续性，本项仍未验证。

最终P201仍PID5909、原daemon/Controller哈希及RX1/RX0/A_BALANCED身份；
LO/rate/BW、两路增益模式/增益/端口、所有scan mask/buffer与预检逐值一致。
8组P201/AGX TX暂存不存在，B210 USB3无持有者；Spark同PID1321/start_ticks1515恢复，
watchdog退出，GPU租约一次获取/释放（约88.22秒），文件锁空闲。无NX操作、训练或生产部署，
普通campaign预处理和`recognizer_available=false`保持；新校正通过独立实验对照入口执行。
原campaign汇总仍描述未完成全库/普通推理，四变体完整结果在关联的实验根，不混写为全库完成。

显式清理开发文件362份20,287,776字节及56目录，加上runner已删除的8份源包与56份TX暂存文件，
共426文件23,862,472字节；本单元scratch、锁和暂存路径均确认不存在。
保留100文件4,624,613字节，其中8份原生IQ共2,097,120字节，无新增IQ副本。
目的、source/capture/request/session、模型/软件身份、逐文件SHA-256与人工删除命令见
[八类证据清单](../evidence/RML2018A_EIGHT_CLASS_TIMING_2026-09-12.json)。
图表位于`/var/tmp/sdrharness-dev/rml-timing-multiclass-20260912/eight-class-comparison.png`。
下一单元优先复核64QAM静默区异常，再补齐其余16类与低源SNR；本轮不完成稳定全库质量或25dB目标。

## 2026-09-12：八类保护间隔异常的离线定位

用户要求继续排查64QAM低于256QAM的原因。本单元仅复用上述八类保留IQ，完成保护间隔残差的
局部分布、频带统计与已知导频时序对照。没有新增RF、源X读取、模型推理、安装或射频配置变更。
代码与父记录起点为`e361dbbfcb32b9b7d830fec305ff97f307d1df81`，分支`codex/sdr-improvements`。

新增`diagnose-rml2018a-guard-anomaly.py`先按父清单验证66份实际读取文件的大小/SHA-256，
再复核8份原生seal、原同步、原LO参数及拒绝时IQ原样返回。标量回放允许1e−12浮点舍入容差，
状态、原因、哈希和全部RF门仍严格一致；不是放宽10dB门。残差为减去原候选LO后的诊断数组，
64QAM候选从未被准入接收处理；不生成替代SINR或识别成绩，不存相消/时序IQ副本。

64QAM（batch66406，源1593744–1593767）的两个完整保护间隔均按原位置拆半：

| 原生样本区间（左闭右开） | 用途 | 候选LO残差功率counts² |
| --- | --- | ---: |
| 25965–26157 | 第一间隔训练半段 | 2.199 |
| 26157–26349 | 第一间隔留出半段 | 2.152 |
| 52077–52269 | 第二间隔训练半段 | 2.160 |
| 52269–52461 | 第二间隔留出半段 | 36.166 |

异常半段约为采集开始后24.890–24.981ms，三个64点块残差分别31.80/36.67/40.02counts²，
较第一间隔留出半段高约12.25dB。两个留出半段合并残差19.159counts²，复现原抑制5.9755dB
和`guard_holdout_not_predictable`。已识别载荷区间为[1325,25901)，此次定位的异常区位于其后；
不能由此证明载荷完全无扰动，也不允许删去该区后宣称旧批通过。

对192点半段作Hann频带积分，绝对频率0–100/100–350/350–700/700–1050kHz均出现功率增加，
异常半段各带功率约9.30/10.68/12.08/5.29counts²；同间隔训练半段约0.245/0.397/0.554/0.888。
窗加权频带和不要求等于未加窗均方值，Parseval归一化已测试。
候选谱峰约−208.15kHz，在此频率的单正弦投影仅解释约2.43%的未加窗残差功率，
支持多个频带的额外扰动，不能归因于单根稳定LO余量；不确认Wi-Fi或具体硬件来源。

在仅供诊断的候选残差上，前三个已知导频训练残差比例约0.00346/0.00378/0.02540，
留出约0.00313/0.00390/0.02297，最后导频也明显变差。导频时间拟合趋势约−1.625ppm，
最大留出延迟偏差约0.00689样本，未见明显整数样本跳变；这些稀疏导频不足以证明其间连续性，
尤其原UHD尾标无事件时间戳，不能把`S`与该异常区直接关联。
该候选时序数值通过原数值门不改变父级`LO_not_validated`或原识别5/24。

八类同法检查中，OOK第三保护间隔的训练半段也有24.09counts²短暂升高，随后留出半段回到2.43；
其原LO留出门仍通过。256QAM保护半段约2.17–2.42counts²，没有此次同样的升高。
这说明异常不是64QAM独有；也暴露仅检查留出功率不能概括每个训练区的局部稳定性。
当前不足以证明调制类型造成差异，或称七类20dB为硬件固有上限。后续施工顺序以DELIVERY_ORDER为准，
先固定增益/衰减做新有限64QAM与256QAM交替复测；任何局部保护间隔方案须另有独立验证，
不能在这批上挑区间或按模型成绩调整门限。

验证：4项独立统计测试通过（局部功率/不修改输入、窗频谱Parseval、正负频率已知单音、坏数据及
回放变更拒绝）；8份原生数据及完整派生JSON重算一致。图表已实际打开核对。
首次使用系统Python时缺`h5py`而停止；改用既有模型venv后完成分析，但该venv缺Matplotlib。
已把数值分析与绘图拆为显式入口，分别使用既有venv和系统Python，无安装；最终分析重新生成并逐值复核，
部分输出随开发缓存清理。没有后台RF/模型进程或硬件暂存，本单元不需要设备停发/恢复操作。

精确删除本轮2文件196,805字节、4目录（含2次空尝试根）；scratch确认不存在。
保留5文件265,318字节：分析JSON、诊断图、4项测试日志、复核收据和清理记录，无新增IQ副本。
根为`/var/tmp/sdrharness-dev/rml-guard-anomaly-20260912`；父source/capture/request/session血缘、
软件/大小/哈希和精确人工删除命令见[证据清单](../evidence/RML2018A_GUARD_ANOMALY_2026-09-12.json)。
原八类证据保持字节不变，生产默认与`recognizer_available=false`保持。

## 2026-09-12：保护间隔LO修复及四批QAM验证

用户明确追问修复是否完成，并要求继续。本单元在`3ca3c678941cea6bb3e7c52e642695bd2717984f`
基础上实现新的保护间隔LO验证方法，再进行64QAM/256QAM交替四批新采集与冻结工程识别。
它修复的是宽带保护间隔残差使整批稳定LO校正被跳过的处理限制，未消除宽带扰动或确定其物理来源。

新`guard-tone-block-margin-v1`复用原训练半段的频率/相量估计，以全部训练和留出半段的
8×24点块相量、均值误差与4倍块标准误差检验预测余量；不剔除异常区、不在留出区重拟合。
余量相对幅度≤sqrt(0.1)，并继续检查相量幅度/相位及独立导频。经验标准误差不是校准置信界，
新相量准则与旧10dB总残差功率门不等价；旧门、旧结果、原始SINR和所有失败不改写。
相消后宽带残差仍保留在IQ和SINR中，方法及限制见[接口](../reference/RML2018A_FULL_RF_CAMPAIGN.md#保护间隔lo块相量验证实验入口)。

75项回归通过，包含240次既有SINR仿真及新40次已知宽带突发实验：接受的相消对已知LO误差
均低于原LO功率的1%，原注入宽带扰动保持；至少20/40次旧门拒绝、新方法通过。
LO跳变、错误稀疏频率别名、过大相关块扰动均拒绝；载荷改变不改变拟合或验证结果。
有限profile的错批次/增益/源行/schema拒绝测试通过。新方法及比较软件在采集前按SHA-256冻结，
没有按新实收或模型预测调参数。

新profile `qam-guard-pilot`只允许4批：66422/75296/66423/75297，类ID14/16/14/16，
新源行1594128–1594151、1807104–1807127、1594152–1594175、1807128–1807151，均Z30，
与旧八类192行不重叠；发包重构和源标签已核验。接法保持AGX B210 RF A TX/RX →20dB衰减器/15cm SMA→P201 RX1，
2455MHz、峰值0.632455532、TX60/RX40、LO偏移+250kHz、2.1MS/s、BW1.5MHz。
每批4秒有限TX、65,535点RX，settle500ms、接收deadline1000ms、单批外层120秒，
总TX上限16秒/33,527,808复样本，总IQ1,048,560字节；AGX空间检查及精确逐批暂存路径在GO前登记。
直接SIGINT/SIGTERM走既有TX owner停止与P201专用generation取消；本次未触发停止故障。

四批全同步，无原生RX削顶、丢样或overflow，ADC分量峰值56/55/56/55（门512）。
本次四批新旧LO均通过，未重现旧保护间隔异常；新旧LO输出/模型输入完全相同。
因此不能把本次64QAM实机恢复全部归功于新准则，不能宣称消除了偶发扰动。
以下SINR为各类48条的条件估计中位数，384个接收质量记录全部有效：

| 类别 | 原始SINR | 旧/新LO SINR | 新LO+时序SINR | 源识别 | 原始识别 | 旧/新LO识别 | 新LO+时序识别 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 64QAM | 7.44dB | 17.68dB | 20.31dB | 46/48 | 9/48 | 41/48 | 40/48 |
| 256QAM | 7.69dB | 19.83dB | 20.03dB | 46/48 | 2/48 | 45/48 | 44/48 |
| 合计识别 | — | — | — | 92/96 | 11/96 | 86/96 | 84/96 |

时序相对LO纠正64QAM行1594142，但回退1594148/1594167及256QAM行1807121，净回退2条，
因此保持实验对照，不作为识别默认；未按这96条调插值器。新源本身也有4条识别错误，不能全部归因于RF。
原campaign汇总保留全库未完成/普通推理未执行；完整五组工程推理在独立比较根，不冒充全库完成。

另在`previous-failure-plan.json`登记对旧batch66406的24行事后回放，无新RF/模型。
复核父seal/源哈希/原始质量后，冻结的新方法保留所有保护半段，其最大相量误差余量0.26725，
通过0.31623经验门；原v1仍拒绝（总抑制5.9755dB）。旧批原始/旧LO SINR中位数7.626dB，
新LO17.603dB（16.82–18.27），加时序20.248dB（19.83–21.29），各24条估计有效。
这直接验证新方法对已知失败输入的修复行为，属于事后回归，不是独立准确率或新实机抗突发证据。
旧5/24预测原样封存，没有为旧批重跑模型或写成通过旧门。

4份新原生seal/同步/源码身份、480实际模型输入/logits/ID/计数及全部质量完整回放通过；
旧失败派生JSON也逐值复核。冻结模型epoch-10、FP16 autocast/FP32权重，checkpoint
`a3c3e41ba9732171d65b023d7d9f1d334d88b7ca28be3d939b0536878c168054`保持。
4批全部恢复LO/采样率/BW、双RX增益模式/增益/端口和全部scan mask/buffer；sdrd PID5909及已装哈希不变。
B210 USB3空闲，所有AGX TX/P201暂存均不存在；UHD尾标U/U/S/S无事件时间戳，RF连续性仍未验证。
Spark PID1321/start_ticks1515恢复，watchdog退出，GPU租约一次获取/释放约77.27秒；锁空闲。
一次postflight命令拼错目录而未运行，纠正精确路径后完整检查通过；没有绕过恢复检查。

精确清理393文件21,948,791字节，其中显式361文件20,160,287字节/55目录，其余为4份源包及28份TX暂存。
保留62文件2,787,209字节，含4份原生IQ1,048,560字节，无新IQ副本；所有本单元scratch/锁/设备暂存已验证不存在。
目的、source/capture/request/session、模型/软件身份、逐文件大小/SHA-256和精确人工删除命令见
[证据清单](../evidence/RML2018A_QAM_GUARD_REPAIR_2026-09-12.json)。
实验入口已实际执行；普通campaign预处理、生产部署和`recognizer_available=false`未改变。
物理扰动来源、剩余16类/低源SNR覆盖、全库稳定性与物理SINR校准仍未完成，不宣称全面修复或生产准入。

## 2026-09-12：固定500kHz滤波与时序对照

用户要求继续改善64QAM低于256QAM及约20dB的质量。本单元从`85aefdbd997540b957e5dc4f8358ac18a1eca058`
出发复用上述4份原生IQ与96条源，先诊断时序后的识别回退，再执行单个预先固定的500kHz低通对照。
无新RF、无增益/衰减/硬件状态变更，不重发旧四批，也未修改时序插值器。

首先复核三条时序回退：1594167的原始源本来就被判为256QAM，时序后回到同一错误类别；
另两条1594148、1807121的原LO正确类别相对最大竞争类别logit余量分别仅0.453/1.109，时序后−1.008/−0.320。
余量是模型原始logit差，不是校准概率。不能据这三条直接认定插值器破坏波形。
对全部96行，仅作归因的源辅助内部标量拟合显示，时序后误差减少，窗口边缘不是唯一误差来源；
四批剩余残差在700–1050kHz频带的Hann积分中位数仍约1.07–1.16counts²，接近整个残差的显著部分。
该诊断拟合不是新的SINR，也不把拟合源用于模型输入，不确认该残差的物理器件来源。

在新`plan.json`中固定一个候选：129抽头Kaiser8、截止±500kHz、DC增益归一化，整个已做LO/CFO校正
的缓冲先滤波再提取载荷，使用实际两侧64点，不逐行补零/循环拼接。组合变体保持原时序估计和坐标，
不根据新结果重新拟合导频或修改插值参数。原X/Z保持条件SINR参考，滤波/时序平面单列。
96条源保真控制使用相邻真实源行、原TX尺度和帧保护间隔；全部通过预定相对误差≤0.003、功率比0.995–1.005门。
实际最大源相对均方误差0.00089056（0.0891%），功率比范围0.998991–1.000199。
源保真数值通过后才执行288个新模型窗口加2warmup；固定候选在结果出来后没有调参或再次推理。

| 类别（每类48条） | 原LO SINR | 仅时序SINR | LO+滤波SINR | LO+滤波+时序SINR | 原LO识别 | 仅时序识别 | LO+滤波识别 | LO+滤波+时序识别 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 64QAM | 17.68 | 20.31 | 19.06 | 22.41 | 41 | 40 | 38 | 40 |
| 256QAM | 19.83 | 20.03 | 21.83 | 22.03 | 45 | 44 | 46 | 46 |
| 合计识别/96 | — | — | — | — | 86 | 84 | 84 | 86 |

SINR均为有效条件估计中位数/dB，两个新实收变体192行全部有效。组合变体64QAM范围17.43–24.00dB，
256QAM20.47–23.17dB，不能把中位数描述为每条都超过22dB。
源原始/源滤波均92/96，每类46/48，所有原来正确的源行保持正确。
相对LO，单独滤波使64QAM的1594134/1594167/1594175回退，256QAM的1807134纠正；
组合变体使64QAM1594148/1594167回退、1594142纠正，256QAM1807134纠正。
相对旧时序，组合变体64QAM无正确性变化，256QAM纠正1807121/1807134，无新增回退。
这支持滤波降低了当前剩余误差，说明约20dB不是由20dB衰减器数值决定的硬上限；
但总识别86/96不等于各类均改善，尤其64QAM仍低于原LO41/48，不默认启用或按真实类别选择处理。
这批已用于诊断，结果是事后工程比较，不是独立准确率；需要新样本确认，不能在相同96条上继续选参数。

3项独立滤波测试通过：DC/对称性、±175kHz已知通带波形保真、±800kHz阻带与白噪声功率增益、
跨窗口真实相邻样本卷积行为。父62份保留文件哈希及原prepared重现通过，
288实际新模型输入/logits/ID/计数、192质量记录和96条诊断记录逐值回放通过，没有复跑模型。
冻结模型身份与父比较完全相同，FP16 autocast/FP32权重；GPU租约一次获取/释放约71.70秒，
Spark PID1321/start_ticks1515恢复、watchdog退出、锁空闲。没有本单元硬件进程或P201暂存。

精确删除353文件20,139,321字节/54目录，scratch及owner锁均不存在。
保留14文件854,884字节，包括固定计划、源保真/质量、模型结果、诊断/回放、恢复/清理与诊断脚本；
无新IQ或IQ副本，旧证据保持原字节。身份、逐文件大小/SHA-256及人工删除命令见
[证据清单](../evidence/RML2018A_FIXED_WIDEBAND_2026-09-12.json)。
新比较入口实际离线执行，普通campaign及生产配置未安装，`recognizer_available=false`保持。
接收增益/ADC余量、噪声与器件来源尚未分离；后续有限选择见DELIVERY_ORDER。

## 2026-09-12：RX40/RX50配对实机与识别对照

用户同意固定TX60、当前20dB同轴接法，比较RX40与RX50。本单元从`006a5ef74c6659424f7c10a0b25f34e63daf7e5d`
开始，新增独立`qam-rx-gain-pilot`，其他有限profile保持RX40限制，普通campaign预处理/生产未部署。
新源batch66440/75314/66441/75315，类14/16/14/16，各24条Z30，共96唯一源行，
与旧八类及旧QAM源行无重叠。源行分别1594560–1594583、1807536–1807559、1594584–1594607、1807560–1807583。
两档使用相同源X/标签/尺度；run ID和导频不同，因此独立同步，不能说发射IQ逐点完全相同。

采集顺序为(40,66440)、(50,66440)、(50,75314)、(40,75314)、(50,66441)、(40,66441)、
(40,75315)、(50,75315)，两类各含一次AB与BA顺序。2455MHz、峰值0.632455532、TX60、
LO偏移+250kHz、采样率2.1MS/s、模拟BW1.5MHz、每点settle500ms及65,535样本/1000ms deadline。
每次TX≤4秒/外层120秒，总TX≤32秒/67,055,616复样本，8份IQ共2,097,120字节；
两根campaign各限制4批，空间与AGX/P201精确暂存路径在GO前登记，SIGINT/SIGTERM走原有限TX停止及generation取消。
计划还预登记LO后≥1dB配对中位收益作为有用改善参考，所有失败保留，不按结果重试。

81项回归通过（含240次SINR仿真），新测试覆盖RX50仅属于此有限profile、错误增益/主机/批次/预算/schema拒绝，
及新源行不重叠。8次全部同步、原生RX无削顶/丢样/overflow。按批次顺序RX40峰55/56/52/52，
RX50峰163/160/156/172，均低于512门。有效源预测功率配对中位上升64QAM9.73dB、256QAM9.96dB，
与设置增加10dB一致；不能把TX增益解释为实际dBm。

各类48对样本，以下为每档条件SINR中位数/dB；最后一列为逐条差值的中位数，并非前两列直接相减：

| 类别 | 接收处理 | RX40 | RX50 | 配对提升中位数 |
| --- | --- | ---: | ---: | ---: |
| 64QAM | 原始 | 7.89 | 7.76 | −0.16 |
| 64QAM | LO | 19.05 | 21.02 | +1.92 |
| 64QAM | LO+时序 | 20.01 | 22.18 | +2.28 |
| 64QAM | LO+500kHz+时序 | 21.82 | 22.70 | +1.11 |
| 256QAM | 原始 | 7.49 | 8.03 | +0.52 |
| 256QAM | LO | 18.89 | 21.24 | +2.88 |
| 256QAM | LO+时序 | 19.80 | 23.63 | +3.77 |
| 256QAM | LO+500kHz+时序 | 21.84 | 24.26 | +2.41 |

新旧LO本次都通过，输出相同；五个接收变体×192接收行共960质量记录全部估计有效。
LO后4批的配对提升中位数分别+3.53/+2.46/+0.76/+3.90dB；64QAM第二对不足1dB，
但两对时序后分别+2.13/+2.43dB，说明分数采样相位也会影响只做LO时的对照，不只由增益决定。
LO后残差绝对功率随增益上升约7.61/6.84dB，比源功率约10dB上升更少；
支持低RX增益下接收链路噪声余量限制，尚不能唯一定位为ADC量化、热噪声或某级电路。
原始SINR几乎不改善符合LO分量也随接收增益一起放大，不能省略LO校正。

每档6组（source/raw/旧LO/新LO/timing/wide_timing）、576窗口+2warmup，GPU顺序运行。
模型固定epoch-10、FP16 autocast/FP32权重，两档源输入与全部源logits相同；冻结源识别均92/96，每类46/48。

| 类别 | 原始 RX40→50 | LO RX40→50 | LO+时序 RX40→50 | LO+500kHz+时序 RX40→50 |
| --- | --- | --- | --- | --- |
| 64QAM（48条） | 19→13 | 41→44 | 43→44 | 40→44 |
| 256QAM（48条） | 4→8 | 39→46 | 40→46 | 41→46 |
| 合计（96条） | 23→21 | **80→90** | 83→90 | 81→90 |

LO配对纠正64QAM1594565/1594590/1594598，以及256QAM1807536/1807540/1807547/1807549/
1807553/1807555/1807571，10条纠正、无新增回退。
RX50内部，组合相对LO纠正1807552但回退1807558，总90/96不变，不因SINR更高默认采用组合。
RX50有3条源本来正确而LO实收错误，同时有1条源错误而实收正确；92与90之差不是独立分离的“两条硬件错误”。
后续有限实验优先参考TX60/RX50+LO，当前物理连接/衰减器不变；停发后仍恢复原射频状态，
不把实验参考写成已持久安装或生产识别准入。

8份原生seal/同步/所有DSP、1152模型输入/logits/ID/计数及配对汇总全部回放复核，源logits跨档逐值相同，
没有复跑模型。新固定滤波源保真门通过，组合还检查真实128点两侧空间；本次无跳过。
完整恢复LO/采样率/BW、双RX增益模式/增益/端口和所有scan mask/buffer，sdrd仍PID5909且哈希不变；
B210 USB空闲、8组TX及P201暂存不存在。按实际采集顺序UHD尾标S/S/S/S/U/S/U/U，无时间戳，RF连续性仍未验证。
两次GPU区间不重叠、租约各获取/释放一次，Spark PID1321/start_ticks1515恢复、watchdog退出、所有锁空闲。

精确清理780文件44,026,660字节，其中显式716文件40,446,172字节/111目录，其余8源包及56TX暂存；
scratch及owner锁均不存在。保留105文件5,914,140字节，包含8份IQ2,097,120字节，无新增IQ副本。
source/capture/request/session、软件/模型/配置、逐文件大小/SHA-256和人工删除命令见
[配对证据清单](../evidence/RML2018A_RX_GAIN_PAIR_2026-09-12.json)。旧证据原字节保持。
本单元没有NX操作、训练或生产部署，`recognizer_available=false`；仍缺其余16类、低源SNR和全库稳定性验证。

## 2026-09-12：其余16类RX50高源SNR有限覆盖

用户继续授权后，从`19804bef2dc8b5f34060bfddd4cd7d2413832064`开始，新增有限
`remaining-high-snr-pilot`补齐前次八类之外的16类；各24条新Z30源行，共384条。
原始HDF5哈希及X/Y/Z成员已核对，与前次八类行不重叠，仍是含历史划分成员的工程比较。
沿用AGX USB3 B210 RF A TX/RX→20dB衰减器及15cm同轴→P201 RX1；2455MHz、
数字峰值0.632455532、TX60/RX50、TX LO偏移+250kHz、2.1MS/s、BW1.5MHz。

新profile只允许登记的16批、AGX及TX60/RX50，每批4秒有限TX、65,535复数RX，
总TX上限64秒/134,111,232复样本，原生IQ共4,194,240字节。
采集前登记空间、精确暂存路径、settle500ms、每点1000ms deadline、单批外层120秒及总采集1000秒；
停止沿用SIGINT/SIGTERM与Controller generation取消。比较只执行`source/raw/guard`三组，
最多1152模型窗口+2warmup，内部650秒/外部680秒；没有增加时序或滤波变体，也未按模型预测调参。

84项回归通过（含240次SINR仿真）；覆盖16类与旧八类的互补、精确源行/发包、
TX60/RX50/AGX/有限批次预算及错误参数拒绝。16批全部同步，原生接收无削顶、丢样或overflow，
峰值156–270counts，低于512门。UHD尾部S/U原样保留，不将RX无丢样等同于发射时间戳或全程连续性通过。

冻结epoch10、FP16 autocast/FP32权重识别：源369/384（96.09%）、原始实收319/384（83.07%）、
保护间隔LO校正350/384（91.15%）；相对原始纠正41条、回退10条，全部失败保留分母。
以下各类分母均为24；SINR中位数只计算该组有效估计，不能把无效条目当作零值或漏收。

| 类别 | 源正确 | 原始实收正确 | LO组正确 | LO执行 | SINR有效条数 | LO组SINR中位数/dB |
| --- | ---: | ---: | ---: | --- | ---: | ---: |
| 4ASK | 22 | 15 | 22 | 应用 | 24 | 18.78 |
| 8ASK | 24 | 24 | 24 | 应用 | 23 | 22.05 |
| BPSK | 24 | 24 | 24 | 应用 | 24 | 19.46 |
| 16PSK | 24 | 24 | 24 | 应用 | 23 | 24.60 |
| 32PSK | 24 | 20 | 20 | 跳过 | 24 | 9.71 |
| 16APSK | 24 | 23 | 23 | 应用 | 23 | 19.86 |
| 32APSK | 24 | 22 | 23 | 应用 | 22 | 18.03 |
| 64APSK | 24 | 23 | 23 | 应用 | 24 | 22.07 |
| 128APSK | 24 | 16 | 24 | 应用 | 24 | 20.43 |
| 32QAM | 24 | 16 | 16 | 跳过 | 24 | 8.29 |
| 128QAM | 23 | 2 | 23 | 应用 | 24 | 21.47 |
| AM-SSB-WC | 15 | 24 | 16 | 应用 | 14 | 14.65 |
| AM-SSB-SC | 21 | 18 | 21 | 应用 | 8 | 16.58 |
| AM-DSB-WC | 24 | 24 | 23 | 应用 | 24 | 19.03 |
| FM | 24 | 20 | 20 | 跳过 | 23 | 13.31 |
| OQPSK | 24 | 24 | 24 | 应用 | 24 | 22.73 |

13批通过保护间隔LO校正，32PSK/32QAM/FM的`guard_tone_error_margin`未通过，
保留原始载荷及跳过原因，旧v1也因`guard_holdout_not_predictable`拒绝。三批最大相量误差余量
分别1.19920/0.58659/1.55177，门仍为sqrt(0.1)≈0.31623；最差留出半段残差分别
2262.29/580.25/3665.51counts²，位于[1673,1865)、[1848,2040)、[54946,55138)。
这不是轻微擦边失败，没有放宽门限或另挑成功片段。三批约9.71/8.29/13.31dB为跳过后的原始估计。

已用同批原生IQ做事后已知导频定位，另选BPSK作对照，无新增RF、模型或源X读取：
129抽头Hamming500kHz FIR后按原CFO旋转；已知marker取三次平铺的中间副本滤波，
用[64,448)及[576,960)点的中心化归一相关，在预期marker位置±128整数点搜索。
四批各三个marker的最佳偏移均为0；32PSK首导频相关0.90590、FM末导频0.87649，
与相邻保护区异常相伴。只说明这些局部搜索未见明显整数错位，不证明导频间连续性，
也不能确认Wi-Fi、TX欠载或某器件为原因。数值及父IQ哈希见保留包`guard-pilot-localization.json`。

原始条件SINR360/384有效、24无效（21增益两半不稳、2残差不稳、1参考不可辨识）；
LO组352/384有效、32无效（19增益不稳、12残差不稳、1参考不可辨识），均进入识别。
AM-SSB-WC源15/24、原始24/24、LO16/24，AM-SSB-SC源21/24且LO后仅8条SINR有效；
模型源域误判与标量两半估计适用性需要单列，不能全部解释为链路噪声或校正损伤。
当前SINR仍是源功率分配/标量链路假设下、含新增失真的条件估计，不是校准级物理SINR。

1152模型输入哈希、全部原生IQ/同步/质量和summary复算通过，没有复跑模型；
源码哈希与实际执行计划一致。前八类/QAM修复/固定滤波/增益配对四份父清单共281个文件哈希不变。
交付校验首次假定父清单统一含`retained_roots`，固定滤波清单实际使用`retained_files`；
按其既有schema修正复核脚本后通过，未改写父证据。
P201完整射频/两路RX/scan mask/buffer恢复到登记前状态，sdrd PID5909及Controller制品哈希不变，
RX1身份健康；B210 USB无人持有，本单元AGX/P201瞬时路径均不存在。
推理已退出，Spark PID1321/start_ticks1515恢复、watchdog停止、GPU/owner锁核对空闲后清理。

精确删除490文件27,392,287字节（含16个源包及112个TX暂存文件），清理55目录；
scratch及owner锁均不存在。保留182文件7,889,235字节，其中16份原生IQ共4,194,240字节，
其余为有限计划、原始审计、模型结果、质量、定位诊断、测试及恢复/清理依据，没有IQ副本。
逐文件大小/SHA-256、source/capture/request/session、模型/profile身份及精确人工删除命令见
[证据清单](../evidence/RML2018A_REMAINING16_2026-09-12.json)。

本单元有限profile已实际运行；普通campaign新LO预处理及生产识别仍未安装，`recognizer_available=false`。
与旧八类合起来覆盖了24种调制，但旧八类使用RX40且处理组合不同，不能合并成同条件24类准确率。
未完成三批局部异常归因、AM估计适用性、低源SNR分层或全2555904条执行；下一单元见DELIVERY_ORDER。

## 2026-09-12：其余16类保护区异常的离线定位

从`01668bb265ca79b4db2d0017f2cfea3221d77f43`继续，复用上一单元32PSK35414、32QAM62038、FM97536
及BPSK17664对照，共4份原生IQ（读取1,048,560字节），并核对全部16批TX日志。
扩展既有`diagnose-rml2018a-guard-anomaly.py`识别剩余16类父schema，但只处理上述固定4批。
没有新RF、源HDF5读取、模型推理或生产修改，也不重复已完成的±128点导频搜索。

原生seal、原同步、旧LO和保护区相消结果逐值复算，三个拒绝仍返回原始IQ。
使用原训练段已拟合的固定LO频率/相量形成候选残差，仅作归因，不输出校正IQ、SINR或模型输入。
保留每段192点训练/留出半窗、64点分块功率及四个绝对频率区间的Hann积分谱；
另外描述候选残差在固定LO频率上的相干投影和互补残差，不把局部投影当作可部署校正。
对每个保护区/导频周围[-640,+1280)点，用192点Hann、64点hop生成全部28窗，
四批共12个区域，无排序/挑段；图上导频与载荷的能量不称为背景噪声。

| 类别 | 各保护区训练/留出功率（counts²） | 最大/最小半段比值 | 异常位置与选定载荷关系 |
| --- | --- | ---: | --- |
| BPSK | 7.78/8.39；13.28/14.24；8.65/9.69 | 2.62dB | 对照，无上述量级起伏 |
| 32PSK | 2256.03/2262.29；1060.63/1073.75；15.68/16.22 | 21.59dB | 前两保护区位于载荷[29065,53641)之前 |
| 32QAM | 623.67/580.25；6.25/6.88；6.14/6.04 | 20.14dB | 第一保护区位于载荷[29240,53816)之前 |
| FM | 16.45/16.06；12.98/12.37；3591.87/3665.51 | 24.72dB | 第三保护区位于载荷[30114,54690)之后 |

这些比值描述同批保护区的起伏，不是标定背景或新SINR值，也不能证明相邻保护区之间一直异常。
异常半段在固定LO频率上的相干投影只占其候选残差0.015%～1.92%，
全频段搜索的最强单音投影也不超过8.28%。能量主要分散于0–100、100–350及350–700kHz三个绝对频率区间，
700–1050kHz区间相对较弱；例如32PSK最差留出段四带功率613.0/1116.5/515.2/4.6counts²，
FM对应819.0/1856.4/932.0/4.1。积分谱使用Hann加权，因此其和不要求等于未加窗时域功率。
支持这些失败包含明显宽带残差，不能仅靠进一步去掉固定LO窄带分量解释或消除；
但尚不能区分发射波形瞬态、外来信号、接收内部因素或传输问题，也不认定是Wi-Fi。

全部16批TX记录均为`fed_complete`、子进程退出0；这只证明有限数据已送入工具并正常退出。
UHD尾部`S`出现4批（2通过/2拒绝），`U`出现12批（11通过/1拒绝），没有一一对应关系。
TX摘要只有主机GO时间，文本尾标记没有逐事件设备时间戳，也没有与P201采样的共同时间锚；
不能把尾部顺序视为事件发生顺序，或把送样完成当作射频全程连续性证明。
现有证据不足以独立确认物理来源，后续先补事件可观测性，再做有限对照。

7项独立测试通过：已知分块突发定位、Hann谱Parseval、已知单音、固定LO/正交另一单音的9+25=34功率分解、
全部区域/窗覆盖与谱归一、输入无修改及无效输入/回放变化拒绝。
全部诊断数值、同步与相消拒绝重放通过；父182份文件大小/SHA-256保持，图已人工检查。
无本单元硬件/GPU所有者或部署，原模型结果和生产能力保持，`recognizer_available=false`。
清理1份缓存123,432字节、2目录，scratch不存在；保留11份3,098,584字节的计划、统计/图、
摘要、测试/复核及清理脚本/记录，无IQ副本。逐文件哈希、父血缘和精确删除命令见
[证据清单](../evidence/RML2018A_REMAINING_GUARD_DIAGNOSTIC_2026-09-12.json)，
[分段频谱图](/var/tmp/sdrharness-dev/rml-guard-anomaly-remaining-20260912/guard-residual.png)只在需要复核时读取。

## 2026-09-12：配置复现归档与论文证据范围

用户要求完整记录当前配置便于复现，并讨论同轴/空口实验在调制识别论文中的作用。
以干净HEAD`403119426d5c0873527d39633956439fde59caf1`为来源新增
[复现与论文说明](../reference/RML2018A_RF_REPRODUCTION.md)和
[版本化配置快照](../evidence/RML2018A_RF_REPRODUCTION_SNAPSHOT_2026-09-12.json)。
快照汇集20dB同轴接法、2455MHz/TX60/RX50/峰值0.632455532、帧与同步/校正合同、
源行、有限预算、checkpoint/profile/标签、UHD运行时、软件哈希、旧恢复基线及全部优化父证据入口。
本次核对8份本机B210运行文件、libuhd、epoch010 checkpoint、已安装Controller及实际执行脚本哈希，
并只读采集venv包版本；原HDF5只核对大小，沿用封存计划SHA，没有通读源X/Y/Z。
P201恢复状态来自最近已验证postflight，本次没有SSH或重新探测硬件，不称实时状态。

文档区分普通campaign默认与有限profile、已执行实验与生产安装、逐单窗工程调用与RF-v1四窗身份，
提供已封存输入/预测只读复核命令与新RF实验的复现前提。
研究建议采用离线原IQ、衰减同轴硬件回放、独立空口场景三层证据；
已知导频/保护区假设、源辅助条件SINR、源成员和派生隔离及完整失败分母均明确，
不把现有工程样本解释为独立未见样本成绩，不承诺SDR使用本身具备算法创新性。
旧实验结果、准入配置与所有已封存附件保持原字节，当前后续硬件单元顺序不变。

配置引用与哈希、文档链接、事实/范围和diff核对完成；纯文档及配置快照变更，
新增RF0、模型推理0、训练0、硬件/生产部署0、临时文件0、IQ副本0。
无本单元需清理的进程、socket或开发目录；既有运行资产和实验保留包按原删除清单保留，未清理旧证据。

## 2026-09-13：带时间记录的B210发送与停流全零对照

用户批准先补发射事件记录并做短时对照。本单元从`7cd2adee19cadf23cb5f3934f30e7b3ac432575b`开始，
新增实验源码`devices/b210/tx-events.cpp`和`validate-b210-event-controls.py`；复用既有P201 Controller、
原生IQ校验、单实例/身份/恢复检查，没有更换普通campaign TX helper或生产UHD运行时。
当前连接仍是B210 RF A TX/RX→20dB/50Ω/2W/DC–8GHz衰减器＋15cm SMA→P201 RX1，
2455MHz、TX60/RX50、峰值上限0.632455532、LO偏移+250kHz、2.1MS/s、BW1.5MHz。

AGX没有UHD开发头文件。配置镜像TLS下载失败后，从Ubuntu ports取得同版本libuhd-dev4.1.0.5-3/arm64，
SHA-256 `88c2efdee1c3351ac95bc1a2b99efcdd460955aef3ec8c11f13521eb2f44bc01`与apt元数据一致，
仅在本单元目录解包头文件，直接链接既有libuhd。首次编译的显式device_addr转换和缩进告警已修正；
最终C++14/O2/Wall/Wextra/Werror/pthread编译通过，没有系统安装或库/镜像替换。

新入口固定serial2508504/channel0/A:A/TX-RX和上述RF参数，检查回读和LO锁定；
必须收到精确GO才发送样本，首次发送安排在查询到的设备时间后0.2秒。
输入仅一帧26,112个fc32样本，峰值受限，循环321次、最多8,381,952复数/约3.9914秒。
逐send保存请求/接受样本数、偏移及AGX单调时钟前后时间，正确处理部分发送、零进展和deadline；
异步接收线程保存UHD事件码、接收时的AGX单调/Unix时刻，以及设备提供时的设备时间。
日志先在内存中有界累计，发送后写出，避免每次send同步写磁盘；两种记录各不超过20,000条。
成功发送EOB后等待1秒收集尾事件，失败/取消也尝试有界EOB与线程退出；外层还有65秒INT/强停保护。
没有设备时间的事件明确为null，未识别事件码原样保留。

计划先停流RX，再零波形主动取消，之后zero1/known1/known2/zero2/停流RX。
4次完整发送加1次主动取消的总TX预算20秒/41,909,760复样本；6份RX各65,535点，共1,572,840字节，
settle500ms/每点1000ms、RX外层15秒、整体400秒，空间预留128MiB；登记时可用809,179,254,784字节。
每次AGX/P201精确路径、单一所有者、停止路径和恢复基线在计划/审计中登记。
零波形是全零fc32；known是固定seed20260913、4点/符号的合成QPSK，加本run已知导频和首尾保护区，
两次known的发射包相同。它不是RadioML源样本，没有HDF5读取、模型推理或SINR准入。

7项C++离线测试和6项Python波形/日志测试通过，覆盖部分发送、无进展、超时、取消、非法接受数、
错误偏移/时间、缺失设备时间、未知事件保留及有限零/QPSK源。
实机主动取消在tx_start后约250ms发SIGINT，记录152,064个已接受样本，进程退出1、原因为TX cancelled，
未把取消伪装成完成；B210 USB恢复空闲。4次完整发送各记录8,346次send/8,381,952接受样本，
累计接受33,679,872个样本（不是独立测量的实际辐射样本数）。

4次完整发送都仅收到1次BURST_ACK，并带设备时间；没有报告underflow、sequence_error或late_packet。
本轮不足以证明旧S/U全部来自原FIFO程序或异常已永久修复，发送实现与输入均已变化。
实测clock_source/time_source为internal，主时钟约33,600,000.12Hz，回读采样率约2,100,000.00748Hz。
设备时间查询前后AGX时刻已留存，但P201报告没有硬件首样本时间戳，因此只能给出TX名义时间位置、
RX命令的主机时间包络，不能把UHD事件精确映射到P201第几个样本或宣称全程连续性。

6份原生RX均无削顶/丢样/overflow，峰值按采集顺序为139/128/202/213/129/86counts，均低于512门。
两次known均同步，保护区LO校正通过，保护半段候选残差分别约11.08–16.27与16.09–22.18counts²。
这只是两份合成QPSK的结果，不能代替原来三批失败或全24类验证。

停流/全零控制都仍见宽带尖峰。初版描述性分析按FFT整数频点减单音，会留下频率量化导致的拍频，
其残差中位值不适合当稳定背景；原analysis保留，另保存版本化fine-analysis。
修正只在前32,768点内将同一个单音模型细化频率、后续分块统计，已通过非整FFT频点单音的独立数值测试；
256点Hann积分排除LO±25kHz，再合计其余0–100/100–350/350–700/700–1050kHz绝对频带。
所有原生IQ、原始估计和已接受校正不改写，下面是描述性非LO频带功率，不是物理噪声底或SINR：

| 控制 | 非LO功率中位数/counts² | 最大块功率/counts² |
| --- | ---: | ---: |
| 停流前 | 5.39 | 1894.77 |
| zero1 | 5.90 | 2195.73 |
| zero2 | 7.13 | 2144.78 |
| 停流后 | 5.45 | 563.96 |

known的载荷本身有能量，不用它的总残差冒充背景。停流仍保留上电B210和同轴连接，不能称射频零能量。
以上说明调制payload并非当前尖峰的必要条件；也没有本轮UHD欠载事件可与尖峰对应。
不能据此确定Wi-Fi、接收器内部、发射器模拟部分或其他耦合来源。
事后从四份控制各选相隔至少2,048点的两个最大非LO功率块，块起点差均为33,536点/15.9695ms，
时间分辨率256点/0.1219ms；这给出约16ms重复间距线索，只有每份约31ms的有限记录，
不证明长期周期，选择规则不是新质量门。尖峰间另有持续数毫秒的平台，不能只报告两个最大点。

全部事件计数/原生seal/原同步/数值统计重放一致，补充分析也复算通过；图已检查。
全部点及最终postflight完整恢复，P201原sdrd PID5909/制品哈希、两路RX/LO/采样率/BW/scan mask/buffer不变，
B210 USB无人持有，P201瞬时目录不存在；新入口实际实机验证但仅作为实验制品保留。
精确删除183文件2,403,629字节、24目录（含5个合成源包、开发头文件/deb和缓存/重复日志），
保留64文件7,843,340字节，其中6份原生IQ为1,572,840字节；无IQ副本。
新二进制按已登记plan保留在本单元build/tx-events，旧UHD程序是未替换的回退路径。
原配置快照与两份相关父证据字节不变；逐文件SHA、构建依据、事件/采集身份、恢复与删除命令见
[证据清单](../evidence/B210_EVENT_CONTROLS_2026-09-13.json)。
本轮没有模型、训练、生产部署或P201 TX/FPGA操作，`recognizer_available=false`。

## 2026-09-13：P201独立50Ω负载与同轴控制比较

用户在前一轮已停发后确认接好独立负载，本单元记录接法为P201 RX1断开B210同轴链路、直接接50Ω负载。
B210保持停发，USB仍枚举在AGX且无持有者。本单元从`c135600c7fb7e9e2411a5d8bd9a1674d29eafbc0`开始，
仅用已有`validate-b210-cable-lo.py capture`的tx=None分支、唯一Controller与原生/恢复校验；
没有修改接收实现、启动新TX或读取数据集/模型。

固定6次单点RX：2455MHz、RX50/manual、2.1MS/s、BW1.5MHz、settle500ms、每次65,535复数，
每点1000ms/Controller外层15秒，间隔1秒，整体150秒/外层180秒；总原生IQ上限1,572,840字节。
空间预留64MiB，登记时AGX可用809,173,049,344字节；AGX结果根和各P201 generation临时路径逐点登记，
SIGINT/SIGTERM走原generation cancel与状态恢复。6次均完成，无削顶、丢样或overflow，峰值12/28/48/11/17/12counts。

比较使用全部6份新负载IQ和前一轮off-before/zero1/zero2/off-after四份同轴控制，
旧文件依据原清单逐一核验，未重发旧控制。统一在原生未滤波IQ上做256点Hann、256点hop，
固定排除229–279kHz（覆盖原LO位置），并合计四个绝对频带0–100/100–350/350–700/700–1050kHz。
没有给负载数据拟合虚构LO，旧控制也用相同固定mask重算；旧fine-analysis保持原字节。
每份255个完整块，最后255点另记样本数和未加窗功率，不丢弃尾部质量信息。

| 条件/记录 | 非LO块功率中位数/counts² | 最大块功率/counts² | >10倍中位数块数/255 |
| --- | ---: | ---: | ---: |
| 独立负载1 | 5.81 | 27.36 | 0 |
| 独立负载2 | 5.84 | 191.40 | 7 |
| 独立负载3 | 5.84 | 490.02 | 9 |
| 独立负载4 | 5.77 | 24.23 | 0 |
| 独立负载5 | 6.14 | 51.56 | 0 |
| 独立负载6 | 5.85 | 14.75 | 0 |
| 旧同轴停流前 | 5.39 | 1894.77 | 3 |
| 旧同轴zero1 | 5.90 | 2195.70 | 18 |
| 旧同轴zero2 | 7.13 | 2144.77 | 4 |
| 旧同轴停流后 | 5.45 | 563.96 | 10 |

阈值在本轮采集前登记，仅描述异常块，不用于丢弃样本或修改接收/模型质量门。
中位功率范围接近，而新负载各记录最大块功率低于这四份旧同轴控制；仍有负载2/3明显突发，
并存在较弱尖峰或平台，不能称接收器完全无干扰或负载等同理想热噪声参考。
按同一规则选择相隔至少2048点的两个最大块、且两者均>10倍中位数时，
旧四份均得到33536点/15.9695ms，新负载只有2/3符合显著双峰条件，间隔分别19.261ms/5.120ms。
因此旧“两个最强尖峰约16ms”的特定模式本轮未重现；不意味着完整穷举检验已证明所有16ms分量消失。
6份新记录各约31.207ms、合计约187.24ms且不连续，不能用于长期占用率或周期稳定性结论。

对照提示同轴路径及其另一端带来的能量/耦合值得优先排查，但接线变化也改变阻抗/接地和外部耦合，
并且这不是同时间随机化配对，尚不能单独确定B210、线缆、P201内部或Wi-Fi为物理来源。
下一步优先在用户确认接回原20dB同轴后、继续停发做有限返测，检验差异是否随接线重复；
当前仍保留P201独立负载，不因此恢复发射。

3项独立数值检查通过：白噪声控制不被描述为双尖峰、注入固定间隔突发正确定位并保留、
已知LO频带单音被固定mask排除；原生seal、全部统计回放一致。父事件控制包64份文件SHA未变，图已检查。
每点和最终P201完整状态恢复，sdrd PID5909/制品哈希不变、RX1身份健康，全部P201瞬时路径不存在，
未使用的AGX TX暂存路径没有创建，B210无持有者。无本单元存活采集进程或socket。
精确删除4文件138,691字节/2目录，scratch不存在；保留50文件2,803,698字节，其中6份原生IQ1,572,840字节。
原始IQ没有副本，源包/模型/构建产物均未新增；逐文件哈希、source/capture/request/session与人工删除命令见
[证据清单](../evidence/P201_INDEPENDENT_LOAD_2026-09-13.json)。
当前接线已同步至AGENTS/设备/入口与推进文档；旧配置快照仍是2026-09-12版本，不追改历史接法。
本轮TX0、模型0、训练0、生产/固件变更0，`recognizer_available=false`保持。

## 2026-09-13：同轴接回后的停发返测

用户确认B210 RF A TX/RX经原20dB衰减器＋15cm同轴已接回P201 RX1。
本单元从`445e16e4f318235323558f4e928072108d769784`开始，B210保持停发，不初始化UHD或发送全零流。
复用前一单元既有RX-only capture与固定统计实现，执行6次2455MHz/RX50/manual、2.1MS/s、BW1.5MHz的停流接收。
每点65,535复数、settle500ms、点超时1000ms/Controller外层15秒，间隔1秒、整体150秒/外层180秒，
精确总IQ上限1,572,840字节；空间预留64MiB，实际可用值和精确AGX/P201路径在新plan登记。
单实例、RX1身份、制品哈希、停止路径及完整恢复沿用已验证流程，无新接收代码或模型/数据集访问。

6次均完成，无削顶、丢样或overflow；单分量峰值12/17/23/18/18/18counts。
直接复用父包哈希固定的统计函数：256点Hann/256点hop、统一排除229–279kHz，
原始未滤波IQ上合计固定四个绝对频带；255个完整块加255点尾部单列。
原负载及旧控制使用已封存统计，核验父分析、统计源码与原生IQ身份，不重新选频带或阈值。
主比较为A1旧停流同轴2份→B独立负载6份→A2本次停流同轴6份；旧zero1/zero2保留为辅助，
不将正在送全零数据的控制混进A1停流分母。

| 阶段 | 非LO块功率中位数范围/counts² | 各记录最大块功率范围/counts² | >10倍各记录中位数的块数 | 显著最大双峰 |
| --- | ---: | ---: | ---: | ---: |
| A1旧同轴停流 | 5.39–5.45 | 563.96–1894.77 | 13/510 | 2/2，均15.9695ms |
| B独立50Ω负载 | 5.77–6.14 | 14.75–490.02 | 16/1530 | 2/6，19.261及5.120ms |
| A2接回同轴停流 | 4.23–4.85 | 12.39–107.92 | 31/1530 | 2/6，7.314及2.438ms |

显著双峰仍使用“相隔至少2048点的两个最大块，且两者均>10倍本记录中位数”的原描述规则。
A2六次最大块功率依次12.39/12.54/107.92/52.78/53.06/56.65counts²；
其中4次有超过相对阈值的块，2次满足上述双峰条件。峰值较低不等于没有突发或模型质量已提高。
较弱平台/尖峰仍保留，计数不是独立事件数，也不当作统计显著性或长期概率。

接回同轴没有使原来大尖峰及约16ms最大双峰模式恢复，**本轮未支持由接线状态稳定触发该模式的解释**。
需要修正仅凭前两阶段将异常重点归于同轴路径的初步推测：时间/环境或其他未记录状态仍可能影响结果，
不能认定换负载修复了问题、B210损坏或P201完全正常。A1来自更早一轮TX实验前后的停流控制，
三阶段顺序执行、数量不同而非随机化同时配对；每次仅约31.207ms，未证明长期周期消失或物理来源。
现阶段不继续根据这些短记录增加功率或调整滤波/相消门限。

新6份原生seal/完整统计回放一致，复用原已验证统计源码，未改公式、未重复其3项独立测试。
父负载包50份文件大小/SHA及旧同轴引用未变，图已检查。所有点及最终完整射频状态恢复，
sdrd PID5909和制品哈希不变，B210仍无USB持有者，P201瞬时目录不存在，TX暂存路径从未创建。
精确删除4文件145,428字节/2目录，scratch不存在；保留49文件3,366,604字节，其中6份原生IQ1,572,840字节。
没有IQ副本、TX、模型、训练、固件或生产变更；逐文件SHA、采集/会话身份、复现脚本和删除命令见
[证据清单](../evidence/P201_COAX_RETURN_2026-09-13.json)。
接线状态已同步至规则及文档入口：20dB同轴已接回确认，B210仍停发；后续发射需新的有限计划，
不重复索取已完成的接线确认。旧负载、同轴失败和复现快照原字节保持，`recognizer_available=false`。


## 2026-09-13四类新源行带事件复测

用户同意后，在已确认的B210 RF A TX/RX→20dB50Ω/DC–8GHz/2W衰减器＋15cm同轴→P201 RX1接法上执行。
新增严格`event-retest-pilot`及[独立入口](../../jetson-agx/sdrharness/scripts/rml2018a-event-retest.py)，
复用已验证事件TX执行器、Controller原生RX和冻结模型source/raw/guard推理；无新滤波、时序、门限调整或增益/衰减变化。
计划封存于`/var/tmp/sdrharness-dev/b210-rml-event-retest-20260913/plan.json`，SHA
`54fed61a914149c8999789b107d698f7432997d2769f04673ccc0bedd5ec4297`，基于HEAD`531fbbc12dd75680b30c40f39848d96e6940acbc`及计划中的源码SHA。
完整21,449,148,312字节HDF5哈希通过；本次96行均Z30、实际X/Y/Z身份通过，与95份既有campaign源记录的1032个唯一行无重叠。

| 类别/ID | batch | 原始HDF5行（含端点） | 原始→LO条件SINR中位数/dB | 源/原始/LO正确数（每组24） |
| --- | ---: | --- | ---: | --- |
| BPSK/3 | 17680 | 424320–424343 | 8.94→21.08 | 24/24/24 |
| 32PSK/7 | 35430 | 850320–850343 | 8.97→18.59 | 24/23/24 |
| 32QAM/13 | 62054 | 1489296–1489319 | 8.52→22.70 | 24/16/23 |
| FM/21 | 97552 | 2341248–2341271 | 13.53→25.34 | 24/21/24 |

RF保持2455MHz、TX60/RX50、数字峰值0.632455532033676、2.1MS/s、BW1.5MHz、LO+250kHz；
settle500ms，每次RX65,535复数ci16点、点期限1000ms、Controller等待上限15秒。
四次有限TX各26,112点×321、至多4秒，合计主机接受33,527,808复数样本/最多16秒；
四次实收加前后停流控制共6份、精确1,572,840字节，计划可用空间809,150,996,480字节、保留128MiB。
使用旧已验收事件二进制`/var/tmp/sdrharness-dev/b210-event-controls-20260913/build/tx-events`，SHA
`8a2a3fcfc242d63c918ecfa61d9bae3705b68f7b27144e9bc4c5cd079034d17b`，没有重编译/复制或系统UHD更换。
四次均完整发送、各一次带设备时间ACK，无报告欠载/序列/迟到事件；实际readback随原始事件日志保留。
P201没有首样本时钟映射，不能由ACK或主机接受样本数证明模拟链路连续性。

96/96同步，原始和LO后各96条均得到条件有效SINR，四批guard门通过。
最大相对相量误差余量依次0.0374/0.1362/0.1001/0.1018，均低于原sqrt(0.1)≈0.3162门；没有放宽门限。
32PSK/32QAM/FM此前失败批余量分别1.1992/0.5866/1.5518，本次未重现该保护区拒绝。
两轮源行、时刻和导频不同且新轮更换了发送执行入口，不能把差异单独归因于事件记录实现或认定故障修复。
32PSK这批LO后仍约18.59dB，而32QAM/FM约22.70/25.34dB；相同收发增益不对应一个固定SINR上限。

前/后停流控制单分量峰值89/30counts，未触发512counts门；未经去LO的256点块功率中位数17.22/15.03counts²、
最大块700.57/180.20counts²。仍有非平稳功率突发，这些原始全带统计不与旧“去LO/非LO频带”数值直接混比，
不据此新增周期、Wi-Fi身份或物理来源结论。每份接收仅约31.207ms，不能作长期平静证明。

冻结epoch10、FP16 autocast及FP32驻留权重，288个单1024窗source/raw/guard输入加2次预热完成。
源/原始/LO合计96/84/95正确（分母均96）；逐条原始→LO为12纠正、1回退，净进11。
回退行1489308：32QAM（ID13）原始判对，LO后判OQPSK（ID23），该行LO后条件SINR约22.55dB。
保留该反例及全部logits，不从模型输出选择处理或更改guard门限；较高条件SINR不保证每条识别改善。
这是共享原源X/Z参考的工程对照；profile身份中另有四窗合同，本次实际只执行单窗，没有宣称四窗聚合验收。
`recognizer_available=false`、名称provisional，无训练、独立locked-test、全库执行或生产部署。

98项不同Python测试通过（92项RML相关、6项事件解析；最终5项新入口测试补跑通过），覆盖有限profile、
越界预算/增益/源身份拒绝、同步失败缺失值及LO拒绝保留载荷。原生6份seal、四份事件、质量/处理确定性、
288实际输入SHA与有限logits/argmax/分母逐值回放通过，复核不重跑模型。
父其余16类182文件、旧事件64文件、同轴返测49文件的大小/SHA全部保持。

每点及最终P201完整射频状态恢复，sdrd仍PID5909/原哈希，USB3 B210空闲且本单元TX进程不存在；
推理后Spark原PID1321/start1515恢复并检查slots空闲，GPU lease已释放、恢复watchdog已停止。
P201六个瞬时路径、四份TX包、本单元owner.lock、scratch和测试暂存根均不存在。
精确删除357文件20,977,614字节/56目录（含四份已在收发后删除的TX包835,584字节）；
保留61文件7,881,376字节，含六份唯一原生IQ1,572,840字节、四份事件及模型输入/预测审计，无源IQ副本或新二进制。
计划/运行/模型身份、逐文件SHA、父二进制保留依赖与精确人工删除命令见
[证据清单](../evidence/RML2018A_EVENT_RETEST_2026-09-13.json)，操作见[接口](../reference/RML2018A_FULL_RF_CAMPAIGN.md#四类radioml带时间事件复测)。
本单元完成四类新样本复测，不完成干扰物理归因、统一24类新源行、AM指标适用性或低源SNR覆盖。

## 2026-09-13AM单边带指标适用性离线复核

从`18b067b5da393a787b33045a8481293713ee557c`继续，完成既有AM指标与源模型混淆的独立离线单元。
使用其余16类RX50父运行中的BPSK17664、AM-SSB-WC79787、AM-SSB-SC84224、AM-DSB-WC88662，
各24条Z30；仅重读96行X/Y/Z（IQ786,432字节）与4份原生IQ（1,048,560字节），没有IQ副本。
计划`/var/tmp/sdrharness-dev/rml-am-metrics-20260913/plan.json`，SHA
`41b0eef695eb77b4ecc7fd8673437a8be6cb71739f37a9dcae4d04c2b0b441f9`；180秒期限、2MiB报告上限、16MiB空间余量，
实际空间值和完整软件身份见计划。原HDF5沿用父完整SHA，核对大小/mtime及所选行哈希，本轮未再遍历整个文件。
[复核脚本](../../jetson-agx/sdrharness/scripts/diagnose-rml2018a-am-metrics.py)只调用离线解析和DSP，无硬件连接或模型实例。
使用既有venv Python `-B`，临时路径指向本单元；依次`plan --root <新根>`、`analyze --root <该根>`，再次analyze核对确定性，根前缀为`rml-am-metrics-`。

| 类别 | 源/原始/LO正确数（各24） | LO后有效SINR | 有效子集中位/dB | 理想y=X有效数 | 源窗均值功率占比中位 |
| --- | --- | ---: | ---: | ---: | ---: |
| BPSK | 24/24/24 | 24/24 | 19.46 | 24/24 | 0.284% |
| AM-SSB-WC | 15/24/16 | 14/24 | 14.65 | 23/24 | 99.67% |
| AM-SSB-SC | 21/18/21 | 8/24 | 16.58 | 24/24 | 99.10% |
| AM-DSB-WC | 24/24/23 | 24/24 | 19.03 | 24/24 | 0.0000387% |

均值功率是1024点有限窗常量投影，不是对物理载波或生成器清洁分量的独立辨识；不因DSB/SSB名称或此值更改标签。
每行取较小的半窗中心化功率占比，其中位数为BPSK97.84%、SSB-WC0.0761%、SSB-SC0.2081%、DSB-WC99.9999%。
SSB两类的增益拟合依赖很小的变化量；额外误差更容易影响半窗复增益，并在预测完整含均值X时放大残差。
原实现保留DC在预测信号和误差中，只在拟合增益时中心化；模型载荷输入没有因此去均值。

LO后WC10条无效：5增益不稳、4残差两半不稳、1参考不可辨识；SC16条无效：14增益不稳、2残差不稳。
无效组的较小半窗中心化占比中位WC0.0455%、SC0.1227%，有效组0.0945%/0.3690%；分布有重叠，不能据此新增判定阈值。
WC行1914896较小半窗占比约5.42e-5，低于原1e-4门；理想控制y=X仍以同一原因无效。
其余95条理想控制均有效，因此只能确定这一条由参考条件本身触发；其他实收不稳定包含新增误差影响，物理成因仍未分离。
源Z不等于各短窗干净成分的独立功率测量，带常量能量的全波形质量也不等于调制细节的保真度。

26条SSB无效SINR中21条仍识别正确（WC7/10、SC14/16）；无效不代表没收到或识别失败。
WC的9条源误判均为SC；LO后的8条误判也均为SC，且全部是源也判错的行，源/LO预测23/24一致。
SC的3条源误判均为WC，LO后逐条预测与源24/24一致。因此两类合计47/48预测一致、11条共同错误。
WC原始→LO有8条回退、SC有3条纠正；DSB-WC有1条额外回退，全部保留。
这说明需要把源域短窗混淆与RF新增影响分别报告，不能用原始WC24/24证明接收优于源域泛化，
也不能凭源/LO预测相近证明处理完全无损。没有针对这些预测调整滤波、增益、LO门或模型。

五项解析数值测试通过：已知均值/中心化功率分解、复增益不变性、理想低变化源的不可辨识失败、
源半窗功率不平稳但理想链路仍可估计、含常量总功率指标的含义。所有统计以固定定义一次计算并确定性回放一致。
父182文件大小/SHA保持；四份原生seal、原同步/guard、源帧及缩放、192份raw/guard质量逐值复核通过，
288归一化模型输入SHA及已存24维logits/argmax/源行关联通过，无新模型调用。
新增96个y=X控制仅为估计器数值诊断，不算RF捕获、独立训练样本或新的识别结果。

当前处理/门限保持，报告规则已补入[复现与论文说明](../reference/RML2018A_RF_REPRODUCTION.md#5-am单边带的条件sinr与识别报告2026-09-13补充)：
逐类并列源Z、各处理面SINR有效覆盖/原因、完整分母的源/raw/guard识别及纠正/回退；invalid保持null，
不因估计无效删除识别行，不把条件SINR改称物理或去载波的调制SINR。
本单元完成适用性诊断和报告边界，不完成AM物理SINR校准、AM源混淆修复或实际接收稳健性验收。
未启动硬件/模型进程、未触及P201/B210/Spark、无生产/训练/权重/默认DSP变更。
删除3份重复日志22,623字节及2个测试暂存目录，测试根不存在；保留4文件559,871字节，全部为派生文本/JSON，无新IQ。
计划、逐行统计、父采集/模型身份、SHA和精确人工删除命令见[证据清单](../evidence/RML2018A_AM_METRICS_2026-09-13.json)。


## 2026-09-13统一24类RX50新源行有限确认

用户“继续”后，从`9f47a558cb11e9b12a75ea4de59b82987fbc4ce0`实现并执行独立`uniform24-high-snr-pilot`。
复用带时间事件入口、既有Controller和冻结模型，固定TX60/RX50、2455MHz、20dB50Ω/DC–8GHz/2W衰减器及15cm同轴，
B210 AGX USB3/serial2508504/RF A TX/RX→P201 RX1；沿用已确认接法，无新天线或衰减调整。
每类ID0–23按顺序24条Z30，共576条，batch由`ceil((cid*106496+102400+3072)/24)`确定；
与95份旧campaign源记录及1份事件复测plan共1128个唯一行无交集。完整原HDF5 SHA、实际X/Y/Z、行级原始IQ哈希、
归一缩放、导频/run_id和帧哈希均核验，不按模型预测挑源行，不将旧RX40/50混合成绩加入本次。

计划`/var/tmp/sdrharness-dev/b210-rml-uniform24-20260913/plan.json`，SHA
`723cf8348742bfbcbd31bb010e551d2e59025bf83270be248cd7ed784ee86c79`。
峰值0.632455532033676、采样2.1MS/s、模拟BW1.5MHz、TX LO+250kHz、settle500ms；
每次RX65,535复数ci16点/262,140字节，单点期限1000ms/Controller等待15秒。
24次TX各26,112×321复数样本，最多96秒、201,166,848主机接受样本；前后停流加24次实收共26份、6,815,640字节。
整体采集900秒上限、单次TX65秒外层保护，预估300秒、实际约216.59秒；预留512MiB，计划可用空间809,138,348,032字节。
本单元AGX与26个P201精确路径、停止路径及推理预算见plan和execution-budget；没有持续发射或新二进制构建。
复用原事件可执行文件SHA`8a2a3fcfc242d63c918ecfa61d9bae3705b68f7b27144e9bc4c5cd079034d17b`。

26/26接收原生检查与完整恢复通过，24批全同步，实收单分量峰值163–354counts，均低于512门。
24次发送均完整接受预算样本且仅一次带设备时间ACK，无报告欠载/序列/迟到；P201仍无首样本时间映射，
这些事件不证明模拟波形连续性或没有干扰。没有为了提高结果重发、重采或放宽DSP门限。

| 类别 | 源/原始/LO流程正确数（各24） | 原始→LO条件SINR中位/dB | LO有效数 | LO动作 |
| --- | --- | ---: | ---: | --- |
| OOK | 24/24/24 | 8.53→20.34 | 23/24 | 应用 |
| 4ASK | 23/20/23 | 7.76→19.99 | 24/24 | 应用 |
| 8ASK | 23/22/24 | 7.62→22.86 | 23/24 | 应用 |
| BPSK | 24/24/24 | 8.41→21.92 | 24/24 | 应用 |
| QPSK | 24/24/24 | 9.05→19.34 | 24/24 | 应用 |
| 8PSK | 24/24/24 | 9.57→19.65 | 24/24 | 应用 |
| 16PSK | 24/24/24 | 9.23→18.75 | 24/24 | 应用 |
| 32PSK | 24/21/24 | 9.10→22.09 | 23/24 | 应用 |
| 16APSK | 24/24/24 | 9.08→20.38 | 24/24 | 应用 |
| 32APSK | 24/24/24 | 7.66→19.73 | 24/24 | 应用 |
| 64APSK | 24/23/24 | 8.09→24.23 | 23/24 | 应用 |
| 128APSK | 24/13/24 | 7.74→20.16 | 24/24 | 应用 |
| 16QAM | 24/24/24 | 8.04→8.04 | 24/24 | 拒绝，保留原始 |
| 32QAM | 24/20/24 | 8.05→22.10 | 24/24 | 应用 |
| 64QAM | 24/5/24 | 7.96→21.05 | 24/24 | 应用 |
| 128QAM | 23/3/23 | 8.07→20.47 | 20/24 | 应用 |
| 256QAM | 23/2/23 | 7.52→23.10 | 24/24 | 应用 |
| AM-SSB-WC | 18/22/19 | 9.21→8.77 | 5/24 | 应用 |
| AM-SSB-SC | 17/12/16 | 11.00→15.71 | 7/24 | 应用 |
| AM-DSB-WC | 24/24/22 | 10.18→20.82 | 24/24 | 应用 |
| AM-DSB-SC | 13/7/14 | 9.68→20.03 | 23/24 | 应用 |
| FM | 24/23/24 | 12.93→26.09 | 23/24 | 应用 |
| GMSK | 24/24/24 | 11.53→19.80 | 24/24 | 应用 |
| OQPSK | 24/24/24 | 9.03→21.28 | 24/24 | 应用 |

LO流程含门限拒绝时的原始载荷回退，因此16QAM对应的8.04dB不是已经完成LO相消后的数值。
23批LO通过；16QAM最大相量误差余量1.9618超过sqrt(0.1)≈0.3162，保留原始24条，仍24/24正确。
其首保护区训练/留出残余约14279.62/14095.06counts²，后续两区约14.15/14.12及32.04/35.28counts²，
说明这份保护区局部异常仍存在；不由相量门或仅ACK事件单独确定物理原因，也不因模型判对宣布波形无损。
其他已通过LO的批次也保留低SINR个别行（如OOK最小−0.43dB、FM最小3.07dB）；中位数不能替代完整分布。

源/原始/LO正确548/457/548，分母均576：95.14%/79.34%/95.14%；17类LO流程24/24。
逐条原始→LO为96纠正、5回退（SSB-WC3条、DSB-WC2条），净进91，均保留。
源/LO预测560/576相同；源正确而LO错误8条、源错误而LO正确8条，因此总正确数相同不表示逐行等价或没有新增损伤。
较明显收益包括64QAM5→24、128QAM3→23、256QAM2→23、128APSK13→24；这些仍是单批工程对照。

原始SINR546/576有效，30条无效（25增益不稳、3残差不稳、2参考不可辨识）；
LO流程530/576有效，46条无效（26增益不稳、18残差不稳、2参考不可辨识），全部仍进入识别。
AM-SSB-WC/SC的LO有效数5/24与7/24，分别识别19/24、16/24；沿用先前指标适用性结论，
不把缺失估计补成0dB、不仅对有效行算识别率。完整逐类原因与质量有效性/识别交叉计数见summary.json。
条件SINR仍使用源X/Z辅助并包含链路失真；没有物理功率/SINR校准，未知实收不直接拥有这些参考量。

前/后停流峰值111/22counts，未经去LO的256点块功率中位14.86/17.09counts²、最大1246.41/54.45counts²，
短控制仍有突发；原始全带统计不与历史去LO频带统计直接混比，不作为长期占用、Wi-Fi身份或来源消失的证据。
固定类别顺序、一类一次短采集，不能将类间差异完全归因于调制，也不能作为跨时段稳定性的证明。

冻结epoch10、FP16 autocast/FP32驻留权重，1728个单1024窗输入加2次预热；没有四窗聚合。
105项不同Python测试通过（99项RML、6项事件解析），含两种有限profile预算/根路径拒绝、24类源区间/包身份、
无效质量与缺失预测保留完整分母；旧四类profile仍限制四批，普通campaign及生产默认保持。
26份原生seal/24份发送事件、全部同步/guard/质量、1728实际输入SHA/有限logits/argmax/源行关联及逐类汇总确定性复核通过，
没有重复推理。父四类61文件、AM离线4文件、原事件64文件SHA/大小保持，95份旧源记录身份也由计划反复核验。

导出`uniform24-results.png`与矢量PDF，含各类完整分母识别及条件SINR中位/min-max和有效数，标明16QAM拒绝保留原始。
PNG已检查。初次使用模型venv绘图时缺少matplotlib，改用已有系统Python/numpy/matplotlib完成，无安装或模型环境改变；
绘图源码、运行版本、汇总SHA和图文件均登记，未把图片或原生IQ放入Git。

每点、采集结束及推理后完整P201状态一致，sdrd仍PID5909/原制品哈希，B210 USB空闲且本单元TX进程不存在；
Spark原PID1321/start1515恢复，slots空闲，GPU lease释放、恢复watchdog已停止。
本单元26个P201路径、24个TX包、owner.lock、模型/绘图cache、测试暂存根均不存在。
精确删除380文件25,332,044字节/57目录（包含已删除的24份TX包5,013,504字节）；
保留227文件44,030,058字节，含26份唯一IQ6,815,640字节、24份事件和模型/图表复现证据，无源IQ副本或新运行时。
完整源/采集/会话、模型/profile/preprocess身份、逐文件SHA和精确人工删除命令见
[证据清单](../evidence/RML2018A_UNIFORM24_RX50_2026-09-13.json)，[入口说明](../reference/RML2018A_FULL_RF_CAMPAIGN.md#统一24类rx50有限确认)。
本单元完成同设置24类Z30有限工程覆盖，不完成独立精度准入、空口验证、物理来源归因、长期波形稳定或全2555904条执行。
`recognizer_available=false`、名称provisional；没有训练/微调、生产部署、P201 TX/FPGA/BOOT或NX操作。


## 2026-09-13四类低源SNR有限分层

用户同意后，从`c57060ad4e150843735931f646f697638f355c8e`推进四类五档独立有限profile。
BPSK/16QAM/64QAM/FM（ID3/12/14/21），源Z20/10/0/−10/−20各24条、共480条原始IQ；
每档执行类别循环移位，固定档位顺序0/20/−20/10/−10。顺序在实收/推理前封存，没有按模型结果挑行或重排。
源batch由`ceil((cid*106496+((Z+20)/2)*4096+3072)/24)`确定，完整原HDF5 SHA及所选X/Y/Z/行级哈希通过；
与95份旧campaign源记录、2份事件plan共1704个唯一源行无交集。各档不是同一个干净信号加不同噪声的配对。
新入口在TX前验证实际Z，在接收质量/归档中逐行使用真实Z，汇总增加类别/源Z单元及by_source_snr；
原Z30 profiles、功率、DSP和门限不变，未重跑旧高源SNR组。

计划`/var/tmp/sdrharness-dev/b210-rml-snr-strata-20260913/plan.json`，SHA
`31596cb5833ea90d3411b6fc455e27ab47d4eec2dbe02e0398c0662a3def471f`。
接法仍AGX USB3 B210 RF A TX/RX→原20dB50Ω/DC–8GHz/2W衰减器＋15cm同轴→P201 RX1。
固定2455MHz、TX60/RX50、复数峰值0.632455532033676、2.1MS/s/BW1.5MHz、LO+250kHz、settle500ms。
每次RX65,535复数ci16点/262,140字节，20次实收及前后停流共22份、5,767,080字节；
每次TX26,112点×321，最多80秒/167,639,040主机接受复数样本。
900秒采集期限、单TX65秒、Controller等待15秒/点超时1000ms；预估240秒、实际约182.72秒。
计划可用空间809,090,592,768字节，预留512MiB；AGX/P201精确路径和停止/恢复方式见plan及execution-budget。

22份原生RX、每点及最终状态恢复通过，20批480条全部同步，实收峰值168–263counts，均低于512门。
20次TX均完整接受样本、各一次带设备时间ACK，无报告欠载/序列/迟到事件。
19批LO通过；16QAM/Z−20（batch53376）的首保护区最大相量余量0.4183高于sqrt(0.1)≈0.3162，按原门拒绝，
首区训练/留出残余834.67/86.12counts²，后续各半区约4.92–5.55counts²；原始载荷、失败原因和分母保留。
没有按背景或分类结果重采、重发、换衰减或放宽门限。ACK不证明模拟连续性，P201仍无首样本时钟映射。

| 源Z/dB | 源/原始/LO正确数（各96） | 原始→LO纠正/回退 | 原始/LO有效SINR数 |
| ---: | --- | --- | --- |
| 20 | 93/71/92 | 22/1 | 95/94 |
| 10 | 94/76/92 | 17/1 | 96/96 |
| 0 | 56/55/53 | 4/6 | 96/96 |
| -10 | 15/13/3 | 3/13 | 96/96 |
| -20 | 5/1/2 | 2/1 | 96/96 |

| 类别 | 源Z/dB | batch | 源/原始/LO正确数（各24） | 原始→LO条件SINR中位/dB | LO有效数 |
| --- | ---: | ---: | --- | ---: | ---: |
| BPSK | 20 | 16854 | 24/23/23 | 8.54→18.30 | 22/24 |
| BPSK | 10 | 16000 | 24/24/24 | 5.97→9.57 | 24/24 |
| BPSK | 0 | 15147 | 23/24/23 | -3.89→-2.16 | 24/24 |
| BPSK | -10 | 14294 | 2/4/1 | -12.72→-11.03 | 24/24 |
| BPSK | -20 | 13440 | 0/0/0 | -22.60→-20.95 | 24/24 |
| 16QAM | 20 | 56790 | 24/24/24 | 8.35→18.32 | 24/24 |
| 16QAM | 10 | 55936 | 24/23/24 | 5.27→9.27 | 24/24 |
| 16QAM | 0 | 55083 | 8/4/3 | -2.44→-0.54 | 24/24 |
| 16QAM | -10 | 54230 | 0/0/1 | -14.52→-12.89 | 24/24 |
| 16QAM | -20 | 53376 | 0/0/0 | -22.01→-22.01 | 24/24 |
| 64QAM | 20 | 65664 | 21/5/21 | 7.62→16.67 | 24/24 |
| 64QAM | 10 | 64811 | 22/7/20 | 5.45→9.17 | 24/24 |
| 64QAM | 0 | 63958 | 1/3/3 | -2.49→-0.77 | 24/24 |
| 64QAM | -10 | 63104 | 1/2/1 | -12.66→-10.88 | 24/24 |
| 64QAM | -20 | 62251 | 0/0/2 | -23.76→-22.13 | 24/24 |
| FM | 20 | 96726 | 24/19/24 | 12.27→19.14 | 24/24 |
| FM | 10 | 95872 | 24/22/24 | 7.46→9.67 | 24/24 |
| FM | 0 | 95019 | 24/24/24 | -2.34→-0.57 | 24/24 |
| FM | -10 | 94166 | 12/7/0 | -14.58→-12.91 | 24/24 |
| FM | -20 | 93312 | 5/1/0 | -22.11→-20.53 | 24/24 |

“LO流程”包含拒绝时保留原始的回退，16QAM/Z−20并未实际应用相消。
总源/原始/LO正确263/216/242（各480），原始→LO为48纠正、22回退，净进26；这个混合SNR总数只作工程清点。
分档结果更有解释力：Z20/10均LO92/96，接近相应源93/96和94/96；Z0为53/96，Z−10/−20仅3/96、2/96。
Z0的BPSK/FM仍23/24、24/24，而16QAM/64QAM各3/24；不能用四类平均数代替逐类可用性。
低源SNR不仅有源域退化，也有实收新增损失与处理回退：Z−10的FM源12/24、原始7/24、LO0/24，
Z0的16QAM源8/24、原始4/24、LO3/24。必须保留反例，不能把低SNR失败都解释成数据集自身噪声。
Z20/10的净收益各+21/+16，Z0/−10/−20各−2/−10/+1，未将LO设为所有SNR或生产默认。
不按源Z或当前预测事后选择较高分一路；未知实收没有原Z真值，这些点不构成可部署的切换阈值。

原始条件SINR479/480有效、LO流程478/480有效；无效均在BPSK/Z20，分别1条/2条残差两半不稳定。
有效估计与可识别性分开：Z−10的LO96/96估计有效但仅3/96识别正确，不能将估计检查通过当作信号可用证明。
Z0各类LO条件SINR中位约−2.16至−0.54dB，Z−10约−12.91至−10.88dB，Z−20约−22.13至−20.53dB。
这些仍是源X/Z辅助、含新增链路失真的条件值；低Z时名义源噪声项占主导，接近源Z不证明物理SINR校准或硬件更干净。
逐行相同峰值归一会同时缩放源信号与源噪声，不能通过提高发射增益消除原数据集内部的噪声。

导频仍为固定已知序列，原始不同Z只用于payload；全部同步不代表未知信号在相同低SNR下盲检测通过。
各点24条不同原始源行/一次短采集，图中连线只连接五个实测单元，不证明连续阈值、独立统计或同一干净波形的噪声响应。
未并入旧Z30、未覆盖其余20类或全部26个源SNR档，也不据此宣布完整全库质量通过。
停流前/后峰值167/23counts，原始256点块功率中位17.78/24.76counts²、最大3819.73/92.61counts²，仍有突发；
不将这些短原始全带控制与历史去LO频带统计直接混比或用于认定Wi-Fi/物理来源。

101项RML相关测试通过，包括20个类别×Z单元、源行块边界、固定预算/路径/增益拒绝、实际Z错配拒绝及完整分母分组；
原事件解析/发送实现未改，复用既有验证。22份原生seal、20份事件、同步/guard/逐行真实Z质量、
1440个实际输入SHA/有限logits/argmax/源行关联与分层计数确定性复核通过，无重复推理。
冻结epoch10/FP16 autocast/FP32权重，实际1440个单1024窗输入加2次预热，不执行profile中另列的四窗聚合。
源标注和全部模型结果保持工程/provisional范围，`recognizer_available=false`，无训练或独立locked-test准入。

生成并检查`snr-strata-results.png`及矢量PDF，四类各自展示完整分母识别、条件SINR中位/min-max和有效数，
明确16QAM/Z−20回退以及已知导频边界；绘图使用已有系统Python/matplotlib，不修改模型venv。
父统一24类227文件及原事件64文件大小/SHA保持，代码/计划/图表/模型和接收身份均登记。
最终sdrd仍PID5909/原制品哈希，P201完整射频状态恢复，B210无USB持有者且本单元TX进程均不存在；
Spark原PID1321/start1515恢复、slots空闲，GPU lease释放且恢复watchdog停止。
22个P201瞬时目录、20份TX包、owner.lock、模型/绘图cache及本轮测试暂存根均不存在。
删除376文件24,496,180字节/57目录（含已删除20份TX包4,177,920字节），保留193文件36,988,199字节，
含22份唯一IQ5,767,080字节，无源IQ副本、新运行时或生产安装。
[证据清单](../evidence/RML2018A_SNR_STRATA_2026-09-13.json)记录逐文件SHA、父血缘、会话/采集/模型身份和精确人工删除方法；
[执行接口](../reference/RML2018A_FULL_RF_CAMPAIGN.md#四类低源snr分层)保持有限profile、原始/LO双路和失败分母，不自动启动长期全库。
