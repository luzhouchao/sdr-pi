# 普通对话重复展示修复 — 2026-09-08

基线 `0de8e5a`，分支 `codex/recognizer-amc-offline-validation`，开始时工作区干净。
用户截图的同一句话分别来自 Controller 的已验证 hold 计划和 `Agent>` 回复，
不是两次模型推理。Controller 随后还为 hold 打印通用“没有生产执行器”提示。
本次仅修复 Web 展示，未修改 Planner、Controller、扫描/批准协议或模型配置。

主对话仅在相邻记录明确构成“保持当前状态计划 → 可选决策记录 → 完全相同
Agent 回复”时隐藏重复计划，并隐藏紧随该回复的那条固定执行器提示。原始
诊断逐条保留，不改写历史。没有匹配回复的计划、跨操作员轮次、其他动作计划、
批准提示和错误均保留，不对任意同文消息做全局去重。

16 项前端回归通过，包含只到达计划的中间状态、匹配/不匹配、跨轮次、真实错误
和原始记录不变。实际 Chromium 对源码预览及最终安装页面分别回放用户截图
对应事件：主对话只有一条回答，5 条原始事件仍在诊断中；另验证接收计划、
`/approve` 提示和失败没有被隐藏，浏览器 JS 错误为零。回放仅修改浏览器内存，
不发送模型请求、不写回事件、不新建会话、不触发射频。

release 构建成功后仅重启/替换现有 Web，Planner/Spark 不重启。已安装 JS 与
源码逐字节一致，健康为 ready，recognizer_available=false；原会话、结果、语料
及 provider 配置保留，complete 初扫没有补跑。制品精确版本/哈希、数据数量、
测试及清理见[审计](../evidence/HOLD_REPLY_DISPLAY_AUDIT_2026-09-08.json)。

沿用用户“不用备份”的选择，未创建新备份或回滚包，本单元未做制品回滚演练。
源码基线留在既有 Git，之前登记的发布/回滚包不修改。只清理本单元
`/var/tmp/sdrharness-dev/hold-display-908c/` 的构建、测试、浏览器、截图和脚本，
精确文件/字节数量与不存在核验记入审计；未保留新的外部临时文件或 socket。
不涉及 RF、识别准入、训练、locked test、在线网关或运维扩展。
