# 3500MHz使用历史TX70/RX50：未确认测试单音

用户要求“就用之前的增益”，核对历史2.440GHz成功记录为TX70/RX50、幅度0.2后，
按[预登记](../evidence/B210_3500_TX70_RX50_PLAN_2026-09-10.md)只执行一次，频率仍3500MHz。
双端弹簧天线和室内条件沿用用户最近确认。**传输/恢复完成，原单音门未通过。**
这次确实恢复了原收发增益组合，不能再把结果描述为只测过低增益。

TX中心3500MHz、SINE+100kHz、rate2.5MS/s、BW500kHz、gain70dB、ampl0.2、
25000000样本/名义10秒，RF A/TX-RX/channel0、serial2508504；gain70不等于70dBm。
RX1/RX0/A_BALANCED、3500MHz、rate2.5MS/s、BW1MHz、manual50dB、settle500ms，
前/中/后各65535ci16点，共786420字节、78.642ms非连续采样。point1000ms、Controller15秒、
runner120秒，远端35秒timeout+2秒强停。采前AGX809246470144字节可用，逐段复查。

| 阶段 | 原始128点RMS中位数ADC | 最大ADC |
| --- | ---: | ---: |
| 停发前 | 2.812 | 8.968 |
| 发射中 | 3.556 | 14.141 |
| 停发后 | 2.679 | 8.494 |

发射中整体幅度有上升，但预期+100kHz±5kHz内候选相对停发前/后仅13.121/12.947dB，
相对发射中谱中位数8.044dB，均未达原20dB门。候选+100366.217Hz不是已确认单音或CFO。
RMS包括最后不足128点的块，未经dBm校准；两个增益同时变更，不能分离各自贡献，
也不能凭整体幅度变化断言发射单音已收到。

另对保留IQ做只读事后频谱检查：Hann FFT、减均值、±500kHz内排除±10kHz直流邻域，
检查发射中最强五个FFT bin，不修改原资格门。最强约−14.763kHz，相对两个停发窗口
只高3.274/2.777dB；其余强峰也存在于背景，前五峰均未同时比两个对照高20dB。
这不是全频带单音检测准入，也不排除较弱或时变信号；派生值见wide-inspection.json。
本轮不追加发射、不拓宽原门制造通过。需要进一步核对实际RF输出、天线适配、端口及
发射时序，不能断定设备故障或3.5GHz不可用。

UHD实际freq/rate/gain与请求一致，LO locked、正常退出；BW500000数值单位Hz，日志
错误MHz标签沿用旧解释。尾部无时间戳S保留，未证明整个名义10秒连续发射或精确覆盖RX窗口。

## 实现、检查和状态

基线3b5ffc2，原runner新增仅显式TX70/RX50的3500MHz tone组合，保留TX0/10/20配RX20，
拒绝交叉组合/未登记增益/RML；旧2.4GHz默认不变。分析prepare增加expected_rx_gain，
默认仍TX0/RX20，按实参核对metadata与UHD。11项paired/边界测试、5项源匹配控制测试、
旧默认3500MHz分析重放通过；本轮三份原生报告/SigMF及数值重放通过，零报告
丢样/溢出/削顶/timeout。sequence1425–1427，generation1789021989889–1789021989891。

P201唯一sdrd5909与原哈希，持久配置/启动脚本和43110监听核对，NX前后身份wheeltec，
USB2508504无占用。每段两路gain/mode/port及公共LO/rate/BW、scan/buffer轮询恢复；
前后快照完全一致、health正常、三个P201瞬时路径消失。TX owner/child退出，NX本轮
空目录rmdir且消失，AGX本轮runner/Controller子进程退出。没有生产部署、模型或训练。

[库存](../evidence/B210_3500_TX70_RX50_EVIDENCE_2026-09-10.json)保留18文件/823209字节，
IQ786420字节；含计划、执行wrapper、原始报告/元数据/IQ/前后验/日志/分析与只读派生。
逐文件SHA-256、source/request/session和精确人工删除命令齐全。显式清理3空日志/0字节、
空scratch，测试自动清理8文件786433字节（随机子目录名未记录，最终为空）；共11文件786433字节。
无NX副本、新socket或旧证据改写。执行wrapper仅作审计保留，不能用于重放。

只读重放：现有venv python -B，PYTHONDONTWRITEBYTECODE=1、OPENBLAS_NUM_THREADS=1。

```python
import importlib.util, json
from pathlib import Path
p = Path('/home/jetson/sdrharness/jetson-agx/sdrharness/scripts/analyze-b210-3500-tone.py')
s = importlib.util.spec_from_file_location('analysis', p)
m = importlib.util.module_from_spec(s)
s.loader.exec_module(m)
m.ROOT = Path('/var/tmp/sdrharness-dev/b210-3500-tx70-rx50-20260910e')
assert m.prepare(70, 50)[0] == json.loads((m.ROOT / 'analysis.json').read_text())
```
