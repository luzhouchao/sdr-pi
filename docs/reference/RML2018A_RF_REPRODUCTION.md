# RadioML2018A射频实验复现与论文适用范围

[文档入口](../README.md) · [执行接口](RML2018A_FULL_RF_CAMPAIGN.md) · [实验记录](../validation/RML2018A_FULL_RF_CAMPAIGN_2026-09-10.md)

本页记录2026-09-12这组实验的复现基线和论文表述边界，不维护另一份施工顺序。
后续软件改动不追改此版本；当前下一单元仍以DELIVERY_ORDER为准。
逐项配置、源行、软件/模型/固件SHA-256及封存路径见
[机器可读配置快照](../evidence/RML2018A_RF_REPRODUCTION_SNAPSHOT_2026-09-12.json)。
快照对应代码提交`403119426d5c0873527d39633956439fde59caf1`，分支`codex/sdr-improvements`。
这是实验快照，不是持续发射计划或生产准入配置；本文整理没有新RF、训练、模型推理或部署。

2026-09-13补充实验另见[发送事件与停流/全零控制](../evidence/B210_EVENT_CONTROLS_2026-09-13.json)：
新增有限事件入口、实测内部clock/time source及六份控制IQ单独归档，不改写上述2026-09-12配置快照或原模型结果。
随后用户确认独立50Ω负载，6次RX另存[负载证据](../evidence/P201_INDEPENDENT_LOAD_2026-09-13.json)。
用户随后确认已接回原20dB同轴，停发返测另存[返测证据](../evidence/P201_COAX_RETURN_2026-09-13.json)。
随后[四类新源行带事件复测](../evidence/RML2018A_EVENT_RETEST_2026-09-13.json)完成，独立入口/计划与288单窗输入另存；
保留12纠正/1回退，不改旧快照或宣称物理干扰消除。
随后[统一24类RX50的576条新源行](../evidence/RML2018A_UNIFORM24_RX50_2026-09-13.json)完成，源/原始/LO正确548/457/548；
完整逐类数据、PNG/PDF和95.14%工程结果另存，保留16QAM拒绝及96纠正/5回退，不改写旧快照或独立准入范围。
随后[四类五档低源SNR分层](../evidence/RML2018A_SNR_STRATA_2026-09-13.json)完成，真实源Z逐行记录，保留48纠正/22回退；
低SNR有源域与实收双重损失，继续报告raw/guard两路，导频仍为已知序列，不将该结果作为盲检测或全SNR默认准入。
当前已恢复下面快照的接线，B210仍停发；具体新收发需登记有限计划，旧快照字节不改写。

## 1. 当前可复现的接线与参数

链路为 **AGX USB3 → B210 RF A TX/RX → 20dB衰减器＋15cm SMA同轴 → P201 RX1 → 网口 → AGX处理**。
用户早期称N210的设备实际枚举为B210，serial2508504；当前不经NX发射。
B210与P201的程序目录分开，共享一份原HDF5，详见[设备工作区](../../devices/README.md)。

| 项目 | 本次有限先导实际设置/身份 | 复现时的含义 |
| --- | --- | --- |
| B210 | serial2508504，AGX USB3，通道0、subdev A:A、UHD天线TX/RX | 外部有限信号源；UHD内部端口命名与面板名分别保存 |
| P201 | 192.168.1.10:43110；RX1/RX0/A_BALANCED；手动增益 | 唯一生产受控SDR，保持RX-only |
| 衰减器/线缆 | 一只20dB、50Ω、2W、DC–8GHz；15cm SMA线 | 标签由用户提供；未测实际插损、驻波、连接器损耗或器件序列号 |
| 中心频率 | 2455MHz | 当前实验频段；不代表空闲频点或通用部署频点 |
| 采样率/模拟带宽 | TX/RX均2.1MS/s、1.5MHz | 带宽是射频滤波器设置；与数字检测滤波分别记录 |
| TX增益/数字幅度 | 60dB；每源行复数峰值0.632455532033676 | 增益不是dBm；不能用TX增益减20dB直接得到接收功率/SINR |
| RX增益 | 50dB，manual | 同轴当前参考；空口须重新按削顶和噪声余量验证 |
| TX LO偏移 | +250kHz | 目标中心仍2455MHz，LO相关杂散单列；不等同改变目标中心频率 |
| B210主时钟 | 实验日志为33.6MHz | 未记录外部共享时钟/参考，不宣称两机同步采样 |
| 单批RX | 65,535复数ci16_le，交织I/Q，262,140字节 | 采样时长约31.207ms；不是4秒连续RX |
| settle/点超时 | 500ms/1000ms | 本实验值；不能误取RF-v1 profile中的100ms settle |
| 接收峰值门 | 单分量最大绝对值≤512counts | 实验余量门；原生削顶/丢样/overflow另检查，失败保留 |
| 普通campaign默认 | TX0/RX20、数字峰值0.2 | 仍保持原值；当前优化只在显式有限profile执行 |

每批24条独立1024点源IQ，前加1024点QPSK已知导频，帧首尾各256点零保护，
合计26,112点；连续重复321次，共8,381,952复数TX样本，约3.9914秒、上限4秒。
重复用于覆盖异步接收，不能把重复帧算作额外独立测试样本。
每行缩放系数、源行号和包哈希保存在各批source/tx-plan/audit中；导频由run_id/batch派生。
新run_id会改变导频，方法相同不意味着发射字节或再次接收IQ相同。
2.1MS/s是本实验选定的RF回放采样率，不声称恢复了RML源数据未提供的原始物理发射参数。

## 2. 接收处理及优化状态

| 环节 | 当前实验行为 | 对未来空口接收的适用条件 |
| --- | --- | --- |
| 数字峰值与TX增益配对 | 提高数字幅度、降低硬件TX增益；曾实测降低LO相对影响 | 可作为发射机校准思路；更换设备/功率/波形后重新测量，不能照搬增益数值 |
| 导频同步/CFO/公共相位 | 129抽头Hamming、500kHz低通仅作检测；搜索±14.5kHz，500Hz步进，最终绝对限17kHz；初始/最终门0.55/0.65 | 当前需要已知导频；未知发射机必须另有盲同步或可识别协议结构 |
| 保护区LO校正 | 原训练半窗估计一个固定复数单音；`guard-tone-block-margin-v1`检查全部训练/留出半段 | 需要已知静默区及捕获期间稳定LO；多径尾迹、突发和频漂可能破坏假设 |
| 相消门 | 每半段192点、8个24点块，误差余量≤sqrt(0.1)，稳定性与导频检查；原v1结果同时保留 | 不按真实类别调门；校正拒绝时原始IQ及原因保留，不强行输出“干净”数据 |
| RX40→RX50 | QAM匹配实测有收益；剩余16类采用RX50 | 是本接法的增益/ADC余量选择；空口强邻道可能使同档增益削顶 |
| 导频时序校正 | 已做有限对照；没有纳入剩余16类的识别处理 | 更正时序通常值得研究，但本实验总识别收益不一致，不能默认对所有类有利 |
| 固定500kHz载荷滤波 | 只作对照，当前未默认启用 | 必须保留调制特征；同频干扰与目标频谱重叠时，普通滤波不能无损分离 |
| 归一化/分类 | 每个1024点载荷做复数RMS归一，float32 [2,1024]，24类logits/argmax | 数据接入规范与训练域必须一致；仅提高SINR不保证提高识别率 |
| SINR记录 | 原Z为source_snr_db，源辅助交叉拟合得到条件有效rx_sinr_db | 本实验依赖源X/Z；未知实收没有这两个真值，不能直接沿用为物理SINR实测 |

块相量公式、保护区边缘排除64点、频率先验±30Hz搜索、幅度/相位稳定性及导频门的完整合同，
均保存在快照`lo_contract/guard_contract`，实现说明见[执行接口](RML2018A_FULL_RF_CAMPAIGN.md#保护间隔lo块相量验证实验入口)。
源X只用于工程质量估计/源对照，不进入保护区相消器；模型输入截取payload，不能把批次、真实类别或导频送进分类器。
当前方案属于**已知帧结构辅助的调制识别工程实验**；盲识别能力仍需单独验证。

冻结模型为AMCMambaD8 epoch010：FP32驻留权重、FP16 autocast计算、FP32输入/返回logits。
checkpoint为`local-assets/amc-eval/checkpoints/rml2018a/rf-v1-ft-batched-seed44/epoch-010.pt`，
SHA-256 `a3c3e41ba9732171d65b023d7d9f1d334d88b7ca28be3d939b0536878c168054`。
标签采用server-v1原24类顺序，名称仍provisional。配置与模型源码身份从快照链接核对。
RF-v1身份内`window_count=4`描述另一条四窗/mean-logit接口；**本实验实际逐个单窗调用classify_logits**，
不能把本次结果写成四窗生产识别成绩。profile中的准入/性能指标也不能代替本次端到端实测。

当前仅实验脚本实际执行上述处理；普通campaign未安装新LO预处理，生产`recognizer_available=false`。
最近一次完整射频恢复基线从封存postflight读取：LO5985999996Hz、采样率30.72MS/s、BW30MHz，
两路RX manual/60dB/A_BALANCED，scan mask和buffer均0；这不是下一次发射或实验接收的推荐值。
本次文档整理没有重新SSH读取硬件，不能把旧postflight称为当前实时状态。

## 3. 复现入口与必备资产

| 复现对象 | 精确依据 |
| --- | --- |
| 当前代码/数值方法 | 上述4031194提交；快照逐文件SHA，B210 helper/transport也由run-plan固定 |
| UHD二进制、FX3与A7-100T镜像 | [runtime-manifest](../../devices/b210/runtime-manifest.json)；8文件及系统libuhd已只读核验；不使用P201 FPGA/BOOT路线 |
| 模型/配置 | 快照checkpoint、profile、candidate、preprocess及label map路径/哈希 |
| Python环境 | 快照记录实际venv、Python3.10.12、torch2.8.0、NumPy1.26.4、h5py3.14.0等包版本；本次未初始化CUDA/模型 |
| 原数据 | 同一RML2018a.hdf5，21,449,148,312字节；SHA来自封存RF计划，本次仅重查大小，未重新通读21GB |
| 最近16类 | [原证据清单](../evidence/RML2018A_REMAINING16_2026-09-12.json)，含384精确源行/包/采集身份、配置、预测和删除方法 |
| 更早配对/消融 | 快照optimization_evidence连接幅度增益配对、LO、八类、QAM、固定滤波、RX增益配对；不混合不同组的准确率 |
| 未解决的局部异常 | [四批离线诊断](../evidence/RML2018A_REMAINING_GUARD_DIAGNOSTIC_2026-09-12.json)，只读复核宽带残差，未提供修复 |

最近一批的封存根为：

- RF：`/var/tmp/sdrharness-dev/b210-rml2018a-remaining-20260912/`。
- 比较：`/var/tmp/sdrharness-dev/rml-timing-multiclass-remaining-20260912/`。

`run-plan.json`是执行合同；`pilot-plan.json`登记有限预算/GO前状态；`source.json`、`tx-plan.json`、
`audit.json`、SigMF元数据固定行号与request/session/capture；`prepared.json`固定模型输入哈希，
`inference.json`固定原始预测，`summary.json`固定统计；postflight/cleanup证明该次恢复与清理。
原生IQ和模型不在Git中，仅克隆仓库不能完成数值复现；必须保留这些已登记资产。
原始证据的删除入口仍在各父清单，配置快照不复制IQ、源数据或模型。

在匹配源码、配置、数据和封存路径的环境中，可用下列只读回放核对输入与已记录预测；
它重算同步/质量和输入哈希，不发射、不重新执行模型，也不证明更换环境后的模型数值一致：

```bash
PYTHONDONTWRITEBYTECODE=1 OPENBLAS_NUM_THREADS=1 \
local-assets/amc-eval/runtime/venv/bin/python -B \
jetson-agx/sdrharness/scripts/compare-rml2018a-pilot-timing.py verify \
  --root /var/tmp/sdrharness-dev/b210-rml2018a-remaining-20260912 \
  --output /var/tmp/sdrharness-dev/rml-timing-multiclass-remaining-20260912
```

如果源码已经前进，先在隔离检出中恢复对应版本和资产路径，不切换/覆盖正在工作的用户分支。
重新采集属于新的实验：创建新唯一根和run_id，显式指定`remaining-high-snr-pilot`、AGX、TX60/RX50，
复用封存preregister/acquire的流程而非直接运行其中写死旧目录的脚本。
新计划重新检查实际接法、单一所有者、空间、停止路径和射频恢复；不覆盖旧IQ或为了通过而自动重试。
当前profile上限16批/384源行/64秒TX/4,194,240接收字节，最多1152模型窗口+2warmup；
比较进程alarm650秒、模型加载后分类循环580秒、外层timeout680秒。
这些只是最近16类的有限边界，不能据此直接启动全库；全库工程目标未完成。

同一源/同一配置的再次RF实验只应期望方法与统计可比，不期望噪声、CFO或接收字节逐点重现。
若要验证环境迁移后的模型一致性，应另登记有限同输入推理对照；本次未执行。

## 4. 同轴、空口与论文证据

对于最终面向无线信号的模型，推荐论文采用三层证据：

| 实验层 | 主要回答的问题 | 尚不能支持的结论 |
| --- | --- | --- |
| 离线原IQ | 数据集任务上的算法性能与计算开销 | 真实RF前端或空间传播适应性 |
| 衰减同轴硬件回放 | 经过真实DAC/混频/ADC/传输后性能如何；校正的配对消融 | 多径、路径损耗、移动、真实共信道干扰下的泛化 |
| 空口天线实验 | 在规定距离/场景/时间/干扰条件下能否识别 | 没覆盖的新设备/新场景或所有未知协议都可泛化 |

同轴适合建立可控硬件基线、调通系统和定位损伤；它仍有器件噪声、LO/CFO、非线性和阻抗误差，
不能称理想无噪声信道，也不能把20dB衰减器写成20dB SNR。
空口增加传播衰减、多径与时变因素，对真实部署的证明力更直接，但也更难归因和复现。
论文若主要研究识别算法，同轴可作为硬件补充；若宣称实际无线部署，建议空口结果作为关键验证，
同轴作为解释性能差异的基线。最终接收未知无线电发射时使用天线；同轴主要是实验连接。

目前已知帧结构辅助的优化可以迁移为协作式链路的工程方案：发射端控制数字幅度和发射链路线性余量，
接收端进行增益管理、同步/频偏补偿及满足假设的LO校正。但未知发射机不能要求它发送我们的导频/保护区；
广义盲AMC还需要检测、频段分离、采样率/符号率适配和不依赖已知payload的同步等前端。
未知条件下质量可记录功率、削顶、检测/同步结果或经独立验证的盲估计，不能假装拥有源X/Z来算当前SINR。

将真实SDR/AGX实验写入论文通常能增强工程可信度，价值来自可复现对照、合理证据边界和新场景结果，
不由“用了SDR”本身保证创新性、录用或加分。功率设置、同步和常规校正常作为必要实验方法；
若将某项校正主张为算法贡献，还需相关工作比较、清晰假设、复杂度与独立数据消融，不能仅凭本批改善断言新颖。
比较不同识别模型时，应共享相同前端、源成员与实收输入、划分和计算预算，把网络收益与同步/校正收益分开。

建议论文记录以下可比较指标与分组；这是研究建议，本次不启动实验或训练：

1. 各类准确率、macro-F1、混淆矩阵；同时给出源模型对照，区分源模型误判和新增链路损失。
2. 分开报告检测/同步成功率、同步成功条件下分类准确率，以及全部预登记发射样本为分母的端到端正确率；
   未同步、估计无效、校正拒绝、削顶和超时均保留。因提前中止未执行的计划样本另列，不能伪装成已测。
3. 源Z、各接收处理面的条件SINR及无效计数分别画图。未知空口不直接沿用依赖源的SINR分箱。
4. 固定硬件/输入的原始、LO校正和待验证滤波/时序消融；不依据真实调制类别选择最有利处理。
5. 固定测试规则后，在不同采集session/日期/位置、距离、视距/非视距与干扰条件下复测，记录天线型号、
   极化、高度、方向、功率/增益、带宽和环境。需要功率/SINR定量结论时补实际校准。
6. 数据按源样本及其全部重发/派生后代、session/day隔离；调参数据不能回流到最终独立测试。
   相邻窗口和重复帧不是独立样本，置信区间应尊重采集组相关性；每类24条不足以支撑广泛稳健性结论。
7. 若主张边缘部署，报告实测采集到结果时延、吞吐、内存、持续运行和失败恢复；模型单独推理速度不能当端到端实时性能。

原RML2018A样本已含噪声/信道损伤，同轴或空口重放会叠加新损伤。
因此可以保留它作为“带既有损伤的RF回放”工程基准；若研究可控物理SNR或干净发射泛化，
可另建经独立调制/星座/EVM检查的干净生成源与可控噪声/信道实验，不必立即替换原数据集。
原Z和新增损伤不能直接按dB相减，也不能把发送Z=0dB等同接收物理SINR=0dB。
当前全库源访问包含历史训练成员，现有结果不能标成未见样本的独立泛化准确率。

论文现阶段可用的保守表述示例：

> 本文建立B210–P201–Jetson AGX的衰减同轴RF回放平台，使用已知导频和保护区完成同步及条件性LO校正。
> 在16种调制、每类24条标称源SNR为30dB的工程样本上，冻结模型对原始源、未校正实收和LO处理组的正确数
> 分别为369/384、319/384和350/384；三批校正拒绝时保留原始输入并计入统计。该结果反映规定硬件、
> 帧结构与样本范围下的工程表现，不代表盲空口识别或独立未见样本测试。

这段描述不合并旧八类RX40数据，也不宣称已经消除三批宽带异常；
后续取得独立空口证据后，再增补场景、方法冻结时间、完整分母和相应结果。

## 5. AM单边带的条件SINR与识别报告（2026-09-13补充）

[离线复核](../validation/RML2018A_FULL_RF_CAMPAIGN_2026-09-10.md#2026-09-13am单边带指标适用性离线复核)
复用既有两类SSB、DSB-WC及BPSK的96行，不改2026-09-12快照、原始记录或生产估计器。
SSB-WC/SSB-SC这两批1024点源窗的均值功率占比中位99.67%/99.10%；
该量是有限窗常量投影，包含低频缓变分量，不能直接命名为独立测得的载波功率，尤其不能据此更改SC/WC标签。

当前方法在每个512点训练半窗去均值后拟合复增益，再预测另一半的**完整源X**。
令预测X总功率为Q、交叉预测残差功率为E、α=1/(1+10^(Z/10))，记录的条件有效SINR为：

```text
10 log10[ (1−α) Q / (α Q + E) ]
```

Z只是数据集给定的名义SNR；此式假设名义功率分配和单一复增益适用，不分离干净调制信号、载波、源噪声和硬件失真。
SSB源窗可供拟合的中心化变化量很小，小的新增误差就可能使两半增益估计不一致，预测完整X时又影响占比很高的常量分量。
一条SSB-WC参考即使理想回放y=X也不能通过可辨识门；其余95条理想对照有效，不能将所有实收无效都归于源窗本身。
本次不放宽门限、不将去均值后的残差另称“物理SINR”，也不改变模型输入预处理。

论文及统一24类工程结果建议逐类同时报告：

- 源Z、原始/LO后条件SINR的有效条数/总条数、仅有效子集的中位数及无效原因分布。
- 源/原始/LO三组正确数/完整源行分母，另列同步失败、缺失预测和逐条纠正/回退。
- SINR有效/无效与识别正确/错误的交叉计数；不可仅保留SINR有效行来计算总体识别率。

本次SSB-WC/SSB-SC的LO后有效SINR分别14/24、8/24，无效26条中21条识别正确。
`invalid`保留null及原因，不填0、不当成未接收，也不解释为真实SINR必然低。
两类源与LO后预测47/48一致，LO后的11条误判均在源输入上也误判；这提示源域短窗类别混淆需要单列。
原始实收WC24/24、LO16/24的下降仍如实保留，不能凭预测一致性证明LO处理完全无损或将这批原始识别增益当可靠泛化收益。

可用于论文的方法表述：

> 本实验保留原始数据集的名义源SNR，并报告已知源参考辅助的条件有效接收SINR。
> 该指标包含新增链路失真，依赖有限窗内的增益可辨识性及稳定性，不作为校准物理SINR或仅调制分量SINR。
> 对未通过估计检查的样本保留无效原因，识别结果仍计入完整分母；同步、质量有效率和识别正确率分别统计。

原始空口未知信号没有源X/Z时，此方法不直接适用。模型输入、增益、LO门和生产准入均未因本次指标说明改变。

## 6. 三套新增数据集的收发设计（2026-09-26，尚未执行）

本节是用户要求“先设计”的工程方案，不是已登记可执行的RF计划。
对象为RML2016A、RML2016B、HisarMod2019；每套接收数据供统一库的八模型使用。
保留原2018A结果，新增数据不使用2018A的行号公式、24类映射或源SNR范围硬编码。
完成本设计的条件是明确帧、载波、处理分支、原生窗口、数据血缘、有限先导预算及实施门；不以设计完成代表收发完成。

### 6.1 输入合同与源行选择

| 数据集 | 已核对源X形状 | 类数 | 模型输入 | 当前D10 / 七baseline |
| --- | --- | --- | --- | --- |
| RML2016A | 220000×2×128 | 11 | B×2×128 | seed46 / seed42 |
| RML2016B | 1200000×2×128 | 10 | B×2×128 | seed45 / seed42 |
| HisarMod2019 | 780000×2×1024 | 26 | B×2×1024 | seed46 / seed42 |

数据集原始X已经是调制后的复基带：不再做一次QAM/PSK调制，不人工补一个单音到payload。
128点不重复、不补零成1024点送模型；1024点处理块的“行数”和单个模型窗“点数”必须分开。
读取实际Y/Z及classes；labels与`/home/jetson/models/amc/<dataset>/class-order.json`逐项匹配。
source_row在原HDF5内唯一，跨数据集以(dataset_id, source_sha256, source_row)唯一标识。

先导建议每类32条、仅最高有效源Z档，固定种子与行号清单，按类交错排列，避免类别完全绑定采集时段。
优先取D10与七baseline冻结val成员的交集；候选不足时报告缺额，不补train、不重发凑独立样本、不按预测挑选。
行号和源Z的实际可用性尚未核对；下方预算是所有类别均足32条的上限设计。
先导只验证链路；全量或更大范围尚未冻结。seed曾按测试成绩选择的D10也不能作为全新独立测试模型。

### 6.2 射频载波、数字移频和导频

继承参考：目标中心2455MHz、TX/RX采样率2.1MS/s、模拟带宽1.5MHz、TX60/RX50、manual、20dB同轴接法。
这些是候选设置，执行前读回真实RF/DSP频率；不把历史读回当作当前状态。

TX采用UHD的+250kHz LO offset：理想关系为RF LO约2455.25MHz、数字等效移频−250kHz，
使有用信号仍居于2455MHz；具体UHD DSP符号以读回及单音验算为准，脚本不得再额外移频一次。
P201接收中心2455MHz；TX LO泄漏预计出现在接收基带约+250kHz，P201自己的DC偏置位于约0Hz。
二者不能混称同一个泄漏，也不能声称+250kHz天然没有有用信号。

理想发射关系为`Re{a·x[n]·exp(j2πf_dsp n/Fs)·exp(j2πf_LO n/Fs)}`；
`f_LO+f_dsp=f_target`只是等效频率关系。实际还存在CFO、采样时钟偏差、相位漂移及LO泄漏。
导频用于已知参考同步，payload内AM等调制自身的载波/直流结构原样保留；禁止统一去均值、盲挖DC或+250kHz陷波。
数字零保护区只代表发送基带为零，RF硬件仍可能泄漏；保护区从模型输入裁掉，不会自动消除payload上叠加的泄漏。

### 6.3 首选原生长度帧

复用现有16窗一帧、run_id/frame_id确定的1024点已知导频：

```text
256点前保护 | 1024点已知导频 | 16个原生IQ窗 | 256点后保护
```

| 数据集 | 每窗点数L | 每帧总点数1536+16L | 每帧时长（2.1MS/s） |
| --- | --- | --- | --- |
| 2016A/B | 128 | 3584 | 1.706667ms |
| Hisar | 1024 | 17920 | 8.533333ms |

导频的两个512点重复半段保留，用于CFO估计；导频本身不是待识别的源样本。
导频不编码类别/SNR，只绑定本次run和帧序号；RX只能用导频识别帧，不能依靠源payload相关搜索选择最佳对齐。
帧序号与源行通过冻结manifest映射。丢帧不使后续行号整体错位；不足16行的尾帧用明确valid mask标空位，不能复制源行填充。
每源行仅作正实数峰值缩放至0.632455532033676，保存缩放因子；保持调制相位和相对幅度，零能量/非有限源行拒绝。
导频沿用旧幅度约定；波形总体峰值另查，不能只验payload。
有限会话内连续送出一次帧序列，块间不停设备，不无限循环，不将padding或重发当新成员。

**发射频谱门是实施前置。** 在既定行号上用FFT检查源窗、完整帧、移频后的PSD、99%占用及带边功率，
分别登记每类/Z和全体分布。短窗零填充FFT只提高显示密度，不增加真实频率分辨率。
1.5MHz模拟带宽不是理想砖墙；还须计入TX处−250kHz偏移与滤波过渡带，不能只检查RX中心±750kHz。
原数据已有噪声，不能把全部带边能量都认作无用噪声并裁掉。若原生回放不满足带宽余量，停止该profile。
备选为发射端整数插值、接收端匹配抽取、恢复128/1024原生窗；插值不是重复采样或模型补零。
其倍率、FIR系数/延时、窗边上下文、额外保护区、峰值与预算须另行冻结，并先通过无RF往返误差/频谱验证。
不在失败后自动改带宽/偏移/采样率；也不以识别准确率挑插值参数。当前首选预算不涵盖该备选。

### 6.4 接收及raw/guard分支

1. Controller独占P201 RX；首个真实IQ块到达AGX后才给TX发GO。原始ci16流按连续样点偏移存储，保留未同步部分。
2. FFT相关作首次导频搜索、既有CFO网格作粗估，逐帧导频精跟踪；导频相关滤波仅服务同步，不自动滤payload。
3. 根据独立导频估计时间偏移、CFO、公共相位；用连续采样索引补偿，监测相邻帧漂移和漏样。
   不以payload真实类别或模型置信度调同步/增益。先保留既有整数定位；分数延时或重采样若必要须独立版本验证。
4. 在同一同步位置形成两路：raw不做LO相消；guard只从可用零保护区拟合窄带泄漏并相消。
   复用现有训练/留出保护区、幅相稳定与独立导频检查；不把前后512点全拿去训练，也不拿payload拟合泄漏。
   短帧须重新按帧边界计算保护区位置；禁止沿用“16×1024”的固定偏移。
5. guard未通过时保存拒绝原因，guard回退到同一raw并标记未相消；不删失败行、不把回退标成成功相消。
   保护区预测通过不保证payload内部泄漏完全稳定，也不保证识别收益为正。
6. 去掉导频/保护区，各自裁出L点；raw/guard各自逐窗RMS归一化成float32 B×2×L，保存分母及同步/相消状态。
   128点质量估计必须按两半64点实现，不能沿用两半512点；短窗估计稳定性需单独核验。
7. raw/guard及原始IQ落盘、缓冲消费者完成、会话停止/射频恢复通过后，才加载模型逐一识别。
   采集时可GPU DSP，不加载识别模型；复用现有GpuLease、BufferPool、Controller和存储体系。

FFT的用途是同步相关、CFO/窄带峰搜索及频谱诊断，不是“把某个bin置零就完成无损相消”。
频偏非整bin与有限窗会产生谱泄漏；实际相消仍需复幅度、频率、时间参考及验证门。

### 6.5 四版本存储和评估

每数据集只设四种逻辑数据版本：source（原HDF5，不复制）、rx_iq（完整原始ADC流）、raw、guard。
raw和guard都已同步/频偏相位校正/逐窗RMS；raw不是未经处理的ADC。
索引/质量/背景采集是附属记录，不额外命名第五套训练数据；TX临时波形完成审计后可删除，保留生成规则和哈希。

每行记录dataset/source_sha/source_row、Y/Z、run/session/capture/request/generation、frame/window、
ADC样点区间、缺失状态、缩放/RMS、同步与guard原因、profile/preprocess/SHA、模型身份及预测。
主图只raw/guard，接收端条件估计SINR分层；source只溯源，不新增source准确率曲线。
配对纠正/回退必须使用同一共同成员和raw参考SINR；各自SINR曲线另列，不能相减不同分箱成员。

沿用已有源参考辅助的条件SINR概念，但先验证128点两半估计及三数据集Z定义是否适用。
全量源X含既有损伤，不是无噪声真值；Z假设不成立时不可套用原噪声功率拆分公式。
无效SINR记录null/原因，识别仍计入完整分母；不能凭source Z或20dB衰减冒充实测SINR。
报告同步率、质量通过率、有效SINR率、全部已发成员端到端识别率与共同合格子集准确率；源行未发单列。
同一实收窗供八模型使用，配对样本不按模型分别挑选。工程对照与独立测试界限保持。

### 6.6 有限先导预算草案与停止条件

各数据集单独会话，建议每类32行。以下仅计算字节和时间，不代替真实计划中的数据/波形哈希及空间读回：

| 项目 | 2016A | 2016B | Hisar |
| --- | ---: | ---: | ---: |
| 最大源行 | 352 | 320 | 832 |
| 帧数 | 22 | 20 | 52 |
| TX复数点数 | 78848 | 71680 | 931840 |
| TX fc32字节 | 630784 | 573440 | 7454720 |
| TX时长秒 | 0.037547 | 0.034133 | 0.443733 |
| raw+guard float32载荷字节 | 720896 | 655360 | 13631488 |

每会话主RX上限12×1048576复数ci16点（50,331,648字节，约5.992秒），前后TX-off背景各
1048576点（各4,194,304字节）；ADC合计58,720,256字节。主RX含GO前等待与发送后尾部，
若GO时剩余RX预算不足覆盖TX与65535点解码lookahead则拒绝发送；不能耗尽RX后仍让TX继续。
预算显式保留旧decoder的65535点lookahead，短发射仍接满首次同步需要的1048576点。
三会话ADC上限176,160,768字节；TX波形合计8,658,944字节；raw+guard载荷合计15,007,744字节。
数据载荷上限合计199,827,456字节，另为每会话索引/质量/日志预留16MiB；执行空间门仍每会话至少512MiB可用，
总存储/RAM上限须由最终实现对实际序列化方式核对，不把该载荷和当作包含任意日志的绝对总量。
每会话总deadline180秒、16个4MiB原始池、最多2个待提交处理块、GPU DSP分配上限4GiB作为候选继承界限。

空间检查及AGX唯一feature路径在执行时登记，P201仅流式缓冲、不暂存IQ；不在设计阶段创建空采集根。
停止路径为本单元STOP/SIGINT/TERM/deadline→停止精确TX子进程→取消对应Controller generation→排空并保留IQ。
异常不自动重发；USB占用退出和完整P201射频/scan mask/buffer恢复逐项核验，失败保存证据。
这些参数仍需实际安装二进制支持、设备身份/单一所有者、现有接法、空间和可用源成员通过后才能生成执行计划。

### 6.7 实施与验证边界

在现有实现上提取dataset adapter与frame profile，参数化L/行数/类数/Z/源行映射。
重点修改入口为`rml2018a_stream_dsp.py`的packet/Decoder、`rml2018a_campaign_gpu.py`的windows/quality、
`rml2018a_campaign_store.py`的行身份/shape，以及guard区间构造；不复制一套Controller或设备驱动。
源metadata只能进入源选择、索引和事后条件SINR，不能进入同步或泄漏拟合。

实施通过条件：128/1024往返形状与行号；丢帧不串号、尾帧不伪造；载波/CFO符号单音核对；
AM含载波输入不被统一去均值；漂移/非整bin泄漏下guard拒绝和raw回退；分块边界不丢样；
现有2018A固定输入处理数值回归；三套×八模型原生长度CUDA有限数值门。
无RF单音/噪声仅作程序检验，不恢复用户取消的自生成调制数据集研究。
实现与离线门通过后按三数据集分别执行上述有限先导，硬件收发成功/恢复与模型精度是分开的验收项。
本次仅完成设计及算术/源码约束核对，无硬件读写、IQ/权重复制、模型推理、训练或生产配置变更。
