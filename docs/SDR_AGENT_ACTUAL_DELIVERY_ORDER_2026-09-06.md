# SDR Agent 实际推进顺序 — 2026-09-06

实施基线：`7164ad08493b1b9d176d23fbf9deae08bc620acc`。
完成状态以 [权威 checklist](SDR_AGENT_PROJECT_CHECKLIST.md) 为准；本文只决定
下一步如何安排，不因排入计划就算完成，也不要求按章节或 S/V 编号顺序施工。
[第1—6章规划](CHAPTER_1_6_RX_ONLY_IMPLEMENTATION_PLAN.md) 保留各交付项的完整范围。

## 当前起点

P201 有界 RX、RF-v1 共享 RMS/四窗、epoch-10 FP16/full-logit 聚合与有限故障实收
已经完成。S1 准入/人工批准代码及隔离 Worker health 验证已经完成；已安装的
Controller/Planner/Web 服务尚未切换到该交付，生产识别仍不可用。
S2 统一结果/observation 也已完成源码、严格负例与保留真实报告 replay；未部署。
V1a 工具/采样规范与隔离 Web/HTTP/浏览器/删除验证完成，未部署，V1b 仍缺实际标签。
S3 生命周期源码和有限真实 Worker 成功/取消/超时/强杀恢复验证完成，未部署。
S4a 共享 GPU 租约、取消/故障释放及隔离 Spark/Mamba 实机验证完成，未部署。
S6a 应用识别归档、Web/终端查看和删除完成隔离 replay/演示验收，未部署；S6b 仍未完成。
S5 工程 Runner、联合 stop、自动 Spark 回灌、固定回归及有限实机/清理完成，未部署。
默认队列保留依赖顺序，完成状态见 checklist；当前下一个独立单元为 S6b。

## 默认推进队列

| 顺序 | 独立交付 | 为什么现在做 | 完成边界 |
| --- | --- | --- | --- |
| 1 | **S2：统一识别结果与 Planner observation** | Worker、存储、UI 和反馈都需要稳定字段 | 四状态、数字 ID/名称状态、来源/时间/身份、校准与拒识引用；内部完整记录与紧凑摘要分离；验证已有 batch 的转换及负例；不冻结生产阈值 |
| 2 | **V1a：RF-v1 证据工具与采样规范** | 独立标签是耗时且无法靠软件替代的依赖，应尽早开始 | 版本化导入/派生、原始 capture 与独立标签依据关联、旧包不改写、人工删除；预注册覆盖/样本量依据及校准/验收分组；工具通过不等于标签已经到位 |
| 3 | **S3：Worker 生命周期与取消** | 在串行调度和 Runner 接入前，先使任务生命周期可靠 | 应用队列上限、整批 deadline、取消确认、强杀/重启 spool 清理、实例/generation 隔离和指标；复用有限输入做实机故障验证 |
| 4 | **S4a：共享 GPU 推理租约** | Runner 闭环需要保证 Spark 与 Mamba 活跃推理不重叠 | 两模型可常驻；推理顺序与取消/故障释放有实机证据；先交付调度正确性，持续资源验收单列 S4b |
| 5 | **S6a：结果保存、查看和删除** | S2 稳定后即可交付可检查的结果，帮助后续联调 | 复用 AGX SQLite/终端/Web，完整有界记录、默认不留 IQ、人工删除；先用 replay 或明确实验结果验证，真实闭环界面验收留 S6b |
| 6 | **S5：Runner 执行、联合 stop 与 Spark 回灌** | 此时结果合同、Worker、GPU gate 和查看路径都可用 | one-shot/interactive/cruise 的批准、预算、audit、停止及迟到结果隔离；固定 Planner 回归；只走工程验证路径，生产 capability 不临时开启 |
| 7 | **S6b：真实闭环结果验收** | S5 后才能证明用户看到的是本次执行结果 | 实际结果保存/恢复/删除、四状态展示、Spark 只读紧凑摘要且不把无标签 top-1 说成事实；依赖 S5/S6a |
| 8 | **S4b + O1a：资源与上线准备** | 使用接近最终闭环的负载才有代表性 | S4b 独立交付资源/热稳定性测试；O1a 再独立交付可重复故障/fuzz、日志轮转、健康告警和升级回滚流程；测试矩阵提前登记 |
| 9 | **V2：冻结校准与拒识** | 只有 V1b 的独立证据达到预注册覆盖条件后才可完成 | validation/独立校准集拟合，独立验收组报告准确率、拒识率、false acceptance、ECE/NLL；不调 locked test |
| 10 | **V3b：一次 locked test 准入** | 模型身份、名称处理、温度/阈值及判定规则必须先冻结 | 依赖 V2/V3a；只查看一次准入结果，失败保留失败证据，不用同一 test 反复调参；重训仍由用户负责 |
| 11 | **A1：部署、RX-only 矩阵验收与 capability** | 软件、数据和运行证据在这里汇合 | 版本化 production profile、可回滚 Worker/Controller 部署、实际产物哈希和有限频率/增益/session 验收；先人工批准，全部门通过才允许实时能力为 true |
| 12 | **O1b：24 小时完整闭环验收** | 单设备长期测试不能证明最终系统持续运行 | 明确有限预算/停止/恢复/清理；运行中遵守已经批准的识别策略，不能自动绕过人工批准；自动触发识别是否放开仍单独决策 |

表中两个同格项也分别测试、清理、提交和推送，不合并成一个跨功能大提交。

## 尽早启动、按条件插入的工作

- **V1b：独立 known-RF/OOD 标签与覆盖证据。** V1a 完成后即可由用户或独立
  来源准备/审核。记录已知类、噪声/空闲、类外及混合/低质量情况，保留来源、
  session/day、谱域条件和歧义。Agent 可以做校验工具，不能把模型预测变成标签。
  工具可用、拿到少量样本、覆盖达标是三个不同状态。
- **V3a：数字 ID/文本名称映射证据。** 可在 V1a 时开始核对；在独立标签进入
  校准前，应确认数字 ID 的含义和对应依据。证据不足时继续使用可信数字 ID，
  文本保持 provisional，不因界面需要显示名称就宣布争议已解决。
- **V2 可前移。** 如果 V1b、V3a 和 S2 校准读取合同提前具备，V2 可插入任一
  已完成交付之后，无需等 UI 或长时测试。V3b 仍只能在规则冻结后开展；A1 仍须
  等所有软件、资源和数据准入门齐备。
- **S6a 可与 S3/S4a 调换。** 若模型环境或 GPU 验证窗口暂不可用，S2 完成后可
  先做存储/UI；不要为等待硬件而停止有价值的软件工作。完整 S6 不提前勾选。
- **故障/fuzz 随修改覆盖。** S2/S3/S5 各自补本接口负例，不把所有故障验证拖到
  O1a；O1a 的目标是统一可重复验证和运维交付，不机械重跑已冻结模型实验。

这是任务依赖安排，不要求多 Agent 并行。若 V1b 仍缺数据，继续完成可独立推进的
软件单元；到 V2/V3b/A1 时明确记录缺失证据，不伪造数据、不通过改配置绕过。

## 每次新任务的执行规则

1. 先读 `AGENTS.md`、权威 checklist、本文件和拟修改子系统的相关文档。
2. 核对分支/HEAD/工作区，从本表选择依赖已满足且尚未完成的**一个独立交付**。
   S1/S2/V1a/S3/S4a/S6a/S5 已完成，当前下一个明确目标是 S6b；不重做已完成单元。
3. 小单元完成条件需写清；S4a 不代表 S4b 完成，S6a 不代表 S6b 完成，V1a 不
   代表 V1b 有了独立标签。清单保留这些区分。
4. 测试、适用的实机验证、精确临时数据清理、checklist 更新和 diff review 均
   完成后，创建聚焦提交并立即 push。只做文档规划无需创建采集临时目录。
5. 始终沿用冻结 epoch-10/FP16/RF-v1；不重跑完整精度实验、不自行训练、不用
   locked test 调试。生产架构固定 P201 RX1/RX0/A_BALANCED → AGX；无 TX、
   FPGA/BOOT 或 NX offload。第7章设备/辐射源身份识别不加入当前关键路径。
