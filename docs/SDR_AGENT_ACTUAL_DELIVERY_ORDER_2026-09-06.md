# SDR Agent 实际推进顺序

更新：2026-09-10。本文只维护施工顺序，不复制完成状态。
完成条件和证据以[权威 checklist](SDR_AGENT_PROJECT_CHECKLIST.md)为准；
范围以[第1—6章规划](CHAPTER_1_6_RX_ONLY_IMPLEMENTATION_PLAN.md)为准。
历史实验结果按[验证索引](validation/README.md)查询，不再在本文件逐次追加旧排期。

## 当前执行选择

用户要求暂缓训练、微调和进一步模型诊断，先推进模型以外的工作。
[接收域适配建议](reference/RF_DOMAIN_ADAPTATION_DEFERRED_2026-09-07.md)只作后续参考。

S1/S2/V1a/S3/S4a/S6a/S5/S6b/S4b/O1a 已有源码、对应隔离验证和清理，不能重做。
[统一 CLI](validation/UNIFIED_CLI_VALIDATION_2026-09-07.md)已安装：交互、脚本与
恢复使用同一个 `sdr-agent`，旧 CLI 兼容修复已结束。当前 Controller/交互代码已
进入已安装制品。[Web 后台升级与会话恢复验收](validation/WEB_RECOVERY_UPGRADE_VALIDATION_2026-09-07.md)
也已完成实际安装、回滚和清理；现有归档界面已进入 Web 制品，Worker、profile/
校准和准入配置未整体部署，这不是 A1。

[非模型运维配置部署](validation/OPERATIONS_RX_DEPLOYMENT_VALIDATION_2026-09-08.md)已完成：
Web/Planner 日志限额、实际 health 兼容、只读定时检查、本地去重告警和配置回滚
均已验收。随后用户授权的[日常 RX 使用验收](validation/DAILY_RX_USE_VALIDATION_2026-09-08.md)
也完成三段有限实收、结果管理、停止/监控共存和状态文案修正。上述已完成单元
不要重做。

## 后续选择

2026-09-10用户将实验频率改为3.5GHz并明确允许当前室内发射、确认双端宽频
弹簧天线。已完成[3500MHz一次低增益单音](validation/B210_3500_TONE_VALIDATION_2026-09-10.md)：
传输/恢复通过但单音门未通过，不自动升增益或重复发射。若继续收发联测，
先界定下一轮增益/天线或衰减链路参数并重新登记有限矩阵；目前不把3.5GHz
链路视为已通过，不恢复模型诊断。衰减器购买讨论不等于已经同轴连接。

用户随后确认双端50Ω负载并要求继续，已完成[3500MHz负载对照](validation/P201_3500_TERMINATION_VALIDATION_2026-09-10.md)：
六段背景接近上轮天线停发水平，采集/恢复完成。当时双端负载、外部停发；
后续联测需实际衰减链路参数或新天线方案，不能在负载接法下默认恢复空口发射。


随后用户报告接回天线并明确要求重发，已完成[一次原参数重试](validation/B210_3500_RETRY_VALIDATION_2026-09-10.md)，
单音仍未通过，发射已停止。当前按双端天线记录；继续排查端口/输出/天线或衰减链路，
不自动追加发射或升级增益，不恢复模型。

随后用户明确同意TX0/10/20dB有限对照，已完成[三档矩阵](validation/B210_3500_GAIN_VALIDATION_2026-09-10.md)。
三档都未确认单音，TX已停发、恢复/清理完成；当前双端天线。后续先核对实际输出、端口/时序
或可核算的同轴衰减链路，不自动加到更高增益或追加发射，不恢复模型。

用户随后明确要求历史TX70/RX50，已完成[3500MHz原增益单次对照](validation/B210_3500_TX70_RX50_VALIDATION_2026-09-10.md)。
原单音门仍未通过，TX已停发、状态恢复；后续核对实际输出/端口/时序与天线或衰减链路，
不能继续只归因于尚未恢复历史增益，不自动重发或恢复模型。

随后按用户要求[切回2440MHz](validation/B210_2440_RETURN_VALIDATION_2026-09-10.md)，保持TX70/RX50，
一次单音原门仍未通过，停发背景突发明显。当前双端天线、外部已停发；不自动追加发射。
后续应围绕可控链路与来源/时序证据界定单元，不能把候选峰或背景突发直接当成已确认单音/Wi-Fi。

随后用户换天线并要求同参数重测，已完成[2440MHz换天线单音通过](validation/B210_2440_ANTENNA_CHANGE_VALIDATION_2026-09-10.md)。
当前TX70/RX50、2440MHz单音可见性有证据，TX已停发、状态恢复。背景突发仍在，旧失败保留；
下一单元应基于当前可用接法界定，不重做已完成试验、不自动恢复模型或认定天线因果/调制保真。

用户随后换433MHz天线，已完成[433.920MHz单次单音通过](validation/B210_433920_ANTENNA_VALIDATION_2026-09-10.md)。
当前433MHz天线、TX已停发、状态恢复；本次背景较平稳但总RMS不更低，不能称已测得更低底噪。
后续有限链路单元可基于这一接法界定，不自动重发、改变增益或恢复模型；具体更换端/型号仍未报告。






2026-09-09用户明确恢复非模型的50Ω负载物理排查，已按实际换线确认完成
负载→天线→负载反向三阶段比较，见[本轮验证](validation/P201_TERMINATION_BACKGROUND_VALIDATION_2026-09-09.md)。
本次物理隔离单元已完成，不为原文字面顺序再重复换线/采集。若继续调查，
应围绕天线输入的外部活动另界定有限非模型单元；具体来源尚未定位，
不由此恢复发射、模型实验或提升完整RF/生产资格。

随后用户要求查清干扰分布，已完成[2.4GHz三轮扫描及独立复测](validation/P201_BACKGROUND_MAP_VALIDATION_2026-09-09.md)。
又按用户要求完成[5GHz常见Wi-Fi中心三轮抽样及复测](validation/P201_WIFI5_BACKGROUND_VALIDATION_2026-09-09.md)，
只覆盖登记中心的1.5MHz窗口，不能当成5GHz全带连续覆盖。
用户指定的[历史宽频记录对照](validation/HISTORICAL_BACKGROUND_COMPARISON_2026-09-09.md)
也已完成：频段模式大体对应但存在变化，不能把不同增益/带宽/统计的读数
直接等同，或将历史谱占用率解释为当前时间突发比例。
当前应基于保留的频率/时间分布讨论来源控制实验或输入质量展示，不自动
重扫或把本轮较弱频段固定为长期安全信道；模型与发射边界保持。

当前已授权的这批非模型独立交付已结束。先按已安装日志/监控观察日常使用中的
实际问题；如有新功能或运行问题，另界定独立单元，不自行增加巡航或采集负载。
模型工作仍暂停，没有可在证据不足时自动启动的校准、locked test 或 A1 单元。
恢复识别主线须用户明确选择，并先满足下列独立证据条件；O1b 仍是最终完整闭环
验收，不能把仅 Web/Planner 监控运行满一天记作完成。

## 识别主线：按证据条件恢复

| 顺序 | 单元 | 前置条件与边界 |
| --- | --- | --- |
| 1 | V1b | 获得并审核独立 known-RF/OOD 标签及覆盖；V3a 已按用户指定的 4090 原始表冻结 server-v1，生产映射引用随 A1 接入。名称映射不替代独立 RF 标签 |
| 2 | V2 | 用合格 validation/独立校准组拟合和冻结温度、拒识规则；独立验收组报告准确率、拒识、false acceptance、ECE/NLL |
| 3 | V3b | 模型、映射、profile、精度及规则冻结后执行一次 locked-test 准入；失败保留证据，不反复使用同一 test 调参 |
| 4 | A1 | 证据齐备后的协调部署、回滚、真实 RX 矩阵与实际 capability；识别先保持人工批准 |
| 5 | O1b | 最终完整闭环的有限预算 24小时验收，含停止、恢复、日志与清理；不绕过批准策略 |

用户尚未恢复模型工作，不能仅因排在表中就执行。非模型运维部署不提前勾选 A1，
S4b 的 GPU 温度缺失按明确豁免保持未测，不再用它阻塞已通过的资源单元。

## 固定边界与外部信号源

- 冻结 epoch-10、FP16 autocast + FP32 权重、RF-v1；不自行训练、不重跑完整精度实验、不读取 locked test。
- `recognizer_available=false`；数字 ID 可信，文本 provisional；独立证据不足时不冻结生产温度或拒识阈值。
- P201 Linux/IIO 有界 RX → AGX 处理。当前天线 RX1，身份 RX1/RX0/A_BALANCED；sdrd 可在启动配置选择 RX1/RX2，冻结 profile 仍只接纳 RX1。
- 用户曾明确授权 NX+B210 在 2.4 GHz 作有限外部信号源；这不增加生产 Agent/P201 TX 能力，本轮 Web/运维工作无需发射。
- 未标注实收只证明链路与质量，不能把 top-1、置信度或 train 波形诊断补成独立验收标签。RF 失败及保留证据见验证/证据目录；继续物理来源隔离需确认适当负载或衰减器，不无衰减直连 TX/RX。
- FPGA/BOOT、P201 TX 和 NX 推理 offload 不进入主线；第7章辐射源身份识别按用户 2026-09-08 指令暂缓，已移出当前待办，须明确恢复后再排期。

## 每次交付

核对分支/HEAD/工作区，读 AGENTS 与相关合同；完成适当测试及实机验证后，精确
清理本单元临时数据、登记最小必要证据、更新 checklist 与验证记录、review diff，
聚焦 commit 并立即 push。不得删除用户应用结果或已有保留证据来充当开发清理。
