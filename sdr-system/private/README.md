# Local SDR credentials

This directory may contain the local `sdr-credentials.env` file used for the
owner-authorized P201 Pro SSH connection. The credentials file is ignored by
Git and must never be committed, copied into reports, or printed by scripts.

The public repository records only whether factory-default credentials remain
enabled and whether rotation is required.
